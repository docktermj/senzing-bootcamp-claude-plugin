# TODO

This are ideas for future specs.

- Drop the "reconcile later / porting-phase" language around the MCP temporary-license option (INV-036 works at runtime; `submit_feedback` is live) — `module-01/phase1-discovery.md:153-155`, `module-02/SKILL.md:366-368,513-517`.
- Add the best-value model/effort prompt to each module's `First:` step enumeration for parity with graduation (source of truth stays `ground-rules.md:169-200`) — coherence nit under INV-063.
- (Audit 2, 2026-07-19, cosmetic) Example-recap screenshot path: the embed at `docs/examples/bootcamp_recap.example.md:151` uses the examples-relative `docs/examples/bootcamp_recap.example.truthset.png` (so the re-rendered PDF embeds it), which won't self-render when the `.md` is viewed on GitHub. Optional: add a one-line note, or accept (the PDF is the intended artifact; INV-065 only requires regenerable).
- (Audit 2, 2026-07-19, cosmetic) `module-03-system-verification/phase3-report-close.md` Step 11 item 1 ("Terminate the web service") reads unconditionally even when the Truth Set visualization did not run (no server was started); mitigated by the line-129 note and Agent Rule 9 (`phase1-verification.md:69-70`). Optional: prefix item 1 with "If a web service was started (Truth Set visualization ran):".
- (Audit 2, 2026-07-19, cosmetic) `module-01-business-problem/phase2-document-confirm.md:10` yes/no question uses "or" in its subject ("data architecture or flows"); satisfies INV-008 and is outside INV-051, but INV-009 soft-discourages "or". Optional reword (e.g. "…architecture and flows…").
- (Audit 2, 2026-07-19, cosmetic — INV-079 polish) Bootcamper-facing FRAMING prose still names modules by number (permitted by INV-079, so optional): `module-07-query-visualize-discover/phase1-query-visualize.md:23,103,113`, `module-05-data-quality-mapping/phase3-test-load.md:7-8,15,18-21`, `module-05-.../phase2-data-mapping.md:198-206,286`, `module-06-data-processing/phaseA-build-loading.md:15-16`. Reword to module names for consistency.
- (Audit 2, 2026-07-19, trivial) `bootcamp-preparation/SKILL.md:128` cites the superseded INV-028 for the module banner; update the internal citation to INV-079.
- (Feedback 2026-07-20, fixture refresh) `docs/examples/bootcamp_recap.example.md:28` still shows the software-integration question as a Module 1 Q&A, but it was relocated to Bootcamp preparation (apparatus-exempt, not recapped) per INV-088 (`relocate-setup-questions-to-bootcamp-preparation`). Refresh the example recap so it no longer implies Module 1 asks it (INV-065 / `refresh-example-recap`).
