# Invariants

The following is the canonical list of invariants for the Senzing Bootcamp
Claude Plugin (SBCP). They are aspects of the SBCP that must **ALWAYS** be true.
Every spec under `specs/` begins "Maintain the invariant conditions in
@INVARIANTS.md" — no spec may be implemented in a way that violates any invariant
below.

Each invariant has a **stable ID** (`INV-NNN`). The ID is a permanent address:
specs, feedback triage, and audits reference invariants by ID, so IDs are never
renumbered or reused. New invariants are appended (see
[Maintaining this file](#maintaining-this-file)); existing ones are only edited
in place, never removed.

For hints when implementing the features these invariants describe, the Senzing
Bootcamp for Kiro Power may be consulted. Use the appropriate Claude Plugin
techniques to implement the features captured in the Kiro Power hook files and
steering files.

- Repository: <https://github.com/docktermj/senzing-bootcamp-kiro-powers/tree/main>
- Kiro Power: <https://github.com/docktermj/senzing-bootcamp-kiro-powers/tree/main/senzing-bootcamp>

## Maintaining this file

This file is machine-extended: as specs are implemented, the guarantee a spec
establishes becomes an invariant that all future work must maintain. The
`implement-spec` skill appends those invariants here. To keep the file
deterministically extensible, follow these rules:

1. **Never delete or renumber an existing invariant.** IDs are permanent
   references. If an invariant becomes obsolete, mark it (e.g. append
   "(superseded by INV-NNN)") rather than removing it.
2. **Editing an existing invariant** is allowed only to clarify wording without
   changing its meaning. A change of meaning is a *new* invariant with a new ID.
3. **To add an invariant**, add its ID to the matching group in [Index by subject](#index-by-subject)
   **in the same edit** (`tests/test_invariants_index.py` fails otherwise), then append it under
   [Invariants added from implemented specs](#invariants-added-from-implemented-specs)
   using the **next unused `INV-NNN`** — that is, one greater than the highest
   `INV-NNN` anywhere in this file (zero-padded to three digits). Record its
   provenance (the spec that established it and the date).
4. **Phrase every invariant as a single, testable MUST/ALWAYS condition.** If a
   spec establishes several, add one invariant per condition.
5. The `## Invariants added from implemented specs` heading and its append marker
   below must be preserved so tooling can locate the insertion point.

---

## INV-001 – INV-004: Foundational constraints

These apply to the SBCP as a whole and to every module, spec, and artifact.

- **INV-001** — The SBCP MUST run on the following platforms: Linux, macOS, and Windows.
- **INV-002** — The SBCP MUST be programming-language agnostic. (⚠️ **Scope stated 2026-07-31, no meaning change.** This binds everything the Bootcamper builds, runs, or takes away: their transform, load, query and visualization code, the generated project, and every artifact produced from their chosen language. It does **not** bind the plugin's own apparatus, which the Bootcamper neither authors nor maintains — hooks are Python 3 exec-form by **INV-052**, dev-only tests are stdlib Python by **INV-108**, and **INV-090**, which makes the visualization server language-native and states that it "strengthens INV-002", is the clearest existing statement of the distinction. **The boundary test:** a rule constraining what the Bootcamper's code must do MUST be stated as behaviour in the any-language contract, never only in a Python reference implementation. A rule that reaches generated code solely through the reference violates this invariant even though the reference itself is exempt — the failure **INV-164** and **INV-190** each had to record case by case, and the reason **INV-106** and **INV-124** state their requirements as behaviour rather than as helper names.)
- **INV-003** — The SBCP MUST be consistent, coherent, and complete.
- **INV-004** — The SBCP MUST be production-ready.

## INV-005 – INV-015: Whole-Bootcamp outcomes

When the Bootcamper runs the SBCP, these MUST hold across the entire Bootcamp.

- **INV-005** — Each question to the Bootcamper is preceded by "👉".
- **INV-006** — Each question is asked only once, unless the Bootcamper asks to have the question repeated.
- **INV-007** — All questions MUST be answered by the Bootcamper. The plugin cannot answer questions nor assume answers.
- **INV-008** — Questions are not ambiguous with respect to a "Yes" or "No" answer.
- **INV-009** — Questions are not "complex". The use of "or" is discouraged.
- **INV-010** — At any time, a Bootcamper can submit "Bootcamp feedback:".
- **INV-011** — At any time, a Bootcamper can change the verbosity of the Bootcamp.
- **INV-012** — All output MUST be relative to the Bootcamper's point of view. Output that is not important to the Bootcamper is suppressed.
- **INV-013** — All shipped modules are performed in order: Module 1 → 2 → 3 → 4 → 5 → 6 → 7. (Advanced Topics — performance, security, monitoring, deployment — ship as production-hardening follow-ups delivered at graduation, not as separate numbered Modules 8-11.) (An optional, non-counted Module 0 preamble is exempt from this ordering — see INV-072.) (Superseded by INV-076: Core runs all modules in order; Customized runs the selected subset in prerequisite order.)
- **INV-014** — Modules are not skipped unless requested by the Bootcamper. (Skipping the optional Module 0 preamble is a permitted, requested skip — see INV-072.) (Customized module selection is such a request: an optional module the Bootcamper does not select is a requested skip — see INV-076.)
- **INV-015** — Submitted bootcamp feedback is captured in `docs/feedback/SENZING_BOOTCAMP_PLUGIN_FEEDBACK.md`.

## INV-016 – INV-018: Bootcamp administration outcomes

- **INV-016** — Every hook's documented purpose begins with the word "to" (e.g. "to process your request", "to review what you said"), as recorded in the hooks' README Purpose column (the Kiro named-hook convention). This governs the hook's stated purpose/name, not necessarily the runtime string a hook emits to the model. (Wording clarified in place, no meaning change; Source: `audit-polish-cleanup`, 2026-07-19.)
- **INV-017** — All Markdown files (`.md`) are kept in appropriate places in the `docs/` directory. **Two** exceptions: the project's own root `README.md`, and the generated `production/` project deliverable, which carries its own Markdown files (e.g. `README.md`, `MIGRATION_CHECKLIST.md`, `GRADUATION_REPORT.md`). (⚠️ **Root-`README.md` exception added 2026-07-31, no meaning change.** This clause named only the `production/` exception and therefore forbade a file the invariant set has required from the start: **INV-050**'s layout tree places `README.md` at the project root (separately from `docs/README.md`), `ground-rules.md`'s project-root whitelist names it explicitly and then bans every *other* root `.md`, and Module 1 Phase 2 has a step headed "Update README.md". The shipped placement rule in `ground-rules.md` has always stated both exceptions; only this invariant's text was incomplete, so nothing moves and no guidance changes. `tests/test_root_readme_exception.py` now asserts this clause and that whitelist agree, so the two cannot drift apart again.)
- **INV-018** — All code files (e.g. `.py`, `.java`, etc.) are kept in appropriate places in the `src/` directory.

## INV-019 – INV-027: Bootcamp preface outcomes

INV-019–INV-021 (the entity-resolution concepts outcomes) have been relocated out of the preface
into the optional **Module 0** (superseded by INV-073; see INV-072). The preface proper now begins
at INV-022 and presents INV-022–INV-027 in order.

- **INV-019** — A banner is presented, "ENTITY RESOLUTION CONCEPTS". (superseded by INV-073.)
- **INV-020** — A description of Entity Resolution is given. (superseded by INV-073.)
- **INV-021** — The Bootcamper is asked if they want to discuss/explore Entity Resolution before beginning. (superseded by INV-073.)
- **INV-022** — A banner is presented, "WELCOME TO THE SENZING BOOTCAMP!".
- **INV-023** — An overview of the Bootcamp is given.
- **INV-024** — The Bootcamper is asked what level of detail they would like in the Bootcamp output. (Superseded by INV-075: now asked in the Bootcamp preparation module, not the preface.)
- **INV-025** — The Bootcamper is asked to choose a track. (Superseded by INV-076: the track question is replaced by the Core-vs-Customized path choice plus module selection, asked in the Bootcamp preparation module.)
- **INV-026** — The Bootcamper is asked which programming language they would like to use. (Superseded by INV-075: now asked in the Bootcamp preparation module, not the preface.)
- **INV-027** — The Bootcamper is asked if they have any questions at this point.

## INV-028 – INV-032: Per-module outcomes

These hold at the boundaries of every module.

- **INV-028** — At the beginning of each module, a banner is presented "MODULE n: [title]". (Superseded by INV-079: the banner is "MODULE: [title]", name only, no number.)
- **INV-029** — At the beginning of each module, show modules completed, the current module, and upcoming modules.
- **INV-030** — At the beginning of each module, explain what is true before the module and what will be true after completing the module.
- **INV-031** — At the beginning of each module, enumerate the steps that will be taken in the module.
- **INV-032** — At the end of each module, give the Bootcamper a list of what was accomplished, what files were produced, why it matters, and what's next.

## INV-033 – INV-046: Module-specific outcomes

**Module 1**

- **INV-033** — The Bootcamper is asked if they want to see a gallery of common business problems that Entity Resolution solves.
- **INV-034** — The Bootcamper describes the business issue requiring Entity Resolution.

**Module 2**

- **INV-035** — The Senzing SDK is installed.
- **INV-036** — A Senzing license is established, by one of:
  - Using the built-in evaluation license.
  - Identifying an existing, owned Senzing license.
  - Requesting a temporary Senzing license via the Senzing MCP server.
  - (The interactive custom-License-Key prompt is relocated out of Module 2 to Module 4 and volume-gated by **INV-093**; the built-in evaluation license is still established here in Module 2 with no prompt, so this invariant remains satisfied at Module 2. Clarification: `single-license-gate-at-data-processing`, 2026-07-22.)
- **INV-037** — A database is initialized for use by Senzing.

**Module 3**

- **INV-038** — The Bootcamper **ALWAYS** sees a dynamic web-app visualization of the Truth Set to verify that Senzing works on the Bootcamper's workstation. (Superseded by INV-077: the visualization is delivered by the selectable Truth Set visualization module — guaranteed when selected, always in Core.)

**Module 4**

- **INV-039** — Data sources to be loaded into Senzing are identified. This is known as the "raw" data.

**Module 5**

- **INV-040** — The "raw" data is analyzed to see if mapping and transformation is needed. (CORD data does not require mapping nor transformation.) (⚠️ **The parenthetical is partly superseded by INV-198, 2026-08-11.** It is true of the `truthset` class it was written for and false in general: `get_sample_data(dataset='las-vegas', source='PPP_LOANS')` returns records carrying eight Entity Specification attributes and eleven raw source columns — structurally loadable, not fully mapped — so a CORD source can require mapping. The **main clause is unchanged** — it is what INV-198 strengthens, since the analysis now answers two questions instead of one. INV-041's and INV-042's exemptions are unaffected, since both are conditioned on a source having been *fast-pathed*, which INV-198 only makes harder to reach.)
- **INV-041** — The "raw" data undergoes mapping to determine how to transform data into "Senzing-ready" data. (CORD fast-pathed sources are exempt — they route directly to loading with no mapping, per INV-040; the already-Senzing-ready fast-path class is CORD, offered only for `provenance: cord`, so a non-CORD source that looks Senzing-ready still goes through mapping. Clarified 2026-07-19 to mirror INV-040's caveat, and 2026-07-20 to state the fast-path class is CORD (`module5-fastpath-cord-only-vs-senzing-ready`); no meaning change.)
- **INV-042** — Code is created to transform data according to mapping rules. (CORD fast-pathed sources need no transformation, so no transform code is created for them, per INV-040; the fast-path class is CORD, offered only for `provenance: cord`. Clarified 2026-07-19 to mirror INV-040's caveat, and 2026-07-20 to state the fast-path class is CORD (`module5-fastpath-cord-only-vs-senzing-ready`); no meaning change.)
- **INV-043** — Using the code, the "raw" data is transformed to Senzing-ready data. (CORD fast-pathed sources are already Senzing-ready and route directly to loading with no transformation, per INV-040; the fast-path class is CORD, offered only for `provenance: cord`. Clarified 2026-07-19 to mirror INV-040's caveat, and 2026-07-20 to state the fast-path class is CORD (`module5-fastpath-cord-only-vs-senzing-ready`); no meaning change.)

**Module 6**

- **INV-044** — Code is created to load the transformed data.
- **INV-045** — Using the code, the transformed data is loaded into Senzing.

**Module 7**

- **INV-046** — Code is created to query, visualize, and discover the results of Senzing Entity Resolution. Query code is always created; visualization and discovery are **offered**, and their code/artifacts are created when the Bootcamper accepts — a Bootcamper's decline is a requested skip (INV-014). (Clarified 2026-07-19 to reflect the offered/opt-in reality permitted by INV-014; no meaning change.)

## INV-047 – INV-049: Graduation outcomes

- **INV-047** — A banner is presented, "GRADUATION".
- **INV-048** — A recap PDF, `docs/bootcamp_recap.pdf`, is **always** created. It must be very professional looking (iterate to make it look professional) and contains "Information Shared", "Questions & Responses", "Actions Taken", and "Journal". (Wording clarified in place 2026-07-20; no meaning change. Source: `drop-trophy-wording`.) (The fourth subsection "Journal" is renamed to "End-of-Module Summary" by INV-103; legacy "Journal" headings remain tolerated.) (⚠️ **Scope of "always" stated 2026-07-31, no meaning change.** "Always" means the bootcamp flow always produces the recap PDF and its production is never optional, skipped, or gated — the sense INV-129, INV-157, INV-163 and INV-173 rely on when they cite this invariant as non-blocking. It is **not** a requirement that the generator emit a file for input that is **not a recap**: **INV-110** requires it to write no output file and exit non-zero on structural mismatch, and that is the sole exception. The bootcamp flow cannot reach it, because graduation authors the recap before rendering it. A recognisable-but-incomplete recap still warns, renders and exits 0.)
- **INV-049** — The `production/` directory is populated.

## INV-050: Project layout

- **INV-050** — The generated Bootcamp project MUST follow this layout:

  ```text
  senzing-bootcamp/
  ├── README.md                          # Project overview
  ├── config/                            # Configuration & progress tracking
  │   ├── bootcamp_preferences.yaml      # Bootcamp settings (language, options)
  │   ├── bootcamp_progress.json         # Current module + completed modules
  │   ├── data_sources.yaml              # Registered data source definitions
  │   ├── engine_config.json             # Senzing engine configuration
  │   ├── license.json                   # Detected Senzing license capacity (written in Module 4)
  │   ├── cord_metadata.yaml             # CORD dataset provenance snapshot (written in Module 4)
  │   ├── session_log.jsonl              # Session activity log (reserved)
  │   └── visualization_tracker.json     # Visualization run tracking (reserved)
  ├── data/                              # All data artifacts
  │   ├── raw/                           # Source data as received
  │   ├── senzing-ready/                 # Senzing-mapped JSONL output
  │   ├── mapping/                       # Mapping working data (specs, samples, intermediates)
  │   ├── samples/                       # Sample fixtures
  │   ├── temp/                          # Scratch/intermediate working files
  │   └── backups/                       # Data backups (reserved)
  ├── database/                          # SQLite Senzing repository
  ├── src/                               # Source code
  │   ├── transform/                     # Source-to-Senzing mappers
  │   ├── load/                          # Loading & redo processing
  │   ├── query/                         # Query, search & discovery
  │   ├── server/                        # Truth Set viz server, generated in the chosen language (INV-090; Python runs the bundled reference)
  │   ├── system_verification/           # Pipeline verification (synthetic VERIFY data) + Truth Set viz artifacts
  │   ├── scripts/                       # Setup & data-generation utilities
  │   ├── resources/                     # Downloaded Senzing resources
  │   └── utils/                         # Shared helpers
  ├── docs/                              # Documentation
  │   ├── README.md
  │   ├── business_problem.md
  │   ├── business_problem.pdf           # Rendered at graduation, Step 5b (best-effort)
  │   ├── data_source_evaluation.md
  │   ├── data_source_evaluation.pdf     # Rendered at graduation, Step 5b (best-effort)
  │   ├── bootcamp_journal.md            # (legacy; superseded by the consolidated bootcamp_recap.md, INV-085)
  │   ├── bootcamp_recap.md
  │   ├── bootcamp_recap.pdf
  │   ├── bootcamp_data_discoveries.md   # Always produced in Module 7 (both .md and .pdf)
  │   ├── bootcamp_data_discoveries.pdf
  │   ├── REVISIT_BOOTCAMP.md            # Return guide, written at graduation (INV-094)
  │   ├── completion_summary.md          # (reserved)
  │   ├── stakeholder_summary_module{n}.md
  │   ├── mapping/                       # Per-source mapping docs
  │   ├── reference/
  │   ├── progress/
  │   ├── visualizations/                # Generated HTML visualizations
  │   └── feedback/
  │       └── SENZING_BOOTCAMP_PLUGIN_FEEDBACK.md
  ├── licenses/
  │   └── g2.lic                         # Senzing license
  ├── logs/                              # Run logs & result summaries
  ├── backups/                           # Project backups/archives; `backups/revisit/` holds the
  │                                      #   graduation revisit/resume bundle (INV-094)
  ├── monitoring/                        # Monitoring assets (reserved; Advanced Topics follow-up)
  ├── tests/                             # Test suite (reserved)
  └── production/                        # Production project (generated at graduation)
  ```

  The tree lists the canonical/primary artifacts, not an exhaustive whitelist: additional
  per-source and per-module working files the flow creates — e.g. `config/mapping_state_*.json`,
  `config/er_current_*.json` / `config/er_baseline_*.json`, `config/fallback_sources.yaml`, and the
  per-module `docs/*.md` write-ups (`docs/data_source_locations.md`, `docs/loading_strategy.md`,
  `docs/results_validation.md`, etc.) — live under the enumerated directories and are consistent
  with this layout. (Clarified 2026-07-20 per the final-review audit; no meaning change.)
  (Corrected 2026-07-28 by the deep-dive audit: `config/license.json` was annotated "written in
  Module 2", but `single-license-gate-at-data-processing` (2026-07-22) moved licence detection to the
  single volume-gated Module 4 gate, which is where `SzProduct.get_license()` is called and the file
  saved — Module 2 only reads the detected limit. The file and its purpose are unchanged; only the
  attribution was stale.)
  (Clarified 2026-07-26 by the deep-dive audit: three always-produced deliverables the tree had
  omitted — `docs/bootcamp_data_discoveries.md`/`.pdf` and `docs/REVISIT_BOOTCAMP.md` — are named
  explicitly, and `backups/` is no longer annotated "reserved" since INV-094 requires the
  revisit bundle there. All three already lived under enumerated directories, so this records
  what the layout already permitted; no meaning change.)
  (Clarified 2026-07-31 by `production-readiness-audit`: `docs/business_problem.pdf` and
  `docs/data_source_evaluation.pdf`, rendered at graduation Step 5b, are named for the same reason
  — the tree already named every other keepsake PDF the flow produces, and the bootcamp treats
  "rendered as a styled PDF" as the signal that a document is a keepsake rather than a working file.
  Both are **best-effort**, not always-produced: Step 5b warns and continues when a source document
  is absent, refused, or fails verification (INV-048), so their absence is conforming. As in 2026-07-26
  they already lived under an enumerated directory; no meaning change.)

  (⚠️ **Every edit to the tree above is bound by INV-202 — recorded 2026-08-12, no meaning change.**
  Each leaf entry MUST be either referenced somewhere under `plugins/` or annotated in its own
  comment as reserved/superseded/legacy/future, and an entry that is no longer produced MUST
  **gain the annotation rather than be deleted** — the tree is cited widely and quoted in audits,
  so shortening it loses the record that the artifact was once intended. The comment column is
  **load-bearing data**, not decoration: a scan that discards it reports correctly-annotated
  entries as defects. `tests/test_invariant_layout_tree.py` enforces this. The rule has bound
  every edit here since `layout-tree-reconciliation` (2026-07-17, commit `cc46a55`) but was
  registered only on INV-202, and the spec that established INV-202 deliberately left this
  invariant untouched — so an editor working from the tree itself had no way to learn it.)

## Invariants added from implemented specs

Invariants below were established by implemented specs and are maintained by the
`implement-spec` skill. Append new entries directly beneath the marker using the
next unused `INV-NNN` (see [Maintaining this file](#maintaining-this-file)).

### Index by subject

Every invariant below appears in **exactly one** group, and `tests/test_invariants_index.py` fails if that stops being true. The list itself stays in
append order — IDs are permanent addresses (see [Maintaining this file](#maintaining-this-file))
— so this index, not the ordering, is how you find the rules on a subject.

INV-001 – INV-050 are **not** indexed here: they are the bootcamp's own outcomes and are
already grouped into sections above. Everything below is a development rule.

Superseded rules are listed separately per group so a reader looking for what applies never
reads a retired rule by accident. They stay in the file because their IDs are permanent
addresses. Supersession here has **two** forms, and the difference decides whether you may
skip the rule:

- **Fully superseded:** the whole invariant is retired and its replacement carries
  everything it said. Skip it.
- **Partly superseded, or superseded then restored:** one clause was replaced while the
  rest still binds, or a later invariant reinstated the behaviour. **Read it** — for several
  of these the invariant is still the only statement of what it requires.

No count is given for either. A count in prose beside an append-only list has nothing
coupling it to an append — rule 3 and `tests/test_invariants_index.py` bind the *ID list* to
every new invariant, and nothing binds a sentence about the list, so the figures here drifted
silently and were wrong when this was found (2026-07-31). Derive them from the file if you
need them.

- **MCP sourcing and tool contracts** — where Senzing facts come from, and what a tool response means.  
  INV-080, INV-125, INV-136, INV-149, INV-150, INV-160, INV-165, INV-169, INV-181, INV-190, INV-192, INV-194, INV-204, INV-205, INV-206, INV-208, INV-212, INV-228, INV-230, INV-231, INV-234
- **SDK usage: methods, flags and responses** — confirming a call's shape before writing it, and reading what comes back.  
  INV-089, INV-115, INV-132, INV-145, INV-148, INV-151, INV-152, INV-164, INV-179, INV-180
- **Data quality, mapping and validation gates** — measuring a source honestly, and what a gate's finding may be used for.  
  INV-084, INV-117, INV-118, INV-128, INV-144, INV-173, INV-174, INV-177, INV-197, INV-198, INV-203, INV-229
- **Recap and certificate content** — what the keepsake must contain and whose values it prints.  
  INV-059, INV-065, INV-085, INV-100, INV-103, INV-113, INV-126, INV-134, INV-156, INV-157, INV-170, INV-176, INV-187
- **Generator behaviour: rendering, encoding, reporting** — how a bundled generator must behave when something goes wrong.  
  INV-110, INV-111, INV-121, INV-129, INV-142, INV-143, INV-159, INV-161, INV-162, INV-163, INV-172, INV-178, INV-183, INV-184, INV-186, INV-193, INV-210
- **Visualization and screenshots** — the app, its offline guarantee, and capturing it faithfully.  
  INV-070, INV-077, INV-081, INV-087, INV-090, INV-091, INV-106, INV-107, INV-122, INV-123, INV-124, INV-127, INV-130, INV-146, INV-147, INV-153, INV-154, INV-155, INV-171, INV-221, INV-223, INV-232, INV-235
  *Fully superseded — skip these; each names its replacement:* INV-071
  *Partly superseded — still binding; read the invariant:* INV-104 (its **tab enumeration** only → INV-155; every other guarantee stands)
- **Questions, gates and bootcamper-facing conversation** — how the Bootcamper is addressed and what is pinned verbatim.  
  INV-051, INV-054, INV-055, INV-056, INV-057, INV-074, INV-096, INV-099, INV-102, INV-112, INV-158, INV-211, INV-214, INV-220, INV-224, INV-225, INV-233
  *Partly superseded — still binding; read the invariant:* INV-079 (its recap `## Module N:` **heading clause** only → INV-085; it remains the only statement of the `MODULE: [NAME IN CAPS]` banner, the transition-question form and the `✅ Module complete` line)
- **Model and effort guidance** — what is recommended, when it is surfaced, and what is never configurable.  
  INV-098, INV-114, INV-138, INV-139, INV-140, INV-141
  *Fully superseded — skip these; each names its replacement:* INV-062, INV-064, INV-119, INV-120
  *Partly superseded or since restored — still binding; read the invariant:* INV-063 (**restored**: INV-137 reinstated this pause unconditionally, so the behaviour described here is once again the behaviour), INV-069 (**restored**: INV-137 reinstated this confirmation gate after a yes to the switch), INV-137 (its **trigger** only → INV-138; the pinned switch question, the gate-after-yes-only rule, the absence of any `model_guidance` preference and the apparatus-exempt carve-out all still bind)
- **Module flow, selection and progress** — which modules run, in what order, and what is recorded between them.  
  INV-058, INV-060, INV-073, INV-075, INV-076, INV-078, INV-082, INV-083, INV-092, INV-093, INV-094, INV-095, INV-097, INV-105, INV-131, INV-133, INV-195, INV-226, INV-227
  *Fully superseded — skip these; each names its replacement:* INV-068, INV-072, INV-088
  *Partly superseded — still binding; read the invariant:* INV-086 (its **recording-location framing** only → INV-087; the first-class-module, full-apparatus and not-apparatus-exempt guarantee otherwise stands), INV-101 (its **Docker-only scope** only → INV-195, which dispatches on the recorded container runtime; the progress-file recording requirement and the warn-and-continue contract otherwise stand)
- **Platform, shell, encoding and file placement** — cross-platform behaviour and where files go.  
  INV-052, INV-061, INV-066, INV-166, INV-167, INV-168, INV-175, INV-185, INV-188, INV-189, INV-199, INV-200, INV-218, INV-222
- **Security and privacy** — the one consented PII path, and what must never be written.  
  INV-109, INV-135
- **Feedback capture** — how bootcamper feedback is recorded so it can be triaged later.  
  INV-053, INV-067, INV-116, INV-196
- **The development record itself** — rules governing specs, the ledger, provenance, and the
  repo's own apparatus. **This group is the exemption** used by `coverage_reports.py shipped`:
  its members bind the development environment, so an ID here is never expected to appear in a
  shipped file under `plugins/`. File a new invariant here only when that is true of it.
  (INV-201, INV-209 and INV-213 were re-filed here from *MCP sourcing and tool contracts* on
  2026-08-13: each governs a **test**, a **marker convention**, or a **spec's** own text rather
  than anything the Bootcamper's guide reads, so scoring them against shipped citations was
  measuring the wrong thing. No invariant's meaning changed; only its index group.
  **INV-108** followed on the same date from *Platform, shell, encoding and file placement*, and
  the report itself is what found it: it governs where the repo's own dev-only tests live and
  what they may import, so no shipped file should ever cite it — it surfaced only because its
  text names `plugins/` in a **prohibition**, telling tests to stay out. The propagate step is
  its reason, not its subject. Filter phrasing was deliberately **not** special-cased to hide it,
  which would only make the next genuine hit worded that way invisible.)  
  INV-108, INV-182, INV-191, INV-201, INV-202, INV-207, INV-209, INV-213, INV-216, INV-217, INV-219

<!-- New invariants go directly below this line. Format:

- **INV-NNN** — <single testable MUST/ALWAYS condition.> (Source: `<spec-name>`, YYYY-MM-DD.)

-->

- **INV-235** — A capture helper's printed label MUST describe what the capture **achieved**, never what its mode intended: where a capture can fall short (a page taller than the viewport, a clamp, a failed measurement) the label MUST distinguish the outcomes, a shortfall MUST be reported on stderr naming both the page height and the captured height, and the "full page" wording MUST be unreachable except on an actual full-page capture — including by *silence*, so the recorded default is the shortfall, not the success. This exists because INV-123 designates that printed label as the input to the caption a caller writes: a label stating the intent makes the helper the source of a false claim, and a caller obeying INV-123 exactly ("take the label, open the image") reproduces it faithfully into the Bootcamper's keepsake. ⛔ **A guard for this MUST assert the page's last content is present in the pixels, not that the image is tall enough to hold it** — the two come apart: measuring `scrollHeight` and screenshotting at exactly that height yields an image whose height equals the page height while the bottom of the page is still missing, because `--window-size` includes window chrome under `--headless=new` and the viewport is ~87px shorter than the image. Verified 2026-08-14: a 2671px page whose footer occupied 2613-2671 produced a 2671px PNG with white where the footer belonged, and a height-only assertion passes that build. The window-chrome offset MUST therefore be measured at runtime rather than hard-coded, since it varies by browser version and platform. (Narrower than INV-122, which governs which file shows which tab; this governs whether a capture's own description of itself is true.) Enforced by `tests/test_capture_single_page.py`. (Source: `single-page-capture-crops-to-the-viewport-and-calls-it-full-page`, 2026-08-14.)
- **INV-234** — Where an MCP tool answers a content request with a **listing** — metadata plus a URL rather than the bytes asked for — every shipped call site MUST either state that shape or cite the single central statement of it, and a per-tool prohibition that follows from a general rule MUST state the general rule and the property that triggers it rather than only the forbidden token. Concretely (verified server **1.32.9, 2026-08-14**): `find_examples` file retrieval, `generate_scaffold` and `download_resource` all return listings, so in each case the content is a second fetch and a step that saves the response saves a URL; `inline` may be passed to `download_resource` **alone**, because only its declared schema carries it (`filename`, `filenames`, `inline`, `version`) while `generate_scaffold` declares `language`/`version`/`workflow` and `find_examples` declares `query`/`repo`/`file_path`/`list_files`/`language`/`max_lines` — the consequence of INV-136, not a fact about the word. ⛔ **This is why the shape of the prohibition matters and not only its content:** stated as "never pass `inline`" the rule generalises wrongly, and a guide that has correctly internalised it will refuse the one call where `inline` is the documented remedy — stranding the firewalled Bootcamper the `on_failure` text exists for, on a step every later step depends on. A saved resource MUST additionally be size-checked against the response's own `size_bytes` before use, so a truncated fetch or a saved error page is caught at the fetch rather than surfacing later as content that is merely absent (INV-228's count-check discipline, applied to a resource). (Narrower than INV-160, which governs the elision shape wherever it appears; this one governs how a *call site* must document it and how a derived prohibition must be worded.) Enforced by `tests/test_download_resource_is_a_listing.py`. (Source: `download-resource-returns-a-url-not-the-specification`, 2026-08-14.)
- **INV-233** — Wherever shipped guidance instructs the guide to **end the turn on** a question, a pinned 👉 question MUST exist in the section that owns it: the same section, or — when the instruction names another step — that step's section. A cross-reference MAY state the rule while the named step owns the wording (INV-183), but the wording MUST actually be there. An instruction with no resolvable question leaves the guide **no legal move**: improvising a gate question breaches INV-056, which pins every gate question's wording so it cannot drift at runtime, while advancing without asking breaks the promise the mode offer made one step earlier — so the failure is silent either way, because advancing anyway looks like normal progress. (Found by the 2026-08-14 phase-3 dry run: `phase2-data-mapping.md`'s INV-205 carve-out routed the entity-plan advance to "this module's own 👉" and step 10 carried none, for seven weeks. Not an edge case — any single-schema plan clears `mapping_workflow`'s 0.80 confidence bar, which triggers its verbatim *"Do NOT ask the user to confirm, approve, type YES, or proceed… Just advance"* fast path, exactly the case the carve-out exists to override.) Enforced by `tests/test_end_the_turn_questions_exist.py`. (Source: `verbose-mapping-requires-a-plan-gate-that-does-not-exist`, 2026-08-14.)
- **INV-232** — A visualization tab the app **suppresses** — one whose data does not exist, so `tabApplicable()` hides its nav button — MUST NOT be captured as a screenshot, and MUST NOT be counted in the capture manifest's `captured` list. It is recorded under `not_applicable`, kept **distinct from `not_present`**, because the two send a reader to different faults: `not_present` means the tab inventory drifted, `not_applicable` means this dataset has no such data and the run was fine. ⛔ **Skipping the image is only half the rule — the count is the half that was load-bearing.** `generate_recap_pdf.py`'s tab-coverage note derives its denominator from `captured` precisely so the number cannot be self-certified, so counting a suppressed tab yields a perfect score against a set the app never offered: the exact mirror of the under-count the manifest was introduced to prevent. ⚠️ **This is not INV-122**, which forbids saving *the default tab* under another tab's name; a suppressed pane really does render its own content, and the fault is that it is captured at all. Where capture cannot determine applicability (no inlined `__DATA__`, unreachable `/api/stats`, malformed data) every tab MUST be treated as applicable, since capture is best-effort by contract and must never degrade to capturing nothing. Because the rule lives in the app's JavaScript and is mirrored in Python across a boundary no import can cross, the two copies MUST be pinned to each other by a test — an unguarded second copy is precisely how the pane/button divergence arose. (Observed 2026-08-14 on Senzing 4.3.4: 4 records in one data source produced **six** PNGs for a page showing **five** tabs, `captured_count: 6, not_present: []`, with the Cross-Source image two lines of text over ~700px of background; a zero-multi-record dataset produced **three** such images. Verified in both directions afterwards — two sources plus a merge still captures all six.) Enforced by `tests/test_capture_suppressed_tabs.py`. (Source: `capture-screenshots-captures-tabs-the-app-suppressed`, 2026-08-14.)

- **INV-231** — The bootcamp's datastore `CONNECTION` MUST be persistent and shareable across processes, and `internal://` MUST NOT be offered as one anywhere in shipped text. ⛔ **The disqualifying fact is the bootcamp's own process model, not a server limitation the server failed to state.** `senzing_viz_server.py:1511-1517` constructs its own `SzAbstractFactoryCore("bootcamp_viz", settings)` and calls `create_engine()` in a **separate** process from the loader, reading the same `CONNECTION` from the project's engine-config file or `$SENZING_ENGINE_CONFIGURATION_JSON`, so a per-process in-memory datastore leaves it opening an empty one. The hazard is that `sdk_guide`'s `engine_config_notes` **recommends** `internal://` for "quick single-process dev/test on v4.3+" and the plugin routes the assistant to `sdk_guide` at runtime (INV-080), so the advice arrives unprompted and the recommendation reads before the limitation clause in the same entry. This is therefore INV-200's "override MCP-suggested paths" rule extended from a path to a connection string, and it applies the server's own note — *"it cannot be shared across processes, persisted, or used with external tools"* — rather than contradicting it. The failure it prevents is silent and **late**: both processes exit 0, every load reports success, and the blank render surfaces three modules later, which is the outcome INV-077 forbids. (Verified on MCP server 1.32.9, 2026-08-14, via `sdk_guide(topic='install', platform='macos_arm', language='java')`; `internal://` appeared **0** times under `plugins/` when the rule was written, so this pins correct behaviour rather than correcting a defect.) Enforced by `tests/test_internal_connection_string_rejected.py`. (Source: `internal-connection-string-breaks-the-viz-server`, 2026-08-14.)

- **INV-230** — A Senzing **dataset name** in shipped prose — bootcamper-facing or agent-facing — MUST use the spelling Senzing's own documentation uses, **confirmed against the MCP server rather than chosen** (INV-080). The closed-up form is reserved for **identifiers** — paths, state tokens, filenames, dataset keys — and an identifier MUST NOT be rewritten to match prose, because it is an address other files resolve against. ⛔ **Scope is deliberately dataset names, not every Senzing proper noun.** The general form would flag non-defects: `entity specification` appears lowercase twice against `Entity Specification` 46 times, and both lowercase uses are legitimate common-noun prose ("the Senzing entity specification (`senzing_entity_specification.md`)"), so a rule reaching that far would produce judgement calls instead of findings. (Observed 2026-08-14: **12 of 172** Truth Set occurrences read `TruthSet`, confined to three files, with `module-03-system-verification/phase1-verification.md:596` carrying **both spellings in one line** — which is what identified it as drift rather than a distinction. The server settles it and draws the line itself, on 1.32.9: `search_docs` returns the documentation page titled "Truth Set Setup" whose prose reads "the Senzing **truth set** demo data", while the closed-up form appears in that same corpus only inside identifiers — `truthset_config.g2c`, `actual_truthset_key.csv`, and `get_sample_data(dataset='list')`'s key `truthset`.) ⚠️ INV-079 governs **module names** and was never violated by this: "Truth Set visualization" was correct everywhere the module is named. Enforced by `tests/test_truth_set_spelling.py`. (Source: `truth-set-is-spelled-two-ways-in-shipped-prose`, 2026-08-14.)

- **INV-229** — A check that compares the engine's behaviour against an outcome the **guide itself predicted** MUST NOT report a mismatch as a failure of the Bootcamper's installation until the engine's own explanation has been obtained (`why_records` / `why_entities`, generated via the MCP tools) and found not to account for the difference; where it does account for it, the mismatch MUST be reported as an **expectation** mismatch with the install stated to be working, and such a check MUST be reported separately from checks that are unambiguous statements about the install. ⛔ **A prediction is not a Senzing fact** (INV-080): there is no MCP route that says how a given record set will resolve, so the module's own reasoning is the only source — which is exactly why its failure must not be attributed to the system. The load-bearing half is the *separation*: the other seven System-verification checks each assert something about the installation, so pooling them lets a miscalibrated prediction tell a bootcamper their working install failed, at the end of the module whose purpose is to tell them it works. (Observed on Senzing 4.3.4, 2026-08-13: three records for one person with the first name varied `Marisol`/`Mari`/`Marisol` and one record missing its phone resolved to **3** entities and the check reported FAILED; the same three with the name identical and only formatting varied resolved to **2** with a 3-record cluster and passed. The engine was right both times — a nickname variant on a record carrying one fewer corroborating feature is a defensible decline. The remedy is therefore twofold: define the cluster by explicit sameness constraints rather than by the phrase "trivial variation", and diagnose rather than grade.) Enforced by `tests/test_results_validation_is_diagnostic.py`. (Source: `verification-grades-the-engine-against-the-guides-own-prediction`, 2026-08-14.)

- **INV-228** — A step that writes a dataset obtained from the Senzing MCP server MUST verify the **written record count** against the count the server itself reported, per source, and MUST stop rather than proceed on a mismatch — expecting exactly `record_count` from an uncapped source URL and `min(record_count, download_url_max_records)` from a capped one. ⛔ **`get_sample_data` returns a PREVIEW plus URLs, not the dataset**, and the preview is clearly labelled as such in the response's own `citation.note` — so a step that saves `records` saves a sample. The count check is what makes the whole class of under-fetch visible: preview-only, rate-limited (the MCP download endpoint answers a burst with a 43-byte `Rate limit exceeded.` body under HTTP **429**, which `curl -sS -o file` writes *into* the JSONL as an unparsable line) and truncated all present identically otherwise — as a file that parses, renders, and misstates what Senzing did. Egress hosts MUST be named per URL **from the response**, never as a single general rule: `download_url` is MCP-hosted while `source_download_url` carries the origin, which is `senzing.com` for the CORD collections and `raw.githubusercontent.com` for the Truth Set. ⚠️ The field name `download_url` denotes **different hosts** in the `source='list'` and per-source responses (verified server 1.32.9, 2026-08-14), so the URL in hand governs, not the key it arrived under. Enforced by `tests/test_truthset_download_is_the_dataset.py`. (Source: `truthset-step-saves-a-five-record-preview-not-the-truth-set`, 2026-08-14.)

- **INV-227** — Whether a bootcamp is in progress MUST be decided by whether the progress file **records a module**, never by whether the progress file exists; a file that is absent, empty, `{}`, malformed, not a JSON object, or carrying a null/blank `current_module` MUST be treated as a fresh start, silently (INV-012), and the predicate MUST NOT raise on any of those (INV-048). ⛔ **An empty progress file is the NORMAL state for a large part of every run**, not a corruption to report: the onboarding preface creates it during its silent project setup and nothing records a module until Bootcamp preparation's final consolidated write, so the window spans the whole preface plus every preparation gate. Testing existence made the `SessionStart` hook announce "offer to resume from the last recorded module" on a project with no recorded module — an instruction the guide cannot execute, where the correct behaviour was onboarding from the top. The distinction was already computed and simply unused, so the fix belongs in the shared predicate rather than at each call site. Enforced by `tests/test_resume_requires_a_recorded_module.py`. (Source: `empty-progress-file-makes-resume-unsatisfiable`, 2026-08-14.)

- **INV-226** — A step that **updates** a shared artifact MUST be reachable only through a citation that also covers the substep which **creates** it, or MUST name the step that owns creation; a citation naming specific substeps MUST NOT omit the creating one. ⛔ **The hazard is the narrowed citation, not the missing file.** Module 0 cited module-completion "Step 2 (2b/2c)" — omitting 2a, the substep that creates the recap header — and on a Core run Module 0 is the *first* module to append, so it was precisely the module that hit the does-not-exist case; graduation meanwhile attributed the `**Bootcamper:**` line to Bootcamp preparation, which is recap-exempt and writes no recap. Two files each assumed the other had done it, and the cost landed on the keepsake: a certificate printing a placeholder name, and graduation's completion-date insertion anchored to a header nobody wrote. A step that updates what nothing creates MUST also create it rather than assume an edit target. Enforced by `tests/test_recap_header_is_owned.py`. (Source: `nothing-owns-creating-the-recap-header`, 2026-08-14, with `project-readme-is-updated-but-never-created` as the second instance of the same shape.)

- **INV-225** — A step with **no 👉 question is non-yielding**: it MUST be presented in the same turn as the next step that does ask, MUST NOT end a turn, and MUST NOT be given a turn of its own. Where several non-yielding steps share one turn their progress checkpoints MUST collapse to **one write at the end of that turn**, carrying the last completed step and without dropping the intervening steps from `step_history`; if the turn cannot complete, whatever did complete MUST be written before stopping. ⛔ **Without this the rules had no legal move**, which is worse than a wrong one: INV-005 forbids a turn ending with zero 👉, "advance exactly one step at a time" reads as forbidding a shared turn, and a guide made to break one of them learns to read the surrounding ⛔ rules as advisory. A run of non-yielding steps is the same case, not a worse one — **System verification contains exactly one 👉 in the whole module**, so a faithful turn there generates code, runs it and loads data before it may legally end, and that is correct: the turn ends where the Bootcamper is actually asked something. A step boundary is therefore not a turn boundary. Enforced by `tests/test_non_yielding_steps.py`. (Source: `statement-only-step-cannot-satisfy-one-question-per-turn`, 2026-08-14.)

- **INV-224** — A 👉 question's answer options MUST render **directly beneath the question**, whether they are pinned or generated at runtime; only informational prose (a detected-platform line, a caveat, a statement true of every option) may precede the 👉, and a **per-option annotation is not informational** — it belongs on its option, inside the list. ⛔ Being unpinnable changes only whether the text is fixed, never where it sits: the programming-language gate builds its options from `get_capabilities` and was the one gate whose placement no rule decided, so the same gate could render two ways between runs — and in the list-first form the question said "reply with a number" while the numbers sat above it, separated on macOS and Windows by several lines of per-language Docker routing. The pre-existing rule *permitted* options to follow the 👉 (INV-211's paragraph) without requiring it, which is what left two defensible readings. Enforced by `tests/test_answer_options_render_below_the_question.py`. (Source: `language-gate-does-not-say-where-its-options-render`, 2026-08-14.)

- **INV-223** — A module that starts a background server MUST capture that server's **process id** at launch and record it in the same checkpoint object that records its port, MUST terminate it by that pid — falling back to a lookup by the recorded **port** when no pid is available — and MUST confirm the port is free before any subsequent step runs. ⛔ **Identifying the server by matching its command line is forbidden**, `pkill -f <script name>` above all: the pattern appears in the matching command's own command line, so the kill signals the invoking shell. Observed on a dry run, 2026-08-13: it terminated the shell with exit code 144 part-way through Truth Set visualization's teardown, leaving the records loaded and the irreversible purge unrun, with the failure presenting as the purge crashing. A name-based match is also wrong in principle, since the server is written in the Bootcamper's chosen language (INV-090) and in general has no script name to match. Waiting a fixed interval is not confirming: the port being free is the exit condition, because the step that follows teardown is a purge that cannot be undone. Enforced by `tests/test_viz_server_process_handle.py`. (Source: `visualization-server-teardown-does-not-record-a-pid`, 2026-08-14.)

- **INV-222** — The Senzing SDK's language packages MUST NOT be installed from a language package manager: they ship inside the Senzing SDK runtime and are made available **by path**. For Python specifically, `pip install senzing` / `senzing-core` in any form (bare `pip`, `python3 -m pip`, or inside a virtualenv) is forbidden, and the paths that replace it — `PYTHONPATH`, and `LD_LIBRARY_PATH` where the native library is not found automatically — MUST be taken from `sdk_guide(topic='install', platform=…, language=…)` at run time rather than hardcoded (INV-080). ⛔ **INV-066's explicit-interpreter and PEP 668 rules govern the plugin's OWN tooling installs only** (`fpdf2`, Playwright) and never authorise pip for the Senzing SDK; the two invariants name each other for that reason. The failure this prevents is silent and deferred: the command **succeeds**, so the module reports a clean install while the PyPI packages shadow the SDK-shipped ones on `sys.path`, and the damage surfaces a module later as `libSz.so: cannot open shared object file` — read as a broken environment rather than as the instruction that caused it. The live server states the rule at `severity: error` for every scaffold workflow and repeats it verbatim in its install gotchas (`generate_scaffold`, `sdk_guide(topic='install')`; server 1.32.9, re-verified 2026-08-14), and it is why a wrong install can pass a version check: that check reads the *engine's* version through the native library, not the bindings that will be imported. Detection is `python3 -c "import senzing; print(senzing.__file__)"` resolving outside the SDK's Python directory. Enforced by `tests/test_no_pip_install_senzing.py`. (Source: `senzing-python-sdk-must-not-be-pip-installed`, 2026-08-14.)

- **INV-221** — Where a bootcamper-facing surface offers an action that opens a piece of content, that surface MUST NOT also render the same content inline. (Source: `triage-the-twelve-uncited-hard-rules`, 2026-08-13.)
- **INV-220** — Where a step branches on material the bootcamp may have authored itself, the branch MUST be taken from the **provenance already recorded earlier in the run** — never from a second mechanism introduced at that step, and never from a question to the Bootcamper, since a question about a scenario the bootcamp invented has no true answer. (Source: `triage-the-twelve-uncited-hard-rules`, 2026-08-13.)
- **INV-219** — A test MUST NOT pin the **verbatim wording** of a claim about an MCP tool's content or about an upstream submission. It asserts the **structural property** the text must carry instead — that a citation names the governing invariant, that a fence carries its ownership label, that a path resolves through a prefix variable — so that correcting the claim when the server moves does not fail the guard. Where such a claim is pinned and must change, the guard is **rescoped, never deleted**, and the reason recorded in its docstring. ⛔ **The failure is asymmetric and lands on the wrong person:** the guard passes on the day it is written and fails only whoever later corrects the claim, with a message asserting the opposite of what the server now says. (Four instances, 2026-08-13: `:195` pinned "documents brew tap / trust / install --cask only" and failed the correction of "only"; `:349` pinned INV-129 where INV-218 governs, which is what kept that wrong citation alive; a new guard banned the string `/opt/homebrew` on a line naming it in order to forbid it; and `:239` pinned "same coverage gap reported upstream" — the phrase but not the date — surviving a false-provenance correction only by how narrowly it was written.) (Source: `a-guard-must-not-pin-the-wording-of-a-claim-about-an-mcp-tool`, 2026-08-13.)

- **INV-218** — A step that **installs or updates software** MUST verify the installed **artifact on the filesystem** rather than the installer's exit status: a zero exit, printed install paths, or a "success" line are necessary and insufficient, because a package manager can purge a download and still print its caveats block listing paths it never wrote. The artifact probed MUST be named for the Bootcamper's platform, and where it cannot be probed the outcome is reported as **undetermined** (INV-163), never as installed. ⛔ INV-129 governs the parallel rule for rendered **deliverables** (PDF, PNG, HTML) and does not reach an install; the two are siblings, not one rule. (Source: `install-verification-has-no-invariant-so-inv129-is-borrowed`, 2026-08-13.)

- **INV-217** — An absence claim recorded in `specs/DECLINED.md` — in a `Revisit if:` clause or a dated revisit note — MUST carry a parseable `MCP-NEGATIVE` marker in INV-209's form, `owner:` clause included, and `specs/DECLINED.md` MUST stay inside `coverage_reports.py`'s negatives scan surface while the rest of `specs/` stays out. ⛔ **This file is the one Senzing claim in the repo with no re-verification path:** a declined spec is never implemented, so `implement-spec` Step 3.3 never re-asks its facts, while INV-216 directs the next reader to trust this file OVER the spec's own citations — so a stale negative here sends the revisit check to the wrong answer while looking evidenced. Prose that QUOTES a retracted claim is exempt and MUST declare it with `MCP-NEGATIVE-SCAN: quoted-history`, so a correction can restate what it corrects. (Source: `declined-ledger-negatives-are-invisible-to-the-scanner`, 2026-08-13.)

- **INV-216** — The unimplemented-spec candidate set MUST be **computed**, not hand-counted, and it MUST subtract **both** terminal states: `IMPLEMENTED.md` *and* `DECLINED.md`. `python3 .claude/skills/implement-spec/list_specs.py` is that computation; `implement-spec` MUST name it, and the prose steps stay as the explanation of what it computes and why. A spec appearing in **both** ledgers MUST be reported rather than silently subtracted twice — it means one of the two records is wrong. ⛔ **A declined spec is not a candidate**, and re-offering one is the specific hazard: the spec *file* argues for the change while only the ledger records the argument against it, so a listing that omits the declined set reads as a live recommendation. Before reopening one, its `Revisit if:` clause MUST be re-verified rather than the spec's original citations trusted. (`implement-spec` Step 3 had stated all of this correctly, including the reason — *"Omitting the declined set re-offers a spec the maintainer has already ruled out, every run"* — and it was still done by hand. On 2026-08-13 a run subtracted only `IMPLEMENTED.md`, reported the **declined** `no-route-for-bootcampers-who-cannot-add-an-mcp-server` as open, recommended it to the maintainer, and implemented it, reverted in `f12de7d`. the declined-ledger guard caught the contradiction *after* the fact; nothing caught the listing that caused it, and the instruction was correct at the time — which is the argument for mechanising rather than restating it more firmly, exactly as INV-207 concluded about care. ⚠️ The script's first draft also flagged ledger headings with no spec file as probable renames and produced **23 false alarms** on a clean repo — they are audits and dry runs recorded straight into the ledger, which `coverage_reports.py` already skips. That channel was removed: a report that cries wolf on correct data trains its reader past the one line that matters.) Enforced by `tests/test_list_specs.py`, which tests the arithmetic against synthetic fixtures because the live repo currently has **zero** open specs — so "it agrees with the repo" would pass on a script that returned nothing at all. (Source: `compute-the-spec-candidate-set-instead-of-hand-counting-it`, 2026-08-13 — a process fix with no spec file, arising from the erroneous implementation of the declined `no-route-for-bootcampers-who-cannot-add-an-mcp-server`.)
- **INV-214** — A verbosity preset governs **form as well as kind**. Where bootcamper-facing output has a prescribed shape and the active preset budgets fewer lines than the shape has, the required elements MUST **merge onto the permitted lines**, joined with `; `, and MUST NOT be dropped to fit; any **per-line** annotation attaches **inline to the value it qualifies**, so collapsing lines never costs a marker. A list of names MAY compress to a count when those names were already given in the same session — a count is not a module number, so INV-079 is unaffected. ⛔ **No bootcamper-facing element may be left with no treatment under a preset.** Where a step emits a group of items and only some carry per-preset rules, the remainder MUST be governed as a group rather than left to the reader, since "reduces only explanatory output" decides *what* survives and says nothing about *how much* of a ten-item overview is explanatory. (The preset system is specified as five content categories at levels 0–3 — a filter over *kinds*. Two sites had a prescribed *shape* and no rule. The onboarding overview is ten bullets of which exactly **two** carried a treatment, and the eight remaining are not uniform: the module list is orientation, the guided-discovery framing is encouragement, and under `minimal` one guide would print all eight and another cut to a sentence. Bootcamp preparation's Step 7 is a six-line recap with a **one-line** budget under `minimal` *and* a ⛔ requiring every honored value stated with a marker defined per line — a collision guaranteed in the one case the marker exists for, a returning bootcamper who pre-seeded `path`, `verbosity` and `programming_language`, needing three markers with one line to hold them. Found in the dry-run **seeded** walk, which is the only configuration that reaches both: `verbosity: minimal` must already be present for the preface's suppression path to run at all. Neither broke a path, and the cost is divergence landing on the recap whose stated purpose (INV-099/INV-133) is to let a returning bootcamper *see what is in force and correct it*.) Extends the per-preset convention INV-096 established for the time estimate from one line to structured output; complements INV-011/INV-012. Enforced by `tests/test_minimal_verbosity_scope.py`. (Source: `minimal-verbosity-scope-against-structured-output`, 2026-08-13.)
- **INV-213** — A **spec** whose diagnosis rests on the MCP server *lacking* something MUST record, on its `MCP re-check` line, `owner-checked: <the route that would CARRY the fact> — <what it returned>`; the outcome vocabulary carries `server does not cover it` for this case. A line declaring `n/a (no Senzing fact)` is exempt, since with no Senzing fact there is no absence claim about the server to substantiate. ⛔ **The tools asked and found empty are evidence about those tools, not about the negative** — "`sdk_guide(topic='configure')` returns no license variable" is true and worthless as support for "no license variable exists". `implement-spec` MUST treat a missing clause on an absence claim as a **blocker** and re-diagnose rather than implement. (This is **INV-209 one level up**, and the level that matters more: a marker is shipped prose, a spec is the *input to implementation*. Three wrong-route absence conclusions occurred in one session, 2026-08-13, each with a real tool, real parameters, a real empty result and an honest date. One **shipped** — it became INV-208 plus a guard banning the correct variable name, and the offline suite certified both because guard and claim shared a premise. The second inverted an entire spec's diagnosis when caught at implement-spec's re-verify step, and was caught only because the author had made the first error hours earlier and went looking: **care is not the remedy**, which is the argument INV-207 already made about this repo. Grandfathered before 2026-08-13, matching `test_spec_ledger_invariants.py`'s cutoff pattern; measured at design time as 1 pre-existing match across 274 specs, and that one an exempt `n/a` line.) Enforced by `tests/test_spec_absence_claims_name_their_owner.py`, which validates against **synthetic fixtures** because both offending specs have since been corrected — a guard asserting only that `specs/` is clean would pass on an already-empty answer. (Source: `a-spec-asserting-server-absence-must-name-the-owning-route`, 2026-08-13.)
- **INV-212** — A step that must **retrieve** bootcamper-facing Senzing content from `search_docs` MUST carry a retrieval strategy, not merely the tool's name: it MUST say what vocabulary to query with, name the documents that hold the material, and name any query whose obvious phrasing returns confidently wrong content. Where the searches do not reach part of what the step's output shape asks for, the step MUST require the gap to be **named as unreached** — with an offer to look it up — and MUST forbid filling it from training data (INV-080). A **bare link stub** in a result (`[Read More](/…)`) is NOT substantive content and MUST NOT be presented as coverage. ⛔ **A step whose output shape exceeds what one obvious query returns, with no strategy for closing the difference, forces a choice between fabricating and under-delivering** — and the fabrication is laundered by the very attribution line the step requires ("Sourced from Senzing docs via the MCP server"). (Module 1 Step 3 asks four attributes × ten use-case categories and named only the tool. A generic query reaches about four categories, which is what made the corpus look thin: queried by **sector** vocabulary, `economic-cost-mismatched-identity-data.md` quantifies ten sectors and supplies the business-value attribute for nearly every category. ⚠️ **The spec that produced this invariant first diagnosed the problem as missing server coverage — concluded from two broad queries, the same wrong-route error as INV-208 the same day.** Two category labels are homonym traps returning *plausible* wrong content rather than nothing, which is the more dangerous shape: "Supply Chain" reaches libpostal's store-chains geodata and a `sz_spark` "CI / supply chain" heading, and "Data Migration" reaches the V3→V4 SDK migration — the one recognized category with no business-use-case material.) Complements **INV-194** (one tool's silence is not absence) by binding the *step* rather than the reader, and **INV-080** by naming the fabrication route a retrieval gap opens. Enforced by `tests/test_pattern_gallery_shortfall.py`. (Source: `pattern-gallery-asks-for-more-than-mcp-can-supply`, 2026-08-13.)
- **INV-211** — Anything presented to the Bootcamper in order to **inform an answer** — a reassurance, caveat, recommendation, framing statement, or consent disclosure — MUST be placed **before** its 👉 question, and this binds the **skill files** as well as the output. Two reasons, the first mechanical: nothing may follow the 👉, because it ends the turn (INV-005), so text written after it is either never delivered or delivered a turn late; the second is that a caveat arriving after the answer cannot inform the choice it exists to inform. The numbered choices belonging to the question, and internal directives such as `*(Internal: end the turn and wait.)*`, are part of the question rather than "after" it, and answer-**handling** instructions ("on yes, …") correctly follow. ⛔ **An instruction that says to do something "before they answer" while sitting below the question is unfollowable as written** and MUST be moved, not reinterpreted — an instruction a reader has to silently repair teaches that the neighbouring ⛔ rules are approximate. (Bootcamp preparation Step 3 always stated this correctly, with the reason; it was stated only there, so it propagated nowhere. Three sites had it backwards, each resolved only by a guide importing the reasoning from another file: Module 1 Step 10a printed its deployment-target reassurance after the question *and* after the end-the-turn directive, when that reassurance is what makes "Not sure yet" a comfortable answer rather than a guess; and **two** teardown-consent sites — `visualization-api-reference.md` and `phase2-close.md` — placed "state what they are consenting to before they answer" below the gate, in front of an **irreversible** teardown whose own next sentence reads "A yes given without that is not an informed yes". The third was found by this invariant's own guard, not by the walk or by the hand survey that preceded it, which had missed it because the sentence begins "State" rather than a cue word the survey looked for.) Enforced by `tests/test_reassurance_precedes_question.py`, which guards the decidable shape — a "before they answer" cue following a 👉 within one section — rather than a blanket "no prose after a 👉", since answer handling legitimately follows a question in these files. (Source: `reassurance-must-precede-its-pinned-question`, 2026-08-13.)
- **INV-210** — Where a bundled script accepts the same configuration from **more than one source**, the precedence MUST be **content-aware**, not existence-based, and the script MUST validate the set it chose before using it. Concretely: a source is preferred only when it is *complete*; an incomplete or unreadable higher-precedence source falls through to a complete lower-precedence one; when two sources are present the one **in force** is named on stderr so the loser is never silently discarded; and when none is complete the script fails naming **the source it used and exactly what that source lacks**, rather than proceeding and letting a downstream component report a different cause. ⛔ **Validity of the container is not validity of the contents.** `{"PIPELINE": {}}` is valid JSON and truthy, so an `if not settings` check passes it — which is how `senzing_viz_server.py` let a stub `config/engine_config.json` silently defeat a fully populated `SENZING_ENGINE_CONFIGURATION_JSON` (its precedence was `if os.path.exists(path)`), then failed inside the engine with `SENZ7426`. That error is *correctly* documented as meaning `SUPPORTPATH` is wrong — `explain_error_code('7426')` leads with "Check SUPPORTPATH FIRST … This is a configuration error, NOT a broken install" (server 1.32.9, 2026-08-13) — so the plugin's own guidance then sent the reader to inspect a `SUPPORTPATH` that was correct where they were looking. **Correct guidance applied to a wrongly-reported cause is worse than no guidance**, because it is followed confidently. The bootcamp reaches this because Module 2 writes the settings file *and* exports the env var, and Module 3b runs the reference server on the Python path with `--settings` defaulting to that file — so any divergence between the two lands here. (Extends **INV-111**/**INV-129** from what a generator must *report* to what a script must *check before acting*: the fallback was honest and the reported reason was still the wrong one — the same shape as the `_clip` defect, found the same way.) Enforced by `tests/test_viz_settings_resolution.py`, which is SDK-free (INV-108) because the resolver is pure. (Source: `viz-server-settings-precedence-and-validation`, 2026-08-13.)
- **INV-209** — An `MCP-NEGATIVE` marker MUST carry an `owner:` clause naming the route that would **carry** the fact, and what that route returned. The format is `MCP-NEGATIVE: <tool(params) asked> — <what is absent> — owner: <route that owns the fact + outcome> — server <version>, <YYYY-MM-DD>`, and a marker without the clause MUST NOT parse — it is reported as *missing* rather than as well-formed, because an optional provenance field is supplied exactly where the author already knows the answer and omitted exactly where this rule is needed. The owner MUST be concrete: an MCP tool call, or an equivalently specific source such as a validator's rejection. Vague back-references ("the same call") are insufficient — the reader must not have to resolve them. Two shapes, both requiring the clause: a **routing** negative (the fact exists elsewhere; the owner is where the reader must go instead of concluding absence) and an **absence** negative (the fact is served nowhere; the owner is the route asked and found empty, and naming it is the only thing separating this from a wrong-route error). ⛔ **The empty call in the claim slot is evidence about that call, not about the negative.** "`sdk_guide(topic='configure')` returns no license variable" is true and worthless as support for "no license variable exists" — the variable lives in `sdk_guide(topic='load', record_count=<above the limit>)`. (This is INV-194 made mechanical. The rule existed, was indexed, and was violated anyway: a wrong-route negative reached a registered invariant (INV-208) and a guard that enforced it, with a real tool, real parameters, a real empty result and an honest date at every step — so it read as reviewed the whole way, and the offline suite certified it because the guard and the claim shared a premise. Nothing in the old one-slot format prompted "which tool would carry this if it existed, and did you ask it?" The clause proved itself during implementation by rejecting the first draft of one of the three live markers, whose owner read "the same call's compatibility_notes" — a back-reference, not a route.) Enforced by `tests/test_dated_negatives_are_marked.py`; the parse is in `.claude/skills/dry-run/coverage_reports.py`, whose `negatives` report prints the clause and tells the reader to re-ask the owner. (Source: `mcp-negative-markers-must-name-the-owning-route`, 2026-08-13.)
- **INV-208** — The Senzing license environment variable is **`SENZING_LICENSE_FILE`**, and the plugin MUST NOT use the spelling **`SENZING_LICENSE_PATH`**, which no MCP tool returns. Exactly **one** tool route carries the correct name: the `compatibility_notes` of `sdk_guide(topic='load', language=…, record_count=<above the default limit>)`, which direct a licensed user to "place the license file at the path specified by `SENZING_LICENSE_FILE` or in the `etc/` directory". Verified on server **1.32.9, 2026-08-13** at `language='python', record_count=1000` and `language='java', record_count=600` — language-independent, and present only when the count exceeds the limit. A `PIPELINE` key (`LICENSEFILE`, `LICENSESTRINGBASE64`) is the other supported route and is what Module 4 Step 8a wires. ⛔ **The plugin MUST NOT assert that no license environment variable exists**, and MUST NOT infer its absence from the topics that omit it: `sdk_guide(topic='configure')` returns only `LD_LIBRARY_PATH`/`PYTHONPATH`, `sdk_guide(topic='install')` shows only the `PIPELINE` keys, and `search_docs` returns no variable name — all three re-checked on 1.32.9, 2026-08-13. (⚠️ **Corrected in place 2026-08-13, same day it was written, by the phase-3 walk that tripped over it.** As first recorded this invariant banned the whole `SENZING_LICENSE_` prefix and asserted "Senzing reads no such variable" — false, and its guard therefore banned the *correct* name while the module-02 note it "fixed" had been right all along. The original error was textbook **INV-194**: a negative concluded from the tools that happen not to carry the fact, without asking the tool that owns it. The correct-a-false-premise clause of this file's own maintenance rules is what this note discharges; an invariant encoding a false premise is worse than a missing one. The wrong spelling reaching graduation's `.env.example` — a deliverable the bootcamper carries into production, where an unread variable licenses nothing and surfaces much later as `SENZ9000|LIMIT` — was and remains a real defect; only the diagnosis of *why* was wrong.) Complements **INV-080** (facts route through the server) and **INV-194** (one tool's silence is not absence), and is the worked example of the latter costing a shipped invariant. Enforced by `tests/test_license_env_var_absent.py`. (Source: `no-license-path-environment-variable`, 2026-08-13.)
- **INV-207** — A claim about **this repo's own reference graph** — which invariant IDs are defined, which are cited, what the ledger records — MUST be verified **after** it is recorded, never before; and evidence for "identifier X is unused" MUST NOT quote X. The recording is itself part of the corpus every such check reads, so a clean result obtained before the claim is written measured a different repo than the one that ships. Where quoting the identifier is unavoidable (a spec whose subject *is* the stray citation), the file carries the scanner's documented `ignore-file` marker and says why. (Three instances, none caught by review: the `harden-write-gate` report test named INV-060/INV-097 and thereby cited them, caught in flight; an `inv205-covers-whether-to-ask-but-not-how` criterion evidenced "no new ID was minted" by **writing the next ID out**, which shipped, put two citations of an undefined invariant into `specs/`, and turned the whole suite red one commit later — after that same run had recorded `citations.py verify` as clean, because the scan ran during the criterion walk and the entry was written afterwards; and the spec filed to report that defect reproduced it in its own illustrative sentence, one paragraph after describing it, caught by the verifier seconds later. The third instance is the argument for a mechanical rule rather than care: care failed under maximum attention. ⚠️ **A count is not a result** belongs to the same failure — the shipped instance recorded "1792 passed, 3 skipped" from a `Ran 1792 tests` line while `FAILED (failures=1, skipped=3)` sat directly beneath it, so a red suite was ledgered as green and the figure became a baseline later specs were checked against. Enforced by `tests/test_spec_ledger_invariants.py`, which pins the ordering instruction, its reason, and the count-is-not-a-result warning in `implement-spec`'s Step 4; `citations.py verify` is what actually detects the condition, and it caught all three.) (Source: `proving-an-id-is-unused-by-writing-it-cites-it`, 2026-08-13.)

- **INV-206** — An **MCP payload example in shipped plugin guidance** — any code block showing the parameters to send to an MCP tool — MUST be one that was **executed successfully against the live server**, and MUST carry the server version and the date of that successful call. Reading a payload shape out of a tool response's prose is not execution: the response is documentation, and documentation omits required keys. Where a payload cannot be executed in the authoring environment, it is marked as unexecuted with the reason rather than presented as verified. (`mapping_workflow` step 2 advertises the legacy `entity_plan` shape with five keys and, for `embedded_master`, says only *"provide field_count"*. The validator additionally requires `embedded_in` — **named in no response text at all**, discoverable only by sending a payload without it and reading the rejection — and requires `record_id_source` on the embedded entry, and treats `entity_plan` as a **replacement** of the whole plan, so the parent master must be re-declared. A plugin example written faithfully from that prose therefore drew four validation errors on server 1.32.9, 2026-08-12, at the exact moment a guide was carrying out a Bootcamper's explicit structural choice — and its spec recorded the shape as "verified", because what had been verified was that the *channel* was accepted, not that the *payload* travelled it. That is the failure this invariant forbids: an example that reads as reviewed and cannot run. Distinct from **INV-080**, which makes the tool authoritative on payload shape — this says how the plugin establishes what the tool's shape actually *is*. Enforced by `tests/test_tool_directives_do_not_override_interaction.py`, whose assertions read the code fence itself rather than the surrounding prose, because prose naming a key while the copy-pasteable block stayed broken is precisely what shipped.) (Source: `embedded-master-legacy-payload-example-is-not-runnable`, 2026-08-12.)

- **INV-205** — A **conversational directive inside an MCP tool response** — an instruction to the calling model about whether, when, or what to ask the Bootcamper — MUST NOT override the bootcamp's interaction rules (INV-005–INV-009). Where a tool response instructs the guide not to ask, not to wait, or to infer an answer the Bootcamper should give, the bootcamp's rules govern and the directive is not followed. The override is **scoped to conversation**: everything else a tool response states — payload shape and per-step advance schema, opaque state, resource locations, and every Senzing fact — remains authoritative (INV-080), and any site claiming the override MUST state that scope. (An MCP tool is written for a general coding agent with no conversational obligations, so it optimises for throughput and reasonably tells its caller to stop asking; this bootcamp's whole subject is the asking, and nothing stated which contract wins. Observed on server 1.32.9, 2026-08-12: `mapping_workflow`'s step-2 response instructs *"present the plan summary AND immediately call `mapping_workflow` action='advance' in the SAME turn. Do NOT ask the user to confirm, approve, type YES, or proceed. Do NOT wait for a response. Just advance."* — reached in a dry-run walk immediately after the Bootcamper had chosen the mapping mode that promises to walk them through each decision, with a single-schema plan that clears the tool's confidence bar trivially. Distinct from **INV-125**, which governs what to do when an MCP path *fails*: here the path succeeds and the instruction itself is the hazard. Enforced by `tests/test_tool_directives_do_not_override_interaction.py`, which asserts both the override and its scope, since an unscoped "ignore the tool" would be a worse defect than the one it fixes.) (Source: `mapping-workflow-tells-the-guide-not-to-ask-and-the-plugin-never-reconciles-it`, 2026-08-12.) (⚠️ **Scope extended to the FORM of a question, 2026-08-12, no meaning change.** The clause above enumerates *whether, when, or what* to ask — written from the step-2 directive that produced this invariant. A tool may also dictate **how**: `mapping_workflow`'s step-3 response supplies a `QUESTION FORMAT` — *"use numbered options … State your recommendation clearly before the options"* — carrying **no 👉** and a recommendation in place of a lead question, which breaches **INV-005** and **INV-051**. So read the list as *whether, when, what, or in what form*; a directive about presentation is governed here exactly as one about whether to ask. ⚠️ **A recommendation is not the defect** — the plugin recommends inside pinned questions routinely, and INV-051 constrains the *lead question* being neutral, not the presence of advice. Keep the tool's substance (the field, its populations, its samples, the numbered options **and** its recommendation) and present it in the bootcamp's shape. Recorded the same day the invariant was: it was generalised from one instance and the enumeration broke on the next, one workflow step later — the hazard this file's own maintenance notes describe about a rule that lists members. Source: `inv205-covers-whether-to-ask-but-not-how`, 2026-08-12.)

- **INV-204** — A reachability or liveness probe MUST use `get_capabilities`, never a content-returning tool (`search_docs`, `sdk_guide`, `reporting_guide`, `find_examples`, `get_sdk_reference`). The probe's only output is whether the server answered, so a document search pays for a retrieval that is then discarded; `get_capabilities` is already required once at session start (`ground-rules.md` → "Session start"), so the probe is a call the guide has to make anyway. (Naming the anti-pattern in order to forbid it is permitted — `onboarding-flow.md` does exactly that — and this does not restrict `search_docs` where content is actually wanted. The rule was reasoned out and written down in `onboarding-flow.md` in the first person of that file, *"It was specified **here** as…"*, so it read as a local fix; Module 3's Step 1 kept the `search_docs` probe for weeks after, and no test knew the rule existed. Enforced by `tests/test_liveness_probe_is_not_a_document_search.py`.) (Source: `module3-connectivity-probe-still-uses-the-deprecated-search-docs-call`, 2026-08-12.)

- **INV-203** — A fetched data source MUST NOT be written to `data/raw/` under its final name, nor recorded as collected in `config/data_sources.yaml`, until **both** checks pass: the fetch returned a **2xx** HTTP status, and its **measured** record count equals the expected count — `record_count` from `get_sample_data(…, source='list')` for an uncapped fetch, or `min(record_count, download_url_max_records)` when fetched via the capped `download_url`. Both counts MUST be recorded in the registry entry. A mismatch is a **failed collection, never a warning**, and a throttled response (HTTP 429, body English prose) MUST be retried with a short backoff rather than treated as data. (The cap clause is load-bearing, not caution: `download_url` serves at most `download_url_max_records` records — 10,000 — so a bare equality test against `record_count` fails 6 of the 11 `las-vegas` sources on correct data. Both figures verified live against MCP server 1.32.9, 2026-08-12: `NOMINO-RISK`, `record_count` 14,119, returns exactly 10,000. Staging belongs in `data/temp/` — INV-050's existing scratch directory — and stays project-relative under INV-200; nothing may be staged in system temp. Enforced by `tests/test_cord_fetch_integrity.py`, whose four negative controls were verified to land.) (Source: `cord-download-rate-limit-is-saved-as-data`, 2026-08-12.)

- **INV-202** — Every leaf entry in INV-050's project-layout tree MUST be either referenced somewhere under `plugins/` or annotated in its own comment as reserved/superseded/legacy/future. An entry that is not produced MUST gain the annotation rather than being deleted — the tree is cited widely and quoted in audits, so shortening it loses the record that the artifact was once intended. The comment column is **load-bearing data**: a scan that discards it (`line.split("#")[0]`) reports correctly-annotated entries as defects, which is exactly how `inv050-layout-tree-names-three-artifacts-nothing-produces` came to claim three `(reserved)` entries were a defect. (Enforced by `tests/test_invariant_layout_tree.py`; the convention itself dates from `layout-tree-reconciliation`, 2026-07-17, commit `cc46a55`, and bound every tree edit since while being registered nowhere.) (Source: `inv050-tree-has-no-reachability-guard`, 2026-08-11.)
- **INV-201** — Every MCP tool the plugin calls MUST be classified in `tests/test_mcp_call_contracts.py`: in `REQUIRED_PARAMS` with the parameters its calling files must name, in `CONDITIONALLY_REQUIRED` with the reason a flat check cannot express its requirement, or in `NO_REQUIRED_PARAMS` with why it needs none — and the three sets MUST partition the server's tool list exactly, so a tool can never sit in none of them. A required parameter whose name is a substring of its own tool (`error_code` inside `explain_error_code`) MUST be matched on a token boundary, never by substring, or the entry cannot fail. This generalises the defect class the source spec names — a guard that states its subject in the general and covers a subset: `REQUIRED_PARAMS` held 2 of the 9 tools with required parameters under a comment reading as though it enumerated them all, and `generate_scaffold` (16 call sites) appeared nowhere in the file. Nothing failed, because an absent dict key is not an assertion. (Source: `required-params-guard-covers-two-of-nine-tools`, 2026-08-11.)
- **INV-200** — Every file the bootcamp writes MUST live inside the Bootcamper's project directory. The `PreToolUse` write-gate MUST block a write whose **resolved** target lies outside it — system temp (`/tmp`, `$TMPDIR`, `%TEMP%`, `%TMP%`), `~/Downloads`, or any path escaping via `..` — while allowing project-relative and in-project absolute paths, **including a project that legitimately lives beneath a temp directory** (resolve first, then compare; a prefix match on the raw string blocks a real project at `/tmp/my-bootcamp`). The rule also binds **MCP tool arguments**, not only file writes: where a tool requires a writable directory — `mapping_workflow` and `analyze_record` both require `workspace_dir`, and the server's own contract warns *"do NOT assume /tmp exists"* (`get_capabilities`, MCP server 1.32.8, 2026-08-11) — the guide MUST pass a project-relative path, and MUST override any tool-suggested path outside the project (`/tmp/…`, `ExampleEnvironment`) rather than following it. (Complements INV-109, which governs the same hook's independent secret check and explicitly scopes itself away from this location logic; and INV-199, which governs the narrower case of files written to configure the Bootcamper's environment.) (Source: `write-gate-location-logic-is-unregistered`, 2026-08-11.)
- **INV-199** — The bootcamp MUST NOT create or modify any file outside the generated project directory in order to configure the Bootcamper's environment — never a global shell profile (`~/.zshrc`, `~/.bashrc`, `~/.profile`, PowerShell `$PROFILE`, or equivalent) — and MUST instead write a project-local environment script that the Bootcamper sources per session (INV-166/INV-167/INV-175 govern that script's portability). Where the bootcamp **relays** MCP install guidance that recommends persisting variables to a shell profile, it MUST state that the bootcamp does not do so on the Bootcamper's behalf. This is the only rule constraining what the bootcamp may write outside the project: every other file-placement rule (INV-050, INV-108, the `docs/` rules) governs where things go *inside* it, so nothing else would notice the breach. The relay clause is not hypothetical — `sdk_guide(topic='install', platform='macos_arm', language='java')` returns "DYLD_LIBRARY_PATH must be set at the shell level before any JVM or Python launch. Add to ~/.zshrc to persist" (re-verified on MCP server 1.32.8, 2026-08-11), which is correct advice for a human operator and forbidden for the bootcamp to act on. (Source: `never-modify-global-shell-config-is-unregistered`, 2026-08-11.)
- **INV-198** — Module 5's CORD fast-path offer MUST be gated on a source being both **structurally loadable** (the engine will accept the record) and **fully mapped** (every root key resolves to an Entity Specification attribute or a structural key), never on the structural test alone, and the two results MUST be recorded as separate fields rather than as one `senzing_ready` flag. A source with one or more unrecognised keys MUST be routed to mapping with **every** unrecognised column named to the Bootcamper, never merely counted. Payload-worthy columns MUST NOT be excluded from that count: a deliberately-kept payload column and an undispositioned one are indistinguishable from the record alone — even `getRecordPreview` reports only which features Senzing read, never what the publisher intended — so they are routed to `mapping_workflow` step 3, which is where `payload` is actually assigned. The threshold MUST remain a count rather than a proportion, since a percentage passes one undecided column in thirty. When every selected source is fully pre-mapped, the guide MUST say the mapping exercise is being skipped and offer an alternative rather than routing past the module's core skill in silence. (Partly supersedes INV-040's parenthetical "CORD data does not require mapping nor transformation", which is true of the `truthset` class it was written for and false of `PPP_LOANS`; INV-040's main clause, and INV-041's and INV-042's fast-pathed-source exemptions, are unchanged.) (Source: `readiness-gate-acts-on-structure-while-naming-semantics`, 2026-08-11.)
- **INV-197** — Where the bootcamp recommends a data source or resource it does not own, any known limitation of that resource MUST be stated at **every** site that recommends it, dated with when it was checked, and marked as an upstream condition to re-check rather than a permanent fact. The guidance MUST name a workable alternative where one exists rather than leaving a warning with no path, MUST NOT describe a partly-usable resource as broken, and MUST NOT be resolved by re-slicing, repairing or vendoring a copy of the data into this repo — a local copy creates a divergent second dataset and hides the upstream fix (the reasoning INV-173 applies to forking an MCP-delivered validator). (Source: `icij-free-data-samples-do-not-join`, 2026-08-11.)
- **INV-196** — The onboarding preface's overview MUST name the bootcamp-feedback trigger phrase in bootcamper-facing text before the first content module, as a statement and never a 👉 question, and MUST say that using it does not cost the bootcamper their place (INV-074's exit banner returns them to the pending question). It is explanatory output — suppressed under the `minimal` verbosity preset and one line under `concise` **where a preset is readable** (INV-011/INV-012, the treatment INV-096 gives the time estimate; on a fresh run none exists yet, INV-075/INV-133, so it is simply shown) — and MUST NOT be repeated at every module start. Graduation's closing invitation is retained in addition, never instead. INV-010 guarantees the capability at any time, but a control announced only in the guide's own rules, in one mid-bootcamp module and at graduation is reachable only by bootcampers who already knew the phrase, which silently skews every feedback entry the project receives toward people told out of band. (Source: `feedback-trigger-is-never-taught-to-the-bootcamper`, 2026-08-11.)
- **INV-195** — Every container the bootcamp starts MUST be recorded with the `runtime` that started it (`docker`, `podman`, or Apple's `container`), and every lifecycle action — the SessionEnd stop, the SessionStart state probe, and the resume message — MUST dispatch on that recorded runtime rather than assuming Docker. An entry with no `runtime` MUST be treated as `docker`, so progress files from earlier runs keep working. A runtime outside the recorded set MUST be reported but never executed, so a value in a progress file cannot become a binary a hook invokes. No session-boundary message may name a tool that did not start the container: naming the wrong CLI is what makes the message untrustworthy rather than merely unhelpful. (Generalises INV-101, whose Docker-only wording assumed the single runtime available when it was written; the warn-and-continue contract is unchanged.) (Source: `container-lifecycle-hooks-assume-docker`, 2026-08-11.)

- **INV-194** — An empty or absent field in one MCP tool's response is **NOT** evidence the server lacks the fact. Before recording that Senzing does not document X, the guide MUST also ask the tool that owns X in prose (`search_docs`), and MUST scope every negative finding to the tool and parameters actually asked — never to "the server". Observed: `get_sdk_reference(topic='flags', filter='SZ_WHY_ENTITIES_DEFAULT_FLAGS')` returns a one-line description with no `composite_members`, no `response_paths`, `applies_to` as the literal glob `["why_entities*"]`, and a `source_file` of the V3→V4 breaking-changes document — so the plugin's own "read the composite's `composite_members`" procedure appeared unrunnable, and the spec written from that observation proposed OR-ing sub-flags in blindly as the remedy. The membership was documented the whole time: `search_docs` returns, from the flags documentation, "The default recommended flags for `why_entities`. **Equivalent to: `SZ_INCLUDE_FEATURE_SCORES`**" — a single flag carrying no entity-name flag, which *explains* the `ENTITY_NAME: null` symptom instead of working around it, and is independently corroborated by the binding defaults in the same response's `method_signatures`. The correct order is: structured tool, then prose tool, then explicit sub-flags as a last resort with what could not be confirmed recorded (INV-080/INV-149). Distinct from INV-149, which is scoped to `response_schemas` and governs *retry* behaviour ("empty is coverage, not a failed call") and did not prevent this: the failure is not retrying, it is concluding **absence** from one tool's silence. Distinct from INV-169, which forbids over-generalizing a positive observation into an absolute about a method; this forbids the negative form. (Complements INV-115, INV-149 and INV-169.) (Source: `why-entities-default-flags-has-no-composite-members`, 2026-07-31.)

- **INV-193** — A count or percentage presented as evidence of **completeness** MUST derive its denominator from outside the artifact it measures. A self-derived figure MUST state what it cannot detect and MUST NOT be cited as proof of completeness. Observed: `generate_recap_pdf.py`'s `embedded N of M images` took `M` from the count of `![](…)` links in the very recap it was rendering, so a recap that embedded only four of six captured tabs reported `embedded 4 of 4 images` — a perfect score against an incomplete set, structurally incapable of detecting the incompleteness it appears to measure. In the reporting session the assistant cited `embedded 12 of 12` to a Bootcamper as proof the screenshots were complete *while being asked exactly that question*; it was right by luck of the input, not by measurement. That is worse than having no metric, because it is the number a reader naturally reaches for. The fix is an external denominator — here `capture_screenshots.py`'s `<name>-tabs.json` manifest, the only count in the chain not read from the recap — and where none is available the check MUST report itself **skipped** rather than passed (INV-163). Distinct from INV-110, which requires that loss be made visible but says nothing about where a denominator comes from: a self-referential metric satisfies INV-110 while hiding the loss. (Complements INV-110 and INV-163.) (Constrains **INV-162**, which requires an embedded-of-referenced count: that count is a loss signal and MUST NOT be cited as a completeness measure — cross-referenced 2026-07-31.) (Source: `embedded-image-count-needs-an-external-denominator`, 2026-07-31.)

- **INV-192** — A parameter an MCP tool's schema marks **optional** may still be mandatory to its *answer*, and a response that asks for one is a **gate, not a result**. Where a tool replies with a `needs_input` decision tree — or any response whose payload sections come back empty while it names a parameter it wants — the guide MUST re-call with that parameter satisfied, MUST NOT parse the gate as content, and MUST NOT conclude from it that a topic has no guidance. Every documented call in the plugin MUST pass the gating parameter, and where a tool gates only on *some* inputs the guidance MUST say which, since a caller who succeeds on the ungated ones has no reason to suspect the rest. Concretely on server 1.32.2 (2026-07-30): `reporting_guide` returns a `needs_input` gate on `language` for `topic='evaluation'` and `topic='graph'`, and on `language` then `scale` for `topic='data_mart'`, while `topic='quality'` returns its content without one — six shipped sites omitted `language`, including a step that cited the 4-Point ER Evaluation Framework as its reason for the call, which is exactly what the gated response withholds. This class is invisible to INV-136, which governs only what the *schema* declares required: a call can satisfy the schema completely, return HTTP 200, and carry a plausible partial payload while the substance is absent. That partial population is the hazard — an empty response reads as failure, whereas a gate carrying a few `anti_patterns` reads as an answer (the INV-115 half-populated-row trap, applied to a tool response rather than a record). (Extends INV-136 from declared requirements to runtime ones; complements INV-115 and INV-125.) (Source: `reporting-guide-topics-gate-on-language`, 2026-07-30.)
- **INV-191** — A dated provenance stamp MUST NOT be advanced for a claim that was not re-verified in that pass, and a refresh MUST target the specific provenance it re-checked rather than every date in the sentence. A line commonly carries two: an MCP stamp the sweep can re-ask, and a live-engine observation it structurally cannot (`visualization-api-reference.md` cites both — "re-verified on MCP server 1.32.2, 2026-07-30 — and corroborated by a dump on SDK 4.3.3, 2026-07-28"). Advancing the second asserts a check that never ran, and it is invisible afterwards because a fresh date is exactly what a verified claim looks like. Where a claim cannot be re-checked in the current environment its stamp MUST be left as written and the gap recorded as skipped (INV-163), never closed by restamping. (Observed 2026-07-30: a line-targeted stamp refresh bumped a live-dump date to a day no dump was run; caught by `tests/test_partial_row_and_schema_coverage.py`. Hardens INV-080.) (Source: `refresh-reverified-provenance-stamps`, 2026-07-30.)
- **INV-190** — Where a lookup tries a **list** of candidates in order — name attributes, encodings, endpoints, paths — an attempt that **fails** MUST be recorded and retried past, never treated as the end of the list. An error is reported only after every candidate has been attempted and none produced a result, and the report MUST name which candidates were tried and which failed; a result found by a later candidate stands, and a failure behind it is not an error. A guard phrased as "nothing has matched **yet**" is not a guard for "there is nothing **left to try**" — the two coincide only on the final candidate, so the defect hides on exactly the first one. Observed in the bundled visualization server, where `if not items: return {…}` meant any engine error searching `NAME_FULL` returned before `NAME_ORG` was ever called, silently reinstating the INV-164 defect on the error path with a message attached naming the attribute that could not have matched an organization anyway. This binds a query or visualization program in **any** language (INV-090/INV-124), not only the bundled Python reference. (Hardens INV-164 on the error path; complements INV-115.) (Source: `search-attribute-fallback-survives-a-failed-attempt`, 2026-07-30.)
- **INV-189** — Install and setup instructions MUST cover every platform INV-001 supports, or explicitly route an uncovered platform to the interface that does serve it. A step headed "on macOS or Linux" with no Windows line and no redirect reads as the complete set, so a Windows reader concludes the route does not exist rather than that it went unwritten — and the two facts are indistinguishable from the page. Where a command differs per platform it MUST be given per platform, verified against the vendor's own current documentation rather than recalled (the Claude Code CLI installs with `curl -fsSL https://claude.ai/install.sh | bash` on macOS/Linux/WSL and `irm https://claude.ai/install.ps1 | iex` in Windows PowerShell — verified 2026-07-30 against code.claude.com/docs/en/setup), and a PowerShell counterpart still obeys INV-167. (Source: `deep-dive-audit-2026-07-30b`, 2026-07-30.)

- **INV-188** — A user-visible string a shipped script **emits** — `--help` text, an error message, any list built at runtime from a data structure — is bound by the same content invariants as the plugin's prose, and conformance MUST be verified by **executing** the script, never by reading the source that produces the string. A guard that scans source cannot see an f-string's output, and the framing that makes a source construct correct does not travel with the value it interpolates: `capture_screenshots.py` kept `network` and `merges` in `TABS` under an explicit "⛔ RESERVED" comment — correct, and necessary so an eight-tab snapshot keeps its slugs — while `--help` and the unknown-id error interpolated the whole dict and presented eight capturable tabs for an app INV-155 fixes at six. A reserved value the script still accepts MUST be reported as retired when it is used, since accepting it silently is what keeps a stale list believed. (Extends INV-155's tab-set conformance from prose and docstrings to emitted strings; `tests/test_tab_set_is_singular.py` runs `--help` and the error path.) (Source: `deep-dive-audit-2026-07-30b`, 2026-07-30.)

- **INV-187** — An input the graduation handover deliberately does **not** copy MUST be named in the handover — in `production/GRADUATION_REPORT.md`'s files-excluded table and in `production/README.md`'s Configuration section — saying where the reader must supply it. `data/raw/` is excluded by design (raw source data is the Bootcamper's to place), but a CORD fast-pathed source loads straight from it with no mapping (INV-040/INV-041), so its loader arrives in `production/` with nothing to read. An excluded input the handover never mentions is indistinguishable from a broken project, and the reader has no way to tell which they are looking at. (Source: `deep-dive-audit-2026-07-30b`, 2026-07-30.)

- **INV-186** — Where a generated deliverable copies code **verbatim**, every data or asset tree that code reads MUST be copied to the same path **relative to the project root**, and the code MUST NOT be rewritten to match a flattened destination. Graduation copied `src/load/**` unchanged to `production/src/load/` while flattening `data/senzing-ready/**` to `production/data/`, so the handover project's loader pointed at a directory that did not exist — invisible to every check, because both halves of the copy succeeded. Rewriting the path instead is forbidden for the same reason it is tempting: the code is the Bootcamper's, and a path edited by graduation is a change they never saw made. (Upholds INV-084 across the `production/` boundary. Source: `deep-dive-audit-2026-07-30b`, 2026-07-30.)

- **INV-185** — Every command the SBCP tells the guide to run against a **bundled** script MUST resolve that script inside the plugin — `${CLAUDE_PLUGIN_ROOT}/scripts/<name>.py`, with the documented skill-relative fallback (`<this-skill-dir>/../../scripts/<name>.py`) — and MUST NOT name it by a bare project-relative path. The command runs in the Bootcamper's project while the script ships in the plugin, and the project layout (INV-050) has no top-level `scripts/`; its own utilities live under `src/scripts/`. `python3 scripts/generate_discoveries_pdf.py` therefore exits 2 with `can't open file` from any project root — and because it was the sole invocation of that generator, and its step is explicitly "report exactly what failed and continue", the realistic outcome was the always-produced data-discoveries deliverable being reported missing rather than produced. Prose-only review cannot catch this class: the line is valid shell and wrong only relative to a working directory no reader is holding. (`tests/test_bundled_script_and_production_paths.py` sweeps every invocation and MUST pass.) (Source: `deep-dive-audit-2026-07-30b`, 2026-07-30.)

- **INV-184** — Every shipped generator that inlines a fallback copy of the brand palette MUST keep that copy equal to the values `brand_tokens.py` derives, MUST hold it in a single named constant rather than repeating the literals per code path, and MUST have that equality asserted by `tests/test_brand_sync.py`. INV-107 named two generators; the property belongs to the *pattern*, and the third (`generate_discoveries_pdf.py`) drifted out of scope unnoticed while its own comment claimed a test asserted it. (Source: `deep-dive-audit-2026-07-30`, 2026-07-30.)

- **INV-183** — A step that instructs the guide to generate a bootcamper-facing artifact MUST, **at that step**, name every rule governing how the artifact is produced — or cite the file that states it — and MUST NOT rely on a rule stated only elsewhere. The step is the only text the guide is certainly reading when it authors the artifact, so a binding rule one file away is unreachable in practice. For a generated page or document that is currently: the shared brand tokens and offline rendering (INV-081/INV-091), escaping of every data-sourced string for the context it lands in (INV-106), verification of the rendered artifact rather than the exit status (INV-129), and its required location (INV-070). The rule MUST be **named and linked, never restated or forked** at the step (INV-080's no-fork discipline), so one statement of record stays authoritative. Observed: Module 5's two `docs/visualizations/` pages were instructed in a single sentence, with the brand MUST reachable only through a "should … where appropriate" restatement and the escaping rule only from another module's contract — so a CDN chart library, an unbranded page, and unescaped bootcamper data were all conforming readings. (Generalizes INV-164 from a reference implementation to any authoring instruction; `tests/test_generated_html_deliverables.py` enforces it for generated HTML, and the invariant is deliberately broader than that test since the next such artifact may not be HTML.) (Source: `module5-quality-pages-are-branded-visual-deliverables`, 2026-07-30.)

- **INV-182** — A spec MUST NOT be recorded as implemented in `specs/IMPLEMENTED.md` until **each** of its `## Acceptance criteria` has been checked individually with its evidence named (a `file:line`, a test, or a command run). A criterion that cannot be proven MUST be recorded as a deviation or as not-runtime-verified — never ticked — and a criterion naming a file, a module, or a **second consumer** MUST be verified by opening that file, not against the change the implementer remembers making. The ledger heading is the only completion signal the repo has and nothing downstream re-checks it: `relocate-integration-deployment-questions-to-module1` was recorded done with graduation never reading the answers its criterion named (INV-097, seven weeks), and `defer-commonmark-to-graduation` with the `production/*.md` half of its pass never built (INV-060, six weeks) — both invisible because neither invariant is cited by any test. (Source: `deep-dive-audit-2026-07-29-minor-fixes`, 2026-07-29.)

- **INV-181** — When the MCP server changes behaviour the plugin documents, every place the plugin states the superseded behaviour MUST be corrected to the current behaviour — including test assertions and docstrings that pin it — and MUST NOT be left version-conditioned to an older server. The plugin pins no MCP **server** version (every `version` argument is the Senzing SDK version), so a Bootcamper always reaches the deployed server and text describing an earlier one actively misleads: the retired numeric verbatim-check claim told them to record an exemption for a gate that had become green. A superseded claim MUST be recorded by appending to its originating spec, never by editing that spec's text, and a test that pinned it MUST be repointed to what survives — the rule or discipline — with a docstring stating what changed and when. (Source: `verbatim-check-numeric-source-values` superseded 2026-07-29.)

- **INV-180** — Code that walks a source's raw or mapped file rows to associate them with resolved entities MUST deduplicate by `(data_source, record_id)` before counting or grouping. A Senzing record's identity is that pair, and re-sending it **replaces** the record rather than adding another (`search_docs`, "Data Source Records (DSRs) Explained"; `get_sdk_reference(topic='parameters', filter='add_record')`; server 1.32.2, verified 2026-07-29) — so *n* rows sharing a key are **one** record, and a mapped file may legitimately hold more rows than distinct keys. Senzing's resolved view, never the file, is the authority on how many records an entity has: a surprising per-record count MUST be cross-checked against it before being reported as a finding. Counting rows once inflated two entities to 23 and 15 records against 2 real ones each, falsely routing both to over-matching review. (Source: `query-programs-dedupe-source-rows-by-record-key`, 2026-07-29.)

- **INV-179** — Before writing code that reads a field from an SDK response, the guide MUST confirm the flag that populates **that field** is present in the flag composite actually in force, reading its `composite_members` — not merely that the field name is correct. A method's own named default composite is **not** `SZ_ENTITY_DEFAULT_FLAGS` and may omit sub-flags that one carries: `SZ_SEARCH_BY_ATTRIBUTES_ALL` and `SZ_FIND_NETWORK_DEFAULT_FLAGS` both carry `SZ_ENTITY_INCLUDE_RECORD_SUMMARY` and omit `SZ_ENTITY_INCLUDE_RECORD_DATA`, and both are the **signature defaults**, so passing no `flags` argument hits this too (server 1.32.2, verified 2026-07-29). A blank field therefore has **three** causes, not two, and the guide MUST NOT stop at the first: a wrong field name, **a correct field name the flags in force do not populate**, or genuinely absent data. Where `response_schemas` confirms the path and a sibling field from the same object reads fine, the flags MUST be suspected before the data, and the fix is to OR in the missing sub-flag — never to switch fields or re-verify a name already known correct. (Extends INV-115, whose two-cause wording this corrects. Source: `method-default-flags-omit-record-data`, 2026-07-29.)

- **INV-178** — A bundled generator that drops a character it cannot encode MUST report the loss on **stderr**, once per run, naming how many distinct characters and passages were affected, identifying the characters by **Unicode name** (never by echoing the character itself), and quoting one locating excerpt with non-ASCII backslash-escaped — while still writing the artifact and exiting 0. The report MUST be pure ASCII, because a legacy Windows code page cannot display the very characters that were dropped and would corrupt the warning into the mojibake it exists to report. A retention or content-retained percentage MUST NOT be treated as covering this class: retention is measured over parsed *source* characters, before sanitisation runs at render time, so a Cyrillic organisation name vanished at "content retained: 96%", exit 0. Reporting MUST be limited to characters that render as **nothing**; a deliberate readable approximation (a stripped combining mark leaving its base letter, or a mapped ASCII equivalent) MUST NOT be reported, or ordinary accented names bury the real losses. (Extends INV-143 from *how* a generator may drop to *that it must say so*. Source: `generators-warn-on-dropped-unencodable-characters`, 2026-07-29.)

- **INV-177** — When a per-source workflow run writes fixed, non-source-qualified filenames into a workspace directory **shared across sources**, every such artifact MUST be relocated to its durable home under a **source-qualified** name before the next source's run begins, and any guidance naming that shared workspace MUST state the qualifier for every fixed filename the workspace receives. `mapping_workflow` writes `profile_report.md`, `schema_hints.md` and `JOURNAL.md` into the single `workspace_dir` under fixed names, with `JOURNAL.md` explicitly append-only (`mapping_workflow(action='start')`, server 1.32.2, verified 2026-07-29; **re-confirmed unchanged on server 1.32.9, 2026-08-13** — step 1 still writes exactly those three fixed names into `workspace_dir` and still calls `JOURNAL.md` "APPEND-ONLY … NEVER rewrite the whole file") — so relocating them unqualified makes each source overwrite, or append onto, the previous source's durable record, silently and with no error. Use the `{source_name}_` form already required for `docs/mapping/{source_name}_mapper.md`. (Source: `module-05-shared-workspace-transient-filename-collision`, 2026-07-29.)

- **INV-176** — Every End-of-Module Summary block MUST be authored in its required **shape**, not merely carry its label: `**What you accomplished:**` and `**Files produced:**` are lists — the label on its own line, then one bullet per accomplishment and one bullet per file with a short "— what it is" gloss — while `**Why it matters:**` is prose that stays inline after its label. The recap PDF renders bullets as bullets and inline text as one wrapped paragraph, so a list authored inline (most often a comma-joined run of paths) reaches the keepsake as a paragraph and cannot be recovered downstream; `--check` validates the label, never the shape, so both shapes pass every existing gate. This binds what the plugin **authors** — `module-completion.md`'s template, graduation's Step 1a backfill, and the shipped `docs/examples/bootcamp_recap.example.md` — and MUST NOT be enforced on **parsing**: existing recaps carry the inline shape and INV-103's presence check is deliberately shape-independent. (Extends INV-103 from which blocks are present to how they are written. Source: `recap-summary-blocks-authored-as-bullets`, 2026-07-29.)

- **INV-175** — Any script the SBCP tells the Bootcamper to `source` MUST resolve its own location in the platform's **default** shell rather than assuming bash, MUST verify the path it computed before exporting anything derived from it, and on failure MUST `return` — never `exit` or `set -e`, which kill or contaminate the Bootcamper's own shell — with a message naming the resolved path (INV-111). `${BASH_SOURCE[0]}` is bash-only and expands to **empty** under zsh, macOS's default shell and therefore the shell a Bootcamper following `source src/scripts/senzing-env.sh` is actually in, so the project root resolves to the wrong directory and the script carries on: every export is wrong and the error surfaces far from its cause. A value derived from an unverified root MUST NOT be exported, and an **empty** value MUST NOT be exported in place of no value — Senzing's own official snippets guard initialization with `settings == null`, testing for *unset* and not empty (`search_docs`, server 1.32.1, 2026-07-28), so an empty export defeats the SDK's own check and fails deeper. Where the plugin ships the idiom it MUST state it once and link to it, and the shipped snippet MUST be executed by a test rather than only asserted present. (Extends INV-166/INV-167 from Windows/PowerShell to the macOS/zsh half of the same class: a supported platform's default shell whose semantics went undocumented. Source: `env-script-must-be-shell-portable`, 2026-07-28.)

- **INV-174** — A completeness, coverage or quality metric MUST NOT count against a record a field that cannot apply to it. Measure per record against the fields applicable to that record's kind — for Senzing data, its `RECORD_TYPE` — and aggregate those per-record results; never average one field's coverage across a whole source of mixed kinds. Applicability MUST be derived from the authority for the data (for mapped output, the Senzing Entity Specification via MCP, which marks type in its feature descriptions and section headings — `DOB` is "Person date of birth", `REGISTRATION_DATE` sits under "(organizations)") rather than from a list hardcoded in the plugin (INV-080), and fields that apply to every kind (`ADDRESS`, `PHONE`, `EMAIL`, identifiers) MUST NOT be excluded. Where a record's kind is unknown — `RECORD_TYPE` is "Recommended", not required — score it against kind-independent fields and report how many records were scored that way. This class of error fails toward false alarm: a sanctions list with NAME and ADDRESS on 100% of records scored 69% and was routed to "recommend fixing before mapping" because person-only features were averaged across a source that was 65% organizations; per-type rescoring gave 97%. (Extends INV-128's presence test from what counts as a present *value* to what belongs in the *denominator*. Source: `quality-score-per-record-type`, 2026-07-28.)
- **INV-173** — Where a validation gate **cannot represent** a legitimate input, its finding MUST NOT be treated as evidence about the data, MUST NOT block the flow (INV-048), and MUST NOT be resolved by altering the data to satisfy the gate. The guidance MUST name the limitation with its provenance, state the exemption path (verify faithfulness against the authority for the artifact — for mapped output, the Senzing Entity Specification via MCP — record the reason, proceed), and MUST NOT ship a forked copy of an MCP-delivered validator, since a fork masks the upstream fix (INV-080). Observed: `sz_verbatim_check.py` harvests its allowed set from string values only (`isinstance(obj, str)`) and offers a key-based waiver alone (`EXEMPT_KEYS = {"DATA_SOURCE", "RECORD_ID"}` plus any attribute ending `_TYPE`), so a numeric source value is reported as a violation whichever way it is emitted — a gate that is unsatisfiable rather than strict, which fired on all 53,321 relationship rows of a correct mapping, and whose obvious workaround distorts the data without even passing. (Second instance of this shape; the first was an MCP-delivered analyzer reporting false errors on a legacy-but-valid format — `analyzer-legacy-sublist-format-false-errors`.) (Source: `verbatim-check-numeric-source-values`, 2026-07-28.)
- **INV-172** — A retained artifact MUST NOT hardcode a fact about the environment that produced it, nor assert an identity for the Bootcamper's data that the caller did not supply. A port MUST be derived from the parsed value actually in use, and dataset wording MUST come from the caller with a neutral default ("the loaded data") — never a guess such as "this Truth Set", since one code path serves the Senzing Truth Set in its own module and the Bootcamper's own data in Query, Visualize and Discover. Such text ships permanently into `docs/visualizations/*.html`, so a wrong claim there is unfalsifiable to the reader and outlives the run: a Module 7 snapshot on a non-default port told its reader to open a port nothing was listening on **and** mislabelled their data as a Senzing demo set. A caller-supplied label reaching rendered HTML MUST be escaped for that context (INV-106). (Source: `snapshot-port-and-dataset-wording`, 2026-07-28.)
- **INV-171** — Apparatus that captures a bootcamper-facing artifact MUST NOT perturb what it is capturing. A view-switching function MUST be idempotent — invoked for the view already shown, it MUST return without redrawing — because a redraw restarts any animated layout and the capture then records an unsettled intermediate state as if it were the result. This binds the app in whichever language it is generated (INV-090/INV-124) rather than any one capture caller, since every capture route (an injected `activate('<tab>')`, `?tab=<id>` deep-linking) and a plain user click all reach the same function; a guard placed in one caller leaves the others defective. Where content animates to its final position, the capture MUST also allow it longer to settle than static content, and that allowance MUST be documented as settle time rather than a timeout. Observed: re-activating the already-active Entity Graph restarted its force simulation mid-capture and produced a valid 47 KB PNG (against ~227 KB) of every node collapsed in a corner, at exit 0, which then reached the recap under a caption describing a graph the image did not show (INV-123). (Source: `default-tab-capture-without-injection`, 2026-07-28.)
- **INV-170** — A value the Bootcamper was **asked for** MUST outrank any value auto-detected before the question was asked, and MUST be persisted everywhere the artifact is generated from. A generator choosing between two sources for the same field MUST prefer the answer and MUST report on stderr when the two disagree, naming both values and the one it used (INV-111). Recording an answer in one place while the generator reads another is indistinguishable from never having asked: graduation's pre-check rejected the auto-detected handle `docktermj`, asked the pinned certificate-name question, recorded the reply as `name` in `config/bootcamp_preferences.yaml` — and the certificate still printed `docktermj`, because the generator read the recap's `**Bootcamper:**` line written at the start of the run, at exit 0 with 99% content retention and no warning (INV-065/INV-113). Retention cannot catch this class: the wrong value does render, so nothing is missing (INV-110 measures loss, not correctness). (Source: `certificate-name-must-reach-the-generator`, 2026-07-28.)
- **INV-169** — A single observation of SDK behavior MUST NOT be generalized into an absolute about a *method*. Where behavior varies with flags, version, or binding, the guidance MUST record what was observed together with the flag set, SDK version and binding it was observed under, and MUST route the reader through a per-installation check (dump one row and read it) rather than asserting the outcome. Stating an over-generalized absolute is worse than stating nothing: it teaches that a correct approach is broken, and a reader who meets a contradicting MCP source has no way to adjudicate without dumping a row themselves. Where an MCP tool documents the behavior, that citation governs and MUST be given (INV-080); the dumped row remains the authority for what a given installation actually returns (INV-115/INV-149). Observed twice: `SZ_EXPORT_ALL_FLAGS` was shipped as "does not exist" and needed a five-place retraction (INV-132), and phase D shipped "do not expect `RELATED_ENTITIES` from `export_json_entity_report`" — false under `SZ_EXPORT_DEFAULT_FLAGS`, and contradicted by `reporting_guide(topic='evaluation')`. (Source: `export-related-entities-is-flag-conditional`, 2026-07-28.)
- **INV-168** — A helper that discovers an external executable MUST search each supported platform's conventional install locations, not only `PATH` plus one platform's absolute paths, and MUST expand those locations from the environment rather than hard-coding a drive letter or a localizable directory name. It MUST NOT report a capability as absent when it is present, and when it does report one absent it MUST name the locations searched — being told to install software one already has is worse than "not found", because it sends the reader to fix the wrong thing. A run where the executable was **found** but the operation still produced nothing MUST be reported distinctly from one where nothing was found (extending INV-122's reason-distinguishing rule to a third case). Where the capability is genuinely missing the helper still degrades per its own contract and MUST NOT install anything to satisfy itself (INV-122, INV-129). Observed: Windows places neither Chrome nor Edge on `PATH`, so a Windows 11 workstation carrying both was told no screenshot capability was available and lost every recap screenshot, while `C:\Program Files\...` was never examined. (Source: `windows-headless-browser-discovery-for-screenshots`, 2026-07-28.)
- **INV-167** — Where the SBCP offers a PowerShell counterpart to a shell command, that counterpart MUST use PowerShell syntax and MUST NOT carry bash-shaped constructs: no `&&`/`||` chaining (use `A; if ($?) { B }`), no `if` used as an expression, no ternary or null-coalescing, no heredocs, and no inline `python -c`. Multi-line or quote-heavy code MUST be placed in a script file under `src/` (INV-018) and run as a file. Each of these is a *parser* error on Windows PowerShell 5.1 — the default `powershell.exe` on Windows 11, whose limits the plugin's guidance MUST assume unless it has verified `pwsh` 7+ — so the message points at syntax rather than at the real cause, which is a command written for another shell. (`tests/test_windows_powershell_guidance.py` sweeps every PowerShell block for `&&`/`||` and MUST pass.) (Source: `windows-powershell-encoding-and-syntax`, 2026-07-28.)
- **INV-166** — Every file the Bootcamp writes MUST be byte-correct UTF-8 with no BOM, and generated files MUST be written through Python or the agent's file tools rather than PowerShell redirection. On PowerShell 5.1 `-Encoding utf8` writes a BOM and `Get-Content` without `-Encoding` decodes as the system ANSI codepage, and both corruptions are invisible to every obvious check — a BOM makes only record 1 of a JSONL fail (reading as one bad source record rather than an encoding fault), and a double-encoded document is valid UTF-8 containing no U+FFFD, merely wrong. Windows-1252 mojibake MUST therefore be detected and reported over `docs/*.md` before the recap renders, and **reported rather than silently repaired**, since a repair is a content change and the normalizer's contract is that it changes formatting only (INV-110). (Source: `windows-powershell-encoding-and-syntax`, 2026-07-28.)
- **INV-165** — An example, hint, or suggested query offered to the Bootcamper MUST be verified by **actually running it** and MUST return at least one result; deriving it from real loaded data is NOT verification. One that returns nothing MUST be dropped with a reported reason (stderr at build time, `console.warn` in a live page), never shipped as a dead control — a hint that finds nothing is worse than no hint, and it also silently misattributes a query defect to the Bootcamper's data. Observed: every Search / Probe chip named after an organization was a real merged entity and still returned nothing, because search tried `NAME_FULL` only (INV-164). (Enforces the "pre-verified" claim the visualization contract already made. Source: `organization-search-requires-name-org`, 2026-07-28.)
- **INV-164** — A name search the Bootcamp performs or generates MUST NOT use `NAME_FULL` alone: it MUST also try `NAME_ORG`, because per the Senzing Entity Specification (Name > Feature: NAME) `NAME_ORG` is the organization name attribute while `NAME_FULL` is the single-field attribute for a name whose type is unknown — so an organization name sent as `NAME_FULL` matches nothing **and raises no error**. The response MUST report which attributes were tried, and a zero-result search MUST be rendered as "nothing matched the attributes tried", never as absence from the data (INV-115). This binds a visualization or query program in **any** language (INV-090/INV-124), not only the bundled Python reference: the defect reached a generated query program precisely because it lived in the reference implementation and in no written rule. Observed on a half-organization dataset, where search silently failed for about half the population. (Source: `organization-search-requires-name-org`, 2026-07-28.) (⚠️ **See also INV-183, 2026-08-11.** It generalises the *lesson* of this invariant — a rule reachable only through a reference implementation does not reach generated code — into a standing requirement that every authoring step names the rules governing its artifact. The attribute rule stated here is **not** superseded and binds unchanged; INV-183 governs where it must appear.)
- **INV-163** — A verification check that cannot run because its tool is absent MUST be recorded as skipped, naming the check, and any completion or closing message MUST state which verification steps did not run; an artifact MUST NOT be described as verified for a check that did not execute. Where a check's tool is not present on every supported platform — poppler (`pdftoppm` / `pdftotext` / `pdfimages` / `pdfinfo`) is absent by default on **both Windows and macOS**, so the page raster and the honest image count are exactly the checks those runs lose — the guidance MUST offer a route needing no new dependency, since INV-129 forbids installing one to satisfy a check. (⚠️ **Platform example corrected 2026-07-31.** As first recorded this named Windows alone, because "poppler is standard on Linux and macOS" is the habitual pairing for Unix-like tooling. It is wrong: poppler is a Linux distribution package, not a macOS system component, and a macOS 26.5.2 machine **with Homebrew in active use** had all four binaries absent. Nothing could have caught it — no test can check what is installed on a platform the suite is not running on, and this repo's CI runs on Linux, where the claim happens to hold. The MUST above is unchanged; only the example was wrong.) Concretely for the recap PDF: the generator's own embedded-image count, then Pillow (already present via `fpdf2` in the project-local venv of INV-066), before `pdfimages -list`, and the `/Subtype /Image` grep last and labeled as overcounting. A skipped check never blocks (INV-048, INV-052/INV-066) and this stays agent-side apparatus, not Bootcamper-facing output (INV-012). (Extends INV-129 from performing verification to reporting its coverage: a keepsake whose layout nobody could inspect is acceptable, one described as verified when its layout was never inspected is not. Source: `pdf-layout-verification-without-poppler`, 2026-07-28.)
- **INV-162** — A generator that drops a referenced asset from a deliverable MUST report each drop on stderr, distinguishing "not found" from "found but unusable", and MUST report an embedded-of-referenced count alongside any content-retention figure. A character-retention figure structurally cannot see a missing image — the characters do render — so retention alone MUST NOT be presented as evidence of a complete artifact: a recap that lost all six of the Bootcamper's screenshots reported `rendered 46707 of 46973 source characters (99%)` and exit 0, and the only clue was an implausibly small file. The drop itself stays non-fatal (INV-048); only silence is forbidden. Where a renderer embeds no assets by design (the stdlib fallback), the count MUST say so rather than reading as a lookup failure. (Extends INV-110/INV-111 from characters to embedded assets. Source: `recap-pdf-images-resolve-against-recap-directory`, 2026-07-28.) (⚠️ **Constrained by INV-193 — cross-referenced 2026-07-31, no meaning change.** The embedded-of-referenced count required here is a **loss** signal: it answers *"did anything this document referenced fail to embed?"*. It MUST NOT be presented as evidence that the artifact is **complete**, because its denominator is read from the document being rendered and therefore cannot see a captured view the document never referenced — a recap embedding four of six captured tabs reports `embedded 4 of 4 images`. A completeness claim MUST take its denominator from outside the artifact (INV-193), and where no external count is available the coverage check MUST report itself skipped rather than passed (INV-163). Both counts are required and neither substitutes for the other; stated here because the any-language contract is built from these invariants and not from the Python reference's comments.)
- **INV-161** — A relative asset reference inside a generated deliverable's source document (e.g. a recap `![alt](path)`) MUST be resolved against **that document's own directory**, never against the process working directory. The path that is correct for reading the source in a Markdown renderer MUST also be correct for rendering it, so an author following the instruction cannot produce a lossy artifact, and no step may require a `cd` to make assets resolve. (Before this, `generate_recap_pdf.py` resolved against `Path.cwd()` while graduation ran it from the project root, so document-relative screenshot paths silently resolved to nothing and no path satisfied both the Markdown recap and the PDF. Source: `recap-pdf-images-resolve-against-recap-directory`, 2026-07-28.)
- **INV-160** — Where an MCP tool's response carries **both** a payload field and metadata describing that payload's size or completeness, a payload that contradicts its own metadata MUST be treated as a **failed retrieval**, never as a real empty result: concretely, `find_examples` file retrieval (`repo` + `file_path`) returning an empty `content` alongside a non-zero `content_length` means the retrieval failed, and `truncated: false` MUST NOT be read as confirming completeness. The guide MUST fall back to the response's own documented access path (fetch `raw_url`, or clone per `access_steps`) and MUST NOT tell the Bootcamper an example file is empty, or silently skip the example, on that basis. An undeclared parameter MUST NOT be adopted as the remedy even when the response's own prose advertises one — only parameters the live schema declares may be passed (INV-136). This does **not** conflict with INV-149: an empty result with *no* contradicting metadata (an empty `response_schemas` `data` array) is coverage, not failure — it is the contradiction that signals the defect. Verified 2026-07-28 against server 1.32.1. (⚠️ **Premise corrected 2026-08-11 against server 1.32.8; every MUST above is unchanged and still binding.** The `find_examples` file-retrieval payload no longer contradicts its metadata — it **declares** the omission: the empty `content` now arrives with **`content_elided: true`** and an ordered `access_steps` array (fetch `raw_url` → `git clone` → an `inline` step that only clients forwarding undeclared arguments can use). So that response is a deliberate elision, not a failed retrieval, and the concrete example above should be read as what 1.32.1 returned rather than as the current test. Proved rather than inferred: a ~20 KB file and a ~800-byte file elide identically, with and without `max_lines`, so it is unconditional and no smaller request returns the body; search mode still returns real `code_snippet` content. The same elision now applies to **`generate_scaffold`**, whose `snippets[]` carry `raw_url`, `size_bytes` and `line_count` and no inline code — so this invariant's scope is the elision shape wherever it appears, not `find_examples` alone. The undeclared-`inline` prohibition is untouched and is now stated by the server itself: *"Clients that validate arguments against the declared schema cannot use this step; prefer fetching raw_url or cloning."* What this changes is the **reason** the guidance holds, not the guidance: fall back to `raw_url`/clone, never tell the Bootcamper an example file is empty, never adopt an undeclared parameter. Source: `find-examples-elision-is-by-design-not-a-failed-retrieval`.) (Source: `find-examples-file-retrieval-returns-empty-content`, 2026-07-28.)
- **INV-159** — A generator MUST NOT reduce any character of a Bootcamper-authored value to `?`, and this MUST hold for the Bootcamper's **name** specifically — the string INV-134 detects from `git config user.name` and the Certificate of Completion prints largest. Characters outside the built-in fonts MUST be folded to a readable ASCII equivalent where one exists (including Latin letters with no NFKD decomposition — `Ł ł Đ đ Œ œ …`, whose loss changes the name rather than degrading it) and otherwise **dropped**, never encoded. A name in a non-Latin script MUST NOT be transliterated by the plugin: how it is spelled in Latin script is the Bootcamper's decision, so the generator MUST instead (a) warn on stderr naming the unprintable characters, (b) print the `CERTIFICATE_NAME_PLACEHOLDER` rather than a blank recipient line, and (c) make "not printable in the recap PDF" one of the conditions that triggers INV-113's pinned name question at graduation, where declining still costs nothing (INV-048). The font-safety inventory MUST cover names, not only punctuation and symbols. (Enforces INV-143, whose inventory covered what deliverables carry *except* the most certain thing they carry: a Bootcamper named 李明 had `??` printed on their certificate, silently, at exit 0 — found by the 2026-07-28 deep-dive audit. Complements INV-113/INV-134; `tests/test_recap_pdf_font_safety.py` enforces it and MUST pass.) (Source: `deep-dive-audit-2026-07-28`, 2026-07-28.)
- **INV-158** — Wherever the SBCP directs the Bootcamper to act **in their Claude interface** — set a model, change reasoning effort, restart an MCP server, click a control — or documents how to install and start the bootcamp, it MUST name **which** interface, using these names: **Claude Desktop** (the desktop application), **Claude Code CLI** (Claude Code in a terminal), **the Claude web app**, **a Claude IDE extension**. "Claude Code" names the *product* and MUST NOT be used to mean an interface — it is correct for what is true of the harness everywhere (a Claude Code plugin, Claude Code hooks, an MCP server configured in Claude Code) and wrong as a synonym for the terminal, because Claude Desktop runs Claude Code too. **"The Claude app" / "the Claude application" is retired**: it named none of the four, so a Bootcamper told to use "your Claude app's model and effort controls" had to guess which controls were meant; it may appear only on a line that frames it as retired (`tests/test_retired_vocabulary.py`, which for this reason also scans the repo-root install docs `propagate-to-public` mirrors as user-facing). Vague wording ("in your Claude interface") is permitted **only** where the interface genuinely cannot be determined, never as a shortcut for one that can. The install docs MUST document both primary interfaces by name, state that they are two interfaces of one Claude Code plugin, and cross-link with anchors that resolve. This is a naming requirement only: the four interfaces INV-098 adapts to are unchanged, and INV-098's own wording was clarified in place to satisfy this. (Source: `name-the-claude-interface`, 2026-07-28.)
- **INV-157** — The labeled blocks INV-103 requires inside each module's **End-of-Module Summary** — What you accomplished, Files produced, Why it matters (the "Bootcamper's takeaway" line stays optional) — MUST be enforced below the heading, not merely documented. `generate_recap_pdf.py`'s `--check` MUST fail naming each block a present summary does not carry, the pre-render audit MUST report the same gap as a warning (non-blocking: warn, render, exit 0 — INV-110/INV-048), and **both** renderers (INV-066) MUST *draw every required block*, marking one the recap does not carry as `(not recorded)` rather than omitting it — an absent block MUST NOT be able to look like a complete page. Detection MUST tolerate the forms a live recap produces (`**Label:**`, `**Label**:`, `- **Label:**`, a bare `Label:`), because a false "missing" sends graduation to backfill content that is already present or to rewrite a finished section (INV-085); legacy `### Journal` sections stay exempt, as INV-103 tolerates them. The requirement MUST also be stated where the recap is written (module-completion's recap step, verified in its existing re-read) and repaired before rendering (graduation's reconcile), and a backfill MUST NOT invent content to fill a label — an unsupported block is left for the renderer to mark. (INV-103 has required these blocks since 2026-07-23 and stated that `--check` must validate them; `--check` compared only H3 headings, so a summary written as one prose paragraph passed as "Recap complete", rendered at ~100% retention, and reached a bootcamper's keepsake with all three blocks absent. Reported from a bootcamp run 2026-07-28.) (Enforces INV-103; complements INV-110 and INV-129.) (Source: `end-of-module-summary-blocks-guaranteed`, 2026-07-28.)
- **INV-156** — The recap's Certificate of Completion MUST be laid out from the Senzing certificate template (`resources/certificate-of-completion.pdf`, a maintainer asset like the style reference behind `brand_tokens`): the ember gradient band, the bordered white card, the Senzing wordmark, the eyebrow, the headline, the tagline over its rule, the presented-to line, the recipient's name, the citation, the award seal, and the `DATE COMPLETED` / `ISSUED BY` signature blocks. Both renderers (INV-066) MUST read **one** shared set of geometry constants, so the fallback is a plainer *rendering* of that design and never a second design; every shared line MUST sit at the same height in both, verified positionally. The certificate MUST NOT claim more than the recap records: the citation states the number of modules **actually completed** (the template's "all 10 modules" is a claim about a full run) and any description of the bootcamp's arc attaches to the bootcamp, not to what was completed — while the modules completed remain named on the page (INV-100). The variable-length module list MUST be *fitted* — wider measure, then type size, truncation last — never allowed to print over the seal or the signature row, since the page is a fixed design with the auto page-break off. Letterspacing MUST be applied through the text state (`Tc`), never as spaces inserted between glyphs: a certificate is searched and copied out of, and "A l e x" is not a name. Colours MUST come from `brand_tokens` or be *derived* from it in the manner of `TABLE_HEAD_FILL` (INV-081/INV-107) — never sampled off the template — and where only the light wordmark ships it MUST be repainted for the white card at render time rather than duplicated as a second asset. Verification MUST be by rasterizing the page (INV-129). (Discharges the deferred template criterion of `landscape-certificate-of-completion`; complements INV-100, INV-113 and INV-126.) (Source: `certificate-of-completion-from-template`, 2026-07-28.)
- **INV-155** — The consolidated visualization app's tab set is exactly **six**: Entity Graph, Merge Statistics, Match Keys, Feature Scores, Cross-Source, and Search / Probe — in the row order of the tab table in `module-03b-truthset-visualization/visualization-api-reference.md`, which remains the ordering authority (INV-147). The relationship-network view MUST be a **mode** of the Entity Graph tab (its "Show only entities with relationships" toggle, both modes served by the same `/api/graph` payload), and no-query merge browsing MUST be a **button** on Search / Probe ("Show all merged entities"); neither may be a tab of its own. There MUST be no Results Dashboard tab and no `/api/dashboard` endpoint — its counts and histogram duplicated `/api/stats`, which now also carries its only unique payload (`sample_entities`) — and `/api/records` MUST be served, since it backs the Records action every entity surface is required to offer. A tab is still shown only when its data exists. (This is INV-104's de-duplication rule applied to the enumeration in INV-104's own text: that list — written 2026-07-23 — named "relationship network" and "results dashboard" as tabs, and the consolidations that removed them registered no invariant, leaving the shipped six-tab app non-conformant with a still-standing invariant. INV-104's single-app, no-separate-static-pages, tab-only-when-data-exists and offer-counts-for-INV-046 guarantees are unchanged and still binding; only its tab enumeration is superseded. Source: `deep-dive-audit-2026-07-27`, retroactively recording `consolidate-merge-statistics-and-results-dashboard-tabs` (2026-07-25) and `consolidate-truthset-viz-merges-and-network-tabs` (2026-07-26), 2026-07-27.)
- **INV-150** — A note an MCP tool computes from a parameter the caller supplied is a **conditional**, not a measurement of this installation. Where the Bootcamp already holds a **detected** value for the same thing — the licence record limit, the installed SDK version, the platform — the detected value governs: the generic note MUST be suppressed rather than relayed (INV-012, a warning the Bootcamper cannot act on is noise) and the divergence recorded in the checkpoint. This does NOT weaken INV-080: both sides remain MCP-sourced, one generically and one by measuring the Bootcamper's own machine, and answering from training data stays forbidden. Concretely: `sdk_guide`'s `record_count` contract states that values above 500 "surface license guidance (default Senzing license limit)" computed from the count alone, so that `LICENSE REQUIRED` note MUST be reconciled against `license_record_limit` (persisted by Module 4's licence gate from `SzProduct.get_license()`) before it is relayed or acted on — `0` or a limit at/above the dataset size means suppress it entirely. Acting on it unreconciled sends a Bootcamper with an unlimited licence to sample down to 500 records, and the shrunken dataset then under-demonstrates the cross-source resolution Modules 6 and 7 exist to show. (Source: `reconcile-sdk-guide-license-note-with-detected-limit`, 2026-07-26.)
- **INV-151** — The Bootcamp's redo drain MUST terminate. The loop sentinel MUST be the **fetch returning no record**, and a redo-*count* method MUST NOT be used as the loop condition — it is a full table scan per call, making the drain O(n²), and because processing a redo record generates more redo records the loop runs longer than the initial count suggests (384 backlog, 400 processed calls observed). Before running an MCP-returned redo snippet the guide MUST check whether it loops on an empty queue: the templates target streaming ingest, and the observed `sdk_guide(topic='redo')` answer prints a pause message and continues forever, so running it unmodified after a batch load hangs the session with no error — indistinguishable from slow work. If it loops, it MUST be adapted (keep the structure and concurrency, replace sleep-and-continue with a break). The drain MUST report how many records it processed and that the queue reached empty; a silent finish cannot be told from a hang. The production always-running processor MUST be described as the *streaming* pattern, explicitly distinguished from the batch drain. (Source: `redo-batch-drain-must-terminate`, 2026-07-26.)
- **INV-152** — The Senzing factory (environment) MUST outlive every engine it creates: a helper that constructs the factory in a local and returns only the engine returns a **dead** engine, because nothing keeps the factory alive. The resulting `SzSdkError - engine object has been destroyed and can no longer be used, create a new one` means *garbage-collected*, not explicitly destroyed, and surfaces at the first engine call far from the line that caused it. The factory MUST be held for the process lifetime or returned alongside whatever it created. Stated as ownership rather than a Python idiom (INV-002): however the chosen language expresses lifetime, the factory's MUST enclose every engine's. (Source: `factory-must-outlive-every-engine-it-creates`, 2026-07-26.)
- **INV-153** — A truncated label MUST remain distinguishable **wherever it appears and whatever it labels** — a bar-chart category, an entity name on a graph node, a legend entry, any future chart: **no two rendered labels may be identical unless their underlying values are identical**, the untruncated value MUST be reachable on hover, and truncation MUST NOT remove the leading characters. Distinctness MUST be checked by comparing the **fitted** strings, not the source values, and a pair that collides while its values differ MUST be disambiguated. Because a server generated in another language is built from `visualization-api-reference.md` and never reads the Python reference (INV-090/INV-124), this general rule MUST be stated in that contract as its own requirement and not only inside a rule about one label kind. (Scope clarified in place 2026-07-29 — no meaning change: this first clause was always unqualified, and the sentence about match keys below has always been an *application* of it rather than its extent. It had been implemented for match keys alone, so graph node labels sharing a 19-character prefix rendered identically on the one surface whose purpose is telling entities apart; `tests/test_graph_label_distinctness.py` now enforces the general property and MUST pass. Found by the 2026-07-28 deep-dive audit, scope settled by the maintainer 2026-07-29.) The label gutter MUST be sized from the data before anything is truncated, and a label MUST NOT be left to overflow and be clipped by the viewport — clipping removes the head of a right-anchored string, which is exactly the distinguishing part. For match keys, middle-ellipsis is required: they are `+A+B+C…` sequences that commonly share a long prefix and differ only in the final segment, so head-only truncation renders the top bars identically — the same unreadable chart failing from the other end. The distinctness property, not the ellipsis strategy, is the requirement. (Source: `visualization-legibility-at-production-scale`, 2026-07-26.)
- **INV-154** — A visualization default that governs legibility MUST be a function of the rendered data's scale, and the threshold MUST be stated as a number in the any-language contract so every implementation (INV-090) picks the same behaviour. Above **400 entities** the Entity Graph MUST open on the relationship subgraph rather than the full population, provided a subgraph exists (`relationships_total` > 0); the mode toggle MUST still switch both ways, a Bootcamper's explicit choice MUST NOT be overridden by a later redraw, and an inline note MUST state both counts ("Showing the N entities that have relationships, of M total") for the same reason the label note exists — otherwise a default reads as the data. Hiding labels does not thin edges: at 4,464 relationships the full population conveys shape only. (Complements the scale-aware *label* defaults, which this extends to node selection.) (Source: `visualization-legibility-at-production-scale`, 2026-07-26.)
- **INV-148** — A **partially populated** parsed row MUST be treated as a probable wrong field name, not as partial data returned by Senzing: where some fields of a record populate and others come back blank, the blank ones' names MUST be confirmed against a dumped raw response before the row is rendered. A half-populated row is more deceptive than an empty one — the fields that did populate signal that the parse worked, so it reads as a real-but-thin result rather than a bug. Observed: `ENTITY_NETWORK_LINKS` entries parsed under the `ENTITY_ID` / `RELATED_ENTITY_ID` pairing used elsewhere yielded `None` for **both** endpoints while `MATCH_KEY` rendered correctly, presenting as a relationship Senzing could not fully describe. (Extends INV-115 from a single blank field to a partial record.) (Source: `network-link-fields-and-uncovered-response-schemas`, 2026-07-26.)
- **INV-149** — A `get_sdk_reference(topic='response_schemas')` result that is **empty** or that stops at the top-level shape is coverage, not a failed call: it MUST NOT be retried as an error, and the dumped raw response is the authority for anything below what it documents. Guidance that requires the lookup MUST state, at the point it requires it, what an empty or shallow result means — otherwise the reader retries a call that will never return more, or treats the top-level arrays as the whole answer. Verified 2026-07-26: `filter='find_network'` returned an entry documenting `ENTITY_PATHS[]`, `ENTITIES[]` and `ENTITY_NETWORK_LINKS[]` with **no** element fields, while `get_version` and `get_license` returned an empty `data` array. **Corrected in place 2026-07-29 (dry run, phase 1) — the first half no longer holds:** on server 1.32.1 `filter='find_network'` now returns the element fields itself (`ENTITY_NETWORK_LINKS[].MIN_ENTITY_ID`, `.MAX_ENTITY_ID`, `.MATCH_KEY`, `.MATCH_LEVEL_CODE`, `.ERRULE_CODE`, `.IS_AMBIGUOUS`, `.IS_DISCLOSED`, plus `ENTITIES[].RESOLVED_ENTITY.*` and `ENTITY_PATHS[].*`), so those names are MCP-confirmed and MUST NOT be carried as unverified cautions any longer; `get_version` still returns an empty `data` array, so the second half stands and remains the example of coverage-not-failure. **Both halves re-confirmed on server 1.32.9, 2026-08-13** (enumeration sweep): `filter='find_network'` still returns the element fields, and `filter='get_version'` still returns `"data": []`. **The rule this invariant states is unchanged** — an empty or shallow result is still coverage rather than a failed call, and a field name that *cannot* be MCP-confirmed still may not be written as the name to parse with. What changed is a dated observation about one method, which is exactly why the lookup must be re-run rather than quoted from here: this server's coverage grows. A field name that cannot be MCP-confirmed MUST NOT be written into guidance as the name to parse with; it may appear only as a marked, unverified caution about where to look (INV-080). (Clarifies INV-115's "does not reach" to cover absent as well as shallow coverage.) (Source: `network-link-fields-and-uncovered-response-schemas`, 2026-07-26.)
- **INV-146** — Every screenshot a visualization capture produced MUST reach the recap. Because capture is one image per tab (INV-122), each file is already a distinct view, so a count cap or "best of" selection can only delete unique content — and any such judgement pulls toward the visually striking, which is what dropped Merge Statistics, Match Keys and Feature Scores (the three *analytical* tabs) from a six-tab app while the surrounding prose described all six, leaving the same three tabs repeated across two visualization sections. Only a true duplicate — two images of the **same** tab, which per-tab capture should not produce — may be deleted. This binds both the capture-time embed (`module-completion.md`) and graduation's orphaned-screenshot backfill; neither may cap, and the backfill in particular is the safety net for a missed embed, so capping it means it cannot restore what the capture step dropped. (Source: `embed-every-captured-tab-in-tab-order`, 2026-07-26.) (⚠️ **Closed INV-123's omission branch — recorded 2026-07-31, no meaning change here.** INV-123 permitted omitting an inert Search / Probe capture as an alternative to captioning it; this invariant had already made that unavailable, and the two shipped without either naming the other. INV-123's disjunction is now removed and the caption is its sole remedy. Nothing in this invariant changes: every screenshot the capture produced still MUST reach the recap, and a true same-tab duplicate is still the only deletable case.)
- **INV-147** — Screenshots MUST be embedded in the recap in the app's tab order — the row order of the tab table in `module-03b-truthset-visualization/visualization-api-reference.md`, which is declared there as the ordering authority — and never in capture, append or filename-discovery order. Both the capture-time embed and graduation's backfill MUST cite that table rather than restate the tab list, so changing a tab's position updates one file; a tab that produced no image is skipped without disturbing the relative order of the rest. Ordering the image lines a backfill itself produced is not a prose rewrite and does not violate the append-only rule (INV-085), which protects a completed section's narrative. The recap is a walkthrough of the app, so a reader must be able to line its images up against the interface left to right. (Source: `embed-every-captured-tab-in-tab-order`, 2026-07-26.)
- **INV-144** — A validator's non-zero exit MUST NOT by itself gate a Bootcamp step. Its findings MUST be classified into **structural invalidity** (the data cannot load — blocking) and **conformance to a recommendation** (the data loads and resolves but is not in the recommended shape — informational), and only the former may block. A conformance finding MUST NOT trigger remapping, rewriting or regenerating data that already loads; converting to the recommended shape may be offered as an optional improvement, never as remediation of a defect or a precondition for continuing. Where a tool's verdict and an authoritative specification **from the same source** disagree, the disagreement MUST be resolved empirically — load one unmodified record and read back the features Senzing extracted — and the probe result and concluded cause MUST be recorded (INV-125); it MUST NOT be settled by preferring one document over the other. Observed: `sz_json_analyzer.py` returns exit 1 with hundreds of "Missing or non-array FEATURES" errors, plus the warning `No NAME features found`, against data that loads and resolves identically to the recommended shape — the analyzer measures conformance to the *recommended* schema (whose validation rules do say `FEATURES` is required), while the specification's prose grants continued support for the legacy shape. Both statements are true and they answer different questions; a warning that a feature was not found where the tool looked MUST NOT be reported as that feature being absent. (Source: `analyzer-legacy-sublist-format-false-errors`, 2026-07-26.)
- **INV-145** — A check on record shape MUST accept every shape the Senzing Entity Specification supports, not only the recommended one: currently the `FEATURES` array **and** the legacy flat structure — "a flat JSON structure with a separate sub-list for each feature that had multiple values", i.e. feature attributes at the record root with a per-feature root sub-list (`NAMES`, `ADDRESSES`, `IDENTIFIERS`) wherever a feature repeats, and no sub-list at all where no feature repeats. A source's shape MUST NOT be inferred from its provenance and MUST be determined by sampling the actual records: CORD ships both forms (verified 2026-07-26 — London/`GLOBALDATA` returns a `FEATURES` array, Las Vegas/`PPP_LOANS` returns flat root attributes). A readiness or fast-path gate keyed on `FEATURES` alone classifies loadable sources as not-ready and routes them through a mapping phase they do not need. (Source: `analyzer-legacy-sublist-format-false-errors`, 2026-07-26.)
- **INV-142** — A bundled generator MUST render a Markdown construct as that construct, never as its source text. Tables MUST be drawn as a grid: bordered cells, a visually distinguishable header, the `|---|` alignment row dropped, and the header repeated when a table spans a page break — with the first body row on a continuation page rendered in the body font, never inheriting the repeated header's font. Ragged rows MUST be padded or truncated to the header's column count rather than desynchronising the grid or shifting a neighbouring row's column origin, and two adjacent tables MUST be visibly separated so they cannot read as one grid whose middle row happens to be bold. Both the preferred and the fallback renderer MUST satisfy this (INV-066); the fallback may use aligned monospace columns but MUST NOT emit pipe source. Verified positionally or by rasterizing the page, never by text extraction alone — 61 raw pipe lines reached a bootcamper's PDF while every success signal (exit 0, `PDF generated:`, 100% content retention) reported success, because the text *was* in the stream, merely unreadable. (Complements INV-121, which governs *where* a run is drawn; this governs *what* is drawn. Verification standard per INV-129.) (Source: `discoveries-pdf-real-tables-and-paragraph-spacing`, 2026-07-26.)
- **INV-143** — A generator's character sanitisation MUST NOT substitute `?` for a character it cannot encode: every character a bootcamper-authored deliverable can carry MUST map to an ASCII equivalent or be dropped deliberately. Latin-1 encodability is necessary but **not** sufficient — `?` is itself encodable, so a source-pair table reading `GLEIF ? OPEN-OWNERSHIP` and a caveat opening `??` pass every encoding check while being wrong on the page. The character inventory under test MUST cover what generated **deliverables** carry, not only what the plugin's own templates emit: scanning the templates is what let `↔`, `⚠️` and thirteen further characters (`≈ ≤ ≥ ≠ ∞ € ™ ⇒ ↑ ↓ ←`, the non-breaking hyphen and the zero-width space) ship corrupted in both PDFs. `tests/test_recap_pdf_font_safety.py` enforces both halves — encodable *and* not reduced to `?` — and MUST pass. (Source: `discoveries-pdf-real-tables-and-paragraph-spacing`, 2026-07-26.)
- **INV-138** — The module/graduation-start model/effort nudge MUST decide whether to ask by comparing the stage's recommendation against the model and reasoning effort the Bootcamper is **currently running**, not against the previous stage's recommendation; the previous stage's row is a fallback used ONLY where the current setting cannot be determined. When the current setting already matches the recommendation, the guide MUST present the one-line statement and MUST NOT ask the switch question — running one model for the whole Bootcamp is a supported choice, so asking someone already on Opus 5 at high effort to switch to Opus 5 at high effort is the pointless question INV-006 and INV-012 forbid. When it differs, the guide MUST ask, in **either** direction — a step up and a step down both ask, because the choice is the Bootcamper's either way — and MUST name only the dial (model or effort) that actually differs, never instructing a change to a dial already set correctly. (Supersedes INV-137's trigger only. Every other part of INV-137 is unchanged and still binding: the pinned 👉 switch question as its own yielding turn, the pinned "👉 Are you done modifying the model and effort?" gate following a **yes** and nothing else, the stage's first step deferred to the turn after confirmation, the guide never changing the session itself, the absence of any `model_guidance` preference, and the apparatus-exempt setup stages presenting no guidance at all.) (Source: `reassess-per-module-model-effort-assignments`, 2026-07-26.)
- **INV-139** — When the recommended model/effort sits **below** what the Bootcamper is currently running, the switch question itself MUST identify it as a step down, MUST state that the recommendation is a cost saving rather than a capability the stage needs, and MUST state that staying put costs them nothing. (Extends the below-current flagging INV-137 retained from INV-120, which applied only to the recommendation-matches statement — the one branch where a downgrade cannot arise — so every real downgrade prompt shipped unexplained. Because the pause is symmetric under INV-138, a step down is asked exactly as a step up is, and an unframed downgrade prompt reads as being asked to accept a worse experience.) (Source: `reassess-per-module-model-effort-assignments`, 2026-07-26.)
- **INV-140** — Every stage the Bootcamp can run MUST have exactly one row in the per-stage model/effort recommendation table, including the apparatus-exempt setup stages (Onboarding, Bootcamp preparation, Entity Resolution Concepts) that present no nudge themselves — so INV-138's comparison always has a defined value on both sides. Adding a module without rating it is a defect; `tests/test_model_guidance_sync.py` checks the table against the canonical module list in Bootcamp preparation (INV-079) and MUST pass. (Source: `reassess-per-module-model-effort-assignments`, 2026-07-26.)
- **INV-141** — A per-stage model/effort row MUST name exactly one model and one reasoning effort. A conditional or two-branch recommendation (e.g. "Sonnet 5, high effort (Opus if bespoke load code)") is forbidden: the nudge fires at stage start, before the condition can be resolved; it cannot be pinned into the verbatim switch question INV-056 requires; and it gives INV-138's comparison two answers. (Source: `reassess-per-module-model-effort-assignments`, 2026-07-26.)
- **INV-137** — Model/effort guidance MUST be unconditional and MUST NOT be configurable: the Bootcamper is NEVER asked how they want it handled, and there is NO `model_guidance` preference — Bootcamp preparation asks no such question and writes no such key, and every reader MUST NOT honor a stale `model_guidance` value left in an older `config/bootcamp_preferences.yaml`. At every module start and graduation start the guide surfaces the stage's recommendation, and the behavior depends only on whether the recommendation **changed** from the stage just completed: **changed** → the pinned 👉 switch question as its own yielding turn, then on **yes** a one-line run-commands statement followed by the pinned "👉 Are you done modifying the model and effort?" gate, with the stage's first step deferred to the turn after the Bootcamper confirms (on **no**, the first step lands that same reply turn); **unchanged** → a concise one-line statement naming model and effort as separate dials, stating either can be changed at any time and applies from the next message, and explicitly flagging a recommendation that sits *below* the current setting so it never reads as advice to downgrade. The confirmation gate follows a **yes** to the switch and nothing else. The guide MUST NEVER change the session model or effort itself — only the Bootcamper can. The apparatus-exempt setup stages (Bootcamp preparation, INV-075; Module 0, INV-078) present no guidance at all. (Supersedes INV-119 and INV-120, retiring the `advisory`/`off`/`prompt` modes and their capture question, and restoring the unconditional INV-063/INV-069 behavior. Maintainer decision, 2026-07-26: the pause is wanted every time the recommendation changes, and the choice is not the Bootcamper's to make. INV-133 no longer governs `model_guidance`, which no longer exists; it continues to govern `path`, `verbosity` and `programming_language`.) (Source: `retire-model-guidance-modes`, 2026-07-26.) (Trigger superseded by INV-138, 2026-07-26: the comparison is against the model/effort the Bootcamper is **currently running**, not against the stage just completed — recommendation-to-recommendation asked a Bootcamper already on the recommended setting to switch to it. Everything else in this invariant stands unchanged, including the pinned questions, the gate-after-yes-only rule, the deferred first step and the apparatus-exempt carve-out. The downgrade flagging here is extended to the switch question by INV-139.)
- **INV-136** — Before invoking any MCP tool, the guide MUST satisfy that tool's **required parameters** as the live schema states them, and MUST use only its **enumerated** action/topic values; a documented workflow step MUST NOT be written in terms of a parameter name used as though it were an action. Concretely: `mapping_workflow(action='start')` MUST pass both `file_paths` and `data.workspace_dir`, and `analyze_record` MUST pass `workspace_dir` — both calls fail outright without them — and `workspace_dir` MUST be a project-local directory (`data/mapping/`), never `/tmp` or a home-relative path, which is the file-placement rule expressed as a parameter. `mapping_workflow`'s only valid actions are `start`, `advance`, `back`, `status` and `reset`; every step advance is `action='advance'` with the step's fields in `data`/`payload`. The opaque `state` from each response MUST be echoed back verbatim, never reconstructed from the bootcamp's own checkpoint. When a step's guidance or an error arrives truncated, the client read cap MUST be excluded before the failure is attributed upstream — the tool documents that a cap below 64 KB "silently TRUNCATES step guidance mid-text and reads as a server bug" — and the concluded cause MUST be recorded (INV-125). (Hardens INV-080 from *what* the guide asks MCP to *how* it calls it. Found by the 2026-07-26 dry run, which caught five payload field names written as actions and both required `workspace_dir` parameters absent — breaking Module 5's mapping workflow at its first call, invisibly to three prose audits and 399 tests.) (Source: `dry-run-mcp-call-contracts`, 2026-07-26.)
- **INV-135** — The evaluation-license request (`submit_feedback` with `category='license_request'`) is the ONLY step permitted to transmit the Bootcamper's personal details off their machine, and it MUST NOT run without their explicit yes to a pinned 👉 question (INV-056) that states what is sent, to whom, and what comes back. Its required first name and work email MUST be requested one question per turn, held for the call alone, and NEVER written to `config/`, the recap, or the feedback file (INV-065); the identifier-stripping rule that governs defect reports (`feedback.md` Step 3c) cannot apply to this call because the call does not function without those fields, so the **consent** discipline carries over instead of the stripping. Declining MUST cost nothing — the external channel and apply-an-existing-key paths remain open, the bootcamp continues on the built-in evaluation license, and the question is asked once (INV-006). A defect report MUST NEVER be sent under `license_request`, and personal details MUST NEVER be sent under a defect category. (Closes the gap where the high-PII path had no gate while the low-PII defect path had a full one. Source: `dry-run-mcp-call-contracts`, 2026-07-26.)
- **INV-134** — The Bootcamper's `name` — the value printed on the recap header and the Certificate of Completion — MUST be **detected silently** during Bootcamp preparation (from `git config user.name`, else the environment) and held for that module's single consolidated preference write (INV-058). Bootcamp preparation MUST NOT ask for it, and when no display name is available it MUST leave `name` unset rather than inventing, guessing, or substituting a host/system value (INV-065). Every downstream consumer — the recap header, graduation, the certificate — reads the persisted `name`; the **only** sanctioned question about it anywhere in the bootcamp is graduation's certificate-quality fallback (INV-113), asked once and only when the detected value is missing, empty, or clearly not a display name. (Recorded 2026-07-26 by the deep-dive audit: the spec below was implemented and ledgered but registered no invariant, and INV-113 plus `graduation/SKILL.md` had been citing INV-076 — which governs the Core-vs-Customized path choice and says nothing about the name — as its authority.) (Source: `bootcamp-prep-name-never-asked`, 2026-07-20.)
- **INV-132** — Before writing code that **calls** a Senzing SDK method, the guide MUST confirm that method's parameter shape for the Bootcamper's chosen language binding. Cross-language documentation MUST NOT be treated as authoritative for parameter shapes — the same method takes a JSON document in one binding and a native collection in another (Python's `find_network_by_entity_id` takes `List[int]`, not the entity-IDs document the Java/C# signatures imply). The reference reaches parameter shapes under **any** topic whenever `filter` names a method — the response carries a `method_signatures` block, so a `flags` or `response_schemas` result already in hand answers the shape without a second call, and flag entries additionally carry `composite_members`, `depends_on` and `response_paths`, which is what a flag family selects. (Corrected in place 2026-07-30 against MCP server 1.32.2: this invariant previously asserted that `flags` and `response_schemas` "document neither the argument types nor what a flag family selects". Both halves were false — `topic='flags', filter='find_network_by_entity_id'` returned every binding's signature plus per-argument divergence warnings, and `topic='response_schemas', filter='get_version'` returned one alongside an empty `data` array. Telling the guide that data it can see is not there is the INV-169 failure mode. Source: `sdk-reference-carries-signatures-under-every-topic`.) It also reaches them via `get_sdk_reference(topic='methods', filter='<method>')` (aliases `functions`/`classes`/`api`), which returns the binding's own signature — this MUST be consulted before falling back to anything local; only where that topic genuinely does not reach is the installed binding the authority, via introspection (`help`, `inspect.signature`, `dir` on the flags enum). (Corrected in place 2026-07-26 by the dry run: this invariant previously asserted the MCP reference could not reach parameter shapes and sent the guide straight to local introspection. Verified against the live server, `topic='methods'` returns `find_network_by_entity_id(entity_ids: List[int], …)` — the exact shape cited below as unreachable. The correction removes a routing-away-from-MCP instruction that contradicted INV-080; the per-binding-confirmation requirement is unchanged.) A flag family's *meaning* and a composite flag's *availability* MUST likewise be confirmed per binding rather than inferred from a name: on the export methods `SZ_EXPORT_INCLUDE_*` selects which entities become rows while `SZ_ENTITY_INCLUDE_*` selects what detail each row carries, and `SZ_EXPORT_ALL_FLAGS` exists for those methods yet is absent from the Python binding. (Extends INV-115 from a method's responses to its inputs; INV-080 still forbids asserting any of it from training data.) (Source: `verify-sdk-parameter-shapes-and-flag-families`, 2026-07-26.)
- **INV-133** — A setup preference already recorded in `config/bootcamp_preferences.yaml` MUST be honored and its capture question MUST NOT be asked; the value in force MUST be stated once in the setup-choices recap (INV-099) whether it came from a question or from the saved file, and a saved value MUST NEVER be overwritten with a recommended default. This gives a returning Bootcamper a way to settle a recurring setup choice permanently without changing the default for anyone else. (Hardens INV-006 for setup preferences; each preference's ask-when-absent behavior is unchanged. Clarified 2026-07-26: this invariant cited INV-119's model-guidance question as its example, which INV-137 has since retired — it now governs `path`, `verbosity` and `programming_language`, and a stale `model_guidance` key MUST NOT be honored.) (Source: `skip-model-guidance-question`, 2026-07-26.)
- **INV-130** — A retained visualization artifact (the standalone snapshot) MUST reflect the state of the app at module close, including any in-module customization the Bootcamper requested and approved: the snapshot MUST be rebuilt after **any** change made to the visualization after its initial build, and the module's completion check MUST compare the snapshot against the running app and warn when they diverge. Re-verifying the live server is not a substitute — the server is torn down and the snapshot is what the Bootcamper keeps and the recap embeds, so a change absent from the snapshot leaves the keepsake contradicting the recap prose that describes it. Divergence warns and never blocks module completion (INV-048). (Source: `rebuild-viz-snapshot-after-customization`, 2026-07-26.)
- **INV-131** — The irreversible teardown of a module's working state — terminating a server, purging loaded records — MUST be the **last** action of that module, after every step that needs the data or the running service (snapshot rebuild, live-server screenshot capture, endpoint verification). Once the data is purged those steps become impossible, so an omission upstream of the purge is permanent rather than recoverable. (Source: `rebuild-viz-snapshot-after-customization`, 2026-07-26.)
- **INV-129** — A step that produces a bootcamper-facing deliverable (PDF, PNG, HTML artifact) MUST verify the **rendered artifact**, not only the exit status and any self-reported metric: inspect the output for content the step knows must be present — rasterize the page, open the image, probe the extracted text positively, count unique objects. A zero exit, a written file, and a content-retention figure are necessary and insufficient, because a retention percentage cannot see content positioned outside the page box and an exit code cannot see an image of the wrong thing. Every such check is best-effort and non-blocking (INV-048, INV-052/INV-066), is agent-side apparatus rather than bootcamper-facing output (INV-012), and MUST NOT install a tool to satisfy itself. A cosmetic pass over a deliverable's source MUST additionally prove it preserved content — comparing before/after non-whitespace content and restoring the original on any reduction — because such a pass runs before the render and would otherwise ship a valid, shorter artifact that every downstream metric reports as a success. (Complements INV-110/INV-111, which govern what a *generator* must report; this governs what the *caller* must confirm.) (Source: `artifact-level-verification-for-deliverables`, 2026-07-26.)
- **INV-126** — The Certificate of Completion MUST identify the plugin version that produced the run, rendered on the **certificate face itself** (not only the recap cover card), taken from the recap header's `Plugin version` meta row. When that row is absent the line MUST be omitted, never rendered as a placeholder — the certificate is the page most likely to be detached, printed, or shared on its own, so a wrong or invented value there is permanently visible. Both the preferred and the fallback renderer MUST render it identically in content and position, and every bottom-anchored attribution line MUST clear the certificate's inner border. Verification MUST be by **rasterizing** the page: text extraction reports a border-clipped string as present and correct while the glyphs are visually sliced in half. (Complements INV-100 and INV-113.) (Source: `recap-pdf-certificate-version-and-list-spacing`, 2026-07-26.)
- **INV-127** — Categorical colors in a bootcamper-facing visualization MUST be assigned from the category values **actually present** in the rendered data, never by lookup in a palette keyed by expected value names. A name-keyed map silently collapses every unmatched category to one fallback color — which is what happens to every bootcamper's own data sources, since the shipped palette names the Truth Set's, so the failure is universal and renders as an uninformative-but-plausible graph rather than an error. Assignment MUST be deterministic across rebuilds (so a regenerated snapshot still matches a screenshot the recap already describes), MUST NOT give two present categories the same encoding — varying a second visual channel once the palette is exhausted rather than reusing a color — and MUST NOT assign the reserved signal color. This binds a visualization server written in any language (INV-090), not only the bundled Python reference. (Source: `source-colors-from-discovered-data-sources`, 2026-07-26.)
- **INV-128** — Field completeness/coverage MUST be computed with an explicit emptiness test on the field's **value**: `false`, `0` and `0.0` count as **present** (they are values, not absences), while a missing key, `null`, an empty or whitespace-only string, an empty container, and a container whose every element is itself empty count as **absent**. A truthiness test MUST NOT be used, and **key presence MUST NOT be used as coverage** — that reports full coverage for a field that is empty in every record. Any coverage figure of exactly 0% or exactly 100% across every record MUST be confirmed against a printed sample value before it feeds a gate, a uniform result being the signature of a presence test measuring the wrong thing. (Extends INV-115's treat-a-blank-as-a-plumbing-failure rule from parsed SDK responses to profiling.) (Source: `quality-scoring-presence-test`, 2026-07-26.)
- **INV-121** — A bundled generator that renders a bootcamper-facing deliverable MUST NOT depend on the ambient cursor position for full-width text: every full-width write MUST reset to the left margin first, via a shared helper or an explicit `new_x`, so no block kind can be added that renders off the page. In fpdf2 a `multi_cell` leaves the cursor at the right margin (`new_x=RIGHT`), so an unreset following write draws entirely off-sheet — producing blank space with no error raised, no effect on the content-retention figure (the text is in the content stream, merely outside the page box), and therefore a successful-looking render that has lost content. Verification MUST be positional (the x of every drawn text run lies inside the page's text column), not a text-extraction presence check; `tests/test_discoveries_pdf.py` enforces this and MUST pass. (Source: `discoveries-pdf-offpage-blocks-and-list-spacing`, 2026-07-26.) (⚠️ **Enforcement extended to the second generator 2026-07-31, no meaning change.** This clause named `test_discoveries_pdf.py` alone, which imports `generate_discoveries_pdf` only — so a rule binding "a bundled generator" was guarded in one generator of two, and the unguarded one was the larger and the one carrying the fixed-layout certificate page. `tests/test_recap_pdf_text_column.py` now enforces it for `generate_recap_pdf.py` and MUST pass; both are required. The behaviour was already correct in both, so this closes a coverage gap rather than a defect — the same class-drift shape INV-184 had to close for INV-107's palette. **Its coverage is path-scoped and that is deliberate rather than complete:** the module-prose write path is negative-controlled by injection, the code-block path is not reached by its fixture, and the stdlib fallback positions text differently so the guard skips when `fpdf2` is absent (INV-163). `generate_document_pdf.py` is excluded with its reason — it imports `generate_discoveries_pdf.main` and has no drawing code of its own.)
- **INV-122** — Screenshots of a tabbed visualization MUST be captured as one image per tab, never as several views of one tab, and each file MUST be named after the tab it shows (`<name>-<tab-slug>.png`). A tab that is not present in the page MUST be skipped and reported on stderr, never captured under its name — otherwise the default tab is silently saved as that tab. Capture MUST remain dependency-optional: when no headless backend is available it exits 2 with a message and the module continues unblocked (INV-052/INV-066/INV-048), and the reason reported MUST distinguish "no headless capability" from "no requested tab exists". (Source: `per-tab-screenshot-capture-and-grounded-captions`, 2026-07-26.)
- **INV-123** — Every embedded screenshot caption MUST be derived from the opened image and the tab it shows — never from the visualization contract, the tab inventory, or the capture plan — and MUST NOT assert content the image does not contain. A caption describing an uncaptured view is a false statement in a permanent, shareable artifact and is the same defect class INV-115 forbids for parsed SDK fields. Where a captured tab is inert because it was taken from the static snapshot rather than the live engine (Search / Probe), the caption MUST say so; it MUST NOT imply a result set that was not captured. (Source: `per-tab-screenshot-capture-and-grounded-captions`, 2026-07-26.) (⚠️ **Omission branch removed 2026-07-31, no meaning change.** This clause read "the caption MUST say so **or the image MUST be omitted**". Omission was never available: **INV-146** requires every screenshot the capture produced to reach the recap and permits deleting only a true duplicate of the same tab, so the disjunction offered an option no conforming implementation could take. Worse, it did not even lose the image — graduation's orphaned-screenshot backfill embeds every PNG the recap does not already reference and captions from the tab slug alone, so an omitted Search / Probe capture returns with a bare tab-name caption and **nothing saying the search box is inert**, which is precisely the outcome this clause exists to prevent. The caption is the sole remedy. Both shipped sites were corrected with this: the capture-time embed in `module-completion.md` no longer offers omission, and graduation's backfill is now required to say so when the image it opened shows an empty or inactive search state — the backfill runs exactly when the capture step's own caption never happened.)
- **INV-124** — A visualization server, in whichever language it is generated (INV-090), MUST expose the tab hooks the recap capture depends on: the contract's tab ids as `tab-<id>` sections and `navbtn-<id>` nav buttons, a page-scope `activate(<id>)` function, and `?tab=<id>` / `?q=<text>` deep-linking applied at the end of initialization **after** the nav is built. Deep-linking MUST be tolerant — an unknown or non-applicable `tab` leaves the default tab active rather than erroring or blanking the page — and `?q=` without `?tab=` MUST default to the search tab. (Source: `per-tab-screenshot-capture-and-grounded-captions`, 2026-07-26.)
- **INV-125** — When a documented MCP-first path fails unrecoverably (a tool rejects input without an actionable reason, or a required resource is unavailable), the sanctioned fallback MUST preserve every quality gate the primary path would have run, MUST record the raw failure verbatim in the relevant checkpoint before falling back, and MUST record which path produced each artifact so the deliverable says how it was made. The fallback MUST NOT be offered pre-emptively, MUST be bounded (a fixed retry limit rather than open-ended guessing), and MUST NOT relax the MCP-first sourcing rule (INV-080) — reading an authoritative specification is not guessing, whereas answering from training data remains forbidden. (Source: `mapping-workflow-truncated-validation-errors`, 2026-07-26.)
- **INV-119** — Model/effort guidance at module start and graduation start MUST be governed by a `model_guidance` preference (`advisory` | `off` | `prompt`), captured once in Bootcamp preparation as a single pinned 👉 question (INV-056) and persisted to `config/bootcamp_preferences.yaml`; when the preference is absent the guide MUST behave as `advisory`. Under `advisory` the guidance MUST cost **zero** extra turns — it appears as a line in the module-start apparatus block and the stage's first step lands in the same turn, with no 👉 question and no confirmation gate. Under `off` no model/effort guidance is presented at all. Under `prompt` the guide MUST reproduce the INV-063/INV-069 behavior exactly, both pinned questions verbatim. The guide MUST never change the session model or effort itself — only the Bootcamper can. The apparatus-exempt setup stages (Bootcamp preparation, INV-075; Module 0, INV-078) present no guidance in any mode. (Supersedes the unconditional pause in INV-063 and the confirmation gate in INV-069, retaining both under `prompt`; partially restores INV-062's non-blocking intent.) (Source: `model-effort-guidance-advisory-not-gate`, 2026-07-25.) (Superseded by INV-137: the `advisory`/`off`/`prompt` modes and their capture question are retired; the pause is unconditional again.)
- **INV-120** — The `advisory` model/effort line MUST name the Bootcamper's **current** model and reasoning effort alongside the recommendation, MUST name model and effort as separate dials, MUST state that either can be changed at any time and takes effect on the next message, and MUST explicitly identify a recommendation that sits *below* the current setting as a downgrade — including why its carve-out may not apply — so the nudge never reads as advice to downgrade. Its phrasing adapts to the Claude interface in use per INV-098 (wording aligned to INV-158's interface names 2026-07-28, no meaning change). (Source: `model-effort-guidance-advisory-not-gate`, 2026-07-25.) (Superseded by INV-137, which retires the `advisory` mode; its requirements — separate dials, changeable any time, a below-current recommendation flagged as a downgrade — are retained verbatim on INV-137's recommendation-unchanged statement.)
- **INV-117** — Before the iterate-vs-proceed gate in Data processing validation, the guide MUST audit resolved-entity match keys for semantic correctness: extract `RESOLVED_ENTITY.RECORDS[].MATCH_KEY` using MCP-generated SDK code (never direct SQL against the repository), tabulate contributing (`+`) and suppressing (`-`) features, separate single-source from cross-source keys, and present the result as a finding that feeds the gate's routing — never as a pass/fail that blocks. Structural mapping validation in Data quality, mapping and transformation MUST state that it is structural, not semantic, and MUST NOT be presented as evidence that a mapping is correct. (Source: `post-load-match-key-semantic-audit`, 2026-07-25.)
- **INV-118** — When a source profile reports an implausible field count (roughly >100 fields or >50 distinct field patterns), the guide MUST diagnose the likely cause — dynamic keys using data values as attribute names — rather than reporting the raw count or printing the full column table; MUST cross-check whether those key names are redundant with values already in the record; MUST recommend pre-processing followed by re-profiling as the sanctioned route; and MUST require before/after proof (redundancy shown before removal, record counts and preserved features unchanged after). The check never blocks mapping. (Source: `detect-dynamic-key-document-shaped-sources`, 2026-07-25.)
- **INV-115** — Before writing any code that parses a Senzing SDK response, the guide MUST look up the response structure via `get_sdk_reference(topic='response_schemas', filter='<method>')`, and MUST NOT infer field names from an example snippet — including the illustrative payloads in this plugin's own documentation. Where `response_schemas` does not reach (nesting below the top-level shape it documents), a dumped raw response is the authority. When a parsed field comes back null, empty, or blank, it MUST be treated as a probable wrong field name before being treated as absent data, and MUST NOT be rendered as though it were a real empty result — the UI states that no value was returned, so the failure is visible rather than reading as "Senzing found nothing". (Hardens the MCP-first invariant, which covered method names and flags but not response structures.) (Source: `lookup-sdk-response-schemas-before-parsing`, 2026-07-25.)
- **INV-116** — Every `## Improvement:` entry in `docs/feedback/SENZING_BOOTCAMP_PLUGIN_FEEDBACK.md` MUST carry a `**Source:**` field valued either `bootcamper-reported` or `self-observed (assistant retrospective)`, and graduation MUST run a session retrospective **before** rendering the recap PDF that files self-observed findings (false starts, errors, course corrections, learnings) using the same entry template and durability re-read as the bootcamper-driven feedback flow. The retrospective is strictly non-blocking, is announced in one line rather than gated behind a 👉 question, does not present the feedback-flow entry/exit banners (INV-074), applies the inclusion test "would this happen to another bootcamper?", and records no hostname, username, IP address, or other personal/host identifier (INV-065). (Source: `graduation-assistant-retrospective-feedback`, 2026-07-25.)
- **INV-112** — The Entity Resolution Concepts module MUST present the pinned quiz-offer question verbatim on every run; "optional" and "never blocks" describe the Bootcamper's **answer**, never whether the question is asked, and the offer MUST NOT be collapsed into the mandatory readiness gate that follows. The module MUST record `quiz_offered` and `quiz_taken` in `config/bootcamp_progress.json` at its close, and MUST assert `quiz_offered` before closing — presenting the offer then if it was missed. Declining remains free and blocks nothing. (Hardens INV-005/INV-056 for this question.) (Source: `guarantee-quiz-offer-is-presented`, 2026-07-25.)
- **INV-113** — Before rendering the recap PDF, graduation MUST verify that a certificate-quality Bootcamper name exists and, only when it does not (missing, empty, or clearly not a display name), ask one pinned 👉 question for the name to print on the Certificate of Completion, persisting the answer to `config/bootcamp_preferences.yaml` so it is never asked twice (INV-006). A rejected system-account value MUST never be printed on the certificate or recorded in the recap (INV-065), and `generate_recap_pdf.py` MUST warn on stderr whenever it renders the placeholder name — never silently. Declining never blocks graduation. (Complements INV-100; the upstream detect-never-ask design of **INV-134** is unchanged. Corrected in place 2026-07-26: this parenthetical previously cited INV-076, which governs the Core-vs-Customized path choice and never covered name detection; no meaning change.) (Source: `certificate-name-fallback-at-graduation`, 2026-07-25.)
- **INV-114** — Bootcamper-facing model/effort guidance MUST name only current, non-superseded models and model IDs — including every copy-pasteable `/model` command — and the per-stage recommendation table MUST be identical wherever it appears. `skills/bootcamp-onboarding/ground-rules.md` holds the **authoritative** copy (it is the file loaded at module start, so the INV-063 nudge never depends on fetching another file); `docs/model-selection.md` is derived from it and MUST carry a dated verification note, because model names, IDs, and pricing are point-in-time. `tests/test_model_guidance_sync.py` enforces all of this and MUST pass. (Source: `refresh-model-guidance-to-current-top-tier-model`, 2026-07-25.)
- **INV-110** — A bundled generator that produces a terminal bootcamper deliverable MUST NOT report success after dropping a material share of its input's content: it MUST audit the input **before** rendering, report a content-retention figure, and — on structural mismatch or retention below its stated minimum — write no output file, emit no success line, and exit non-zero. A recognisable-but-incomplete input stays non-blocking (warn, render, exit 0), so an imperfect recap still produces its PDF (INV-066). `tests/test_recap_pdf_guard.py` enforces the two outcome classes for `generate_recap_pdf.py` and MUST pass. (Source: `recap-pdf-generator-fail-loudly-on-content-loss`, 2026-07-25.) (⚠️ **Boundary with INV-048 stated 2026-07-31, no meaning change.** The structural-mismatch refusal above is the **one** case where **INV-048**'s "a recap PDF is always created" does not apply: a keepsake rendered from a document that is not a recap is worse than none, and the bootcamp flow cannot reach this case because graduation writes the recap before rendering it. INV-048's non-optional and non-blocking senses are untouched, and the retention half of this invariant stays deliberately non-blocking — a recognisable-but-incomplete recap warns, renders and exits 0.)
- **INV-111** — When a bundled generator falls back from its preferred renderer or an optional dependency to a lesser path, it MUST state on stderr which case occurred and why — distinguishing "not installed for this interpreter" (naming the interpreter, so a virtualenv mismatch is legible) from "installed but unusable" — so a degraded render is never inferred from silence. The fallback itself still proceeds; only silence is forbidden. (Source: `recap-pdf-generator-fail-loudly-on-content-loss`, 2026-07-25.)
- **INV-051** — Every 👉 question to the Bootcamper that offers two or more choices MUST use a neutral lead question followed by a numbered list; the choices are never joined with "or" (a testable hardening of INV-009). Legitimate yes/no answer-format hints (e.g. "respond yes or no") are exempt. (Source: `interaction-or-questions`, 2026-07-15.)
- **INV-052** — All plugin hooks MUST be Python 3 scripts invoked in Claude Code exec form (`command: "python3"` plus the script path in `args`), so hook execution has no shell dependency on Linux, macOS, or Windows. Hooks may require only `python3` on `PATH` (already a bootcamp prerequisite via the Module 3 visualization server and the graduation recap); any other runtime MUST be optional with a graceful fallback. A hook added as a shell script, or via shell-form invocation, is a violation. (Source: `cross-platform-hook-execution`, 2026-07-15.)
- **INV-053** — Every bootcamp feedback entry written to `docs/feedback/SENZING_BOOTCAMP_PLUGIN_FEEDBACK.md` MUST include a diagnostic "Context when reported" section — capturing, where available, the time, plugin version, current module and step, recent questions and the Bootcamper's responses, behind-the-scenes state, the observed problem, the expected behavior, and the expected-vs-actual divergence — gathered silently (no extra question), with "Unknown" for missing sources. (Source: `enrich-feedback-context`, 2026-07-15.)
- **INV-054** — The Stop-hook closing-question safety net MUST NEVER cause the Bootcamper to see the same 👉 question twice; it MUST block only when it can positively determine the current turn ended without a 👉 question, and MUST stay silent whenever that determination is not decisive (no readable transcript, or the turn's assistant text is not yet flushed). (Source: `stop-hook-false-positive`, 2026-07-16.)
- **INV-055** — The Stop-hook nudge MUST be disableable via a documented opt-out — the `SENZING_BOOTCAMP_DISABLE_STOP_NUDGE` environment variable or a `disable_stop_nudge` key in `config/bootcamp_preferences.yaml`. (Source: `stop-hook-false-positive`, 2026-07-16.)
- **INV-056** — Every mandatory (⛔) gate question presented to the Bootcamper MUST have its exact wording pinned verbatim in the skill file, not left to the model to improvise, so it cannot drift into an INV-008/INV-051 violation at runtime. (Source: `onboarding-explore-gate-wording`, 2026-07-16.)
- **INV-057** — A terminal "END OF SENZING BOOTCAMP" banner MUST be presented exactly once, as the final output, after the Bootcamper declines further exploration at the end of graduation, and never while exploration continues. (Source: `end-of-bootcamp-banner`, 2026-07-16.)
- **INV-058** — Administrative config writes (progress and preferences) MUST be batched to step and module boundaries rather than performed on every sub-step, preferring minimal single writes, and the onboarding preface MUST persist all preface choices in one consolidated write (a concrete hardening of INV-012). (Source: `suppress-admin-write-noise`, 2026-07-16.)
- **INV-059** — The in-progress module recap MUST be checkpointed to `docs/progress/recap_checkpoint.md` at step boundaries and folded — append-only, idempotently, and never rewriting a completed `## Module N:` section — into `docs/bootcamp_recap.md` by the `PreCompact`, `SessionEnd`, and `SessionStart` hooks, so a module interrupted mid-way never loses its recap narrative. (Source: `recap-durability`, 2026-07-16.)
- **INV-060** — CommonMark prettification of bootcamp-authored `*.md` files MUST be deferred to graduation: during the bootcamp the guide writes functional, plain Markdown without incremental lint compliance, and graduation MUST perform a single best-effort, structure- and content-preserving CommonMark normalization pass over `docs/*.md` (before the recap PDF renders) and the generated `production/*.md`, never reordering, removing, or rewriting completed `## Module N:` recap sections or their four required subsections. (Source: `defer-commonmark-to-graduation`, 2026-07-16.)
- **INV-061** — The bootcamp MUST auto-detect the operating system and architecture (from the environment, else `uname`/`systeminfo`), persist them during onboarding, and reuse them in later modules rather than re-asking; a platform question may be presented only as a fallback when detection is genuinely unavailable or ambiguous, and its wording stays pinned verbatim (INV-056). (Source: `auto-detect-platform`, 2026-07-16.)
- **INV-062** — At every module start, and at graduation start, the guide MUST surface the recommended session model and reasoning effort as a concise, non-blocking suggestion carrying the exact `/model` and `/effort` commands — never as a 👉 question or ⛔ gate, and never blocking progress. The specific per-stage model/effort values are advisory (maintained in `docs/model-selection.md`) and are not part of the invariant. (Source: `module-start-model-nudge`, 2026-07-16.) (Superseded by INV-063.)
- **INV-063** — At every module start and graduation start, the guide MUST surface the recommended session model and reasoning effort with the exact `/model` and `/effort` commands. When the recommendation **changes** from the current stage, it MUST pause with a single 👉 yes/no question offering the switch — its own yielding turn, never combined with another 👉 question; when unchanged, it remains a concise, non-blocking statement. The guide MUST never change the session itself and MUST never block beyond that one optional question. The specific per-stage model/effort values are advisory (maintained in `docs/model-selection.md`) and are not part of the invariant. (Supersedes INV-062.) (Qualified by INV-098: the "exact commands" apply to the Claude Code CLI; in Claude Desktop, the Claude web app, or a Claude IDE extension, or when the interface is unknown, the nudge phrases the recommendation by intent. Wording aligned to INV-158's interface names 2026-07-28, no meaning change.) (The apparatus-exempt setup modules — Bootcamp preparation, INV-075, and Module 0, INV-078 — do NOT present this nudge, consistent with their module-start apparatus exemption and mirroring INV-096's estimate exemption; clarified in place 2026-07-23, no meaning change.) (Source: `model-effort-change-prompt`, 2026-07-16.) (Superseded by INV-119, which makes this pause conditional on the `prompt` model-guidance preference rather than unconditional; the behavior described here is retained verbatim under that mode.) (INV-119 in turn superseded by INV-137, which restores this pause unconditionally — so the behavior described here is once again the behavior, with no preference gating it.)
- **INV-064** — When the Bootcamper accepts a recommended model/effort switch at a module start or graduation start, the guide MUST continue the accepted path in a **single** turn: a one-line statement instructing the `/model`/`/effort` commands, immediately followed by the stage's first step, ending that turn on that step's single 👉 question. It MUST NOT insert a separate confirmation-only gate between the switch and the first step, and MUST NOT defer the first step to a later turn. (Hardens INV-005/INV-054 for the accepted-switch continuation; complements INV-063.) (Source: `model-switch-single-turn-continuation`, 2026-07-16.) (Superseded by INV-069.)
- **INV-065** — A sanitized, non-PII example recap MUST ship inside the plugin — a source `.md` fixture at `plugins/senzing-bootcamp/docs/examples/bootcamp_recap.example.md` and its rendered `bootcamp_recap.example.pdf` — the PDF MUST remain regenerable from the `.md` via `generate_recap_pdf.py`, and neither MUST contain real personal data. (Complements INV-048; hardens INV-004 for shipped example assets.) (Source: `example-recap-reference`, 2026-07-17.)
- **INV-066** — Any Python package install the plugin instructs MUST use an explicit interpreter (`python3 -m pip`, never bare `pip`) and be robust to PEP 668 externally-managed environments (prefer a project-local virtualenv), and MUST NOT modify the global/system Python; where a working fallback exists (e.g. the recap PDF's stdlib renderer), an install failure MUST degrade to it rather than block. (Hardens INV-052/INV-048 and the file-placement rules.) (⚠️ **Scope stated 2026-08-14, no meaning change — see INV-222.** This governs the plugin's **own tooling** installs (`fpdf2`, Playwright). It does **not** reach the Senzing SDK's language packages, which INV-222 forbids installing from a package manager at all. The distinction needs stating because this invariant is *satisfiable by* the forbidden command: Module 2 instructed `python3 -m pip install senzing` with the bare-`pip` prohibition honoured and PEP 668 handled, so a reviewer checking that step against this invariant found it compliant — which is how it survived three audits. Nothing here changes; INV-222 carves the SDK out.) (Source: `robust-fpdf2-install`, 2026-07-17.)
- **INV-067** — When bootcamp feedback is appended to `docs/feedback/SENZING_BOOTCAMP_PLUGIN_FEEDBACK.md`, the write MUST be verified to have landed (re-read and confirm the entry is present, re-appending if not) before the bootcamper is told it was saved, and no later bootcamp step (CommonMark normalization, production build, cleanup) may delete or empty that file. (Hardens INV-015; mirrors the recap verify of INV-059.) (Source: `feedback-file-durability`, 2026-07-17.)
- **INV-068** — Module 3 MUST register the TruthSet data source codes (CUSTOMERS, REFERENCE, WATCHLIST for the standard TruthSet, or the codes present in the acquired data for the Step 2a substitute) and commit them as the default engine config **before** the Step 6 data load runs, so the load never fails with SENZ2207 for those codes on the first attempt. (Upholds INV-038.) (Superseded by INV-083.) (Source: `module3-register-truthset-data-sources`, 2026-07-17.)
- **INV-069** — When the Bootcamper accepts a recommended model/effort switch at a module start or graduation start, the guide MUST end that reply turn on a single confirmation gate whose wording is pinned verbatim (INV-056) — "👉 Are you done modifying the model and effort?" — presented after the one-line `/model`/`/effort` run-commands statement, and MUST defer the stage's first step to the turn after the Bootcamper confirms. The gate is asked only once (INV-006); the switch-offer question remains its own prior yielding turn (INV-063). On a declined switch, the first step lands on the reply turn with no gate. (Supersedes INV-064.) (Source: `model-effort-switch-done-confirmation`, 2026-07-17.) (Superseded by INV-119, which retains this gate under the `prompt` model-guidance preference only; INV-119 in turn superseded by INV-137, which restores this gate unconditionally after a yes to the switch.)
- **INV-070** — Every generated HTML visualization the bootcamp produces MUST be written under the generated project's `docs/visualizations/` directory, never the `docs/` root or another `docs/` subdirectory. (Hardens INV-050.) (Source: `layout-tree-reconciliation`, 2026-07-17.)
- **INV-071** — The bundled visualization (`scripts/senzing_viz_server.py`) MUST render with no network access: D3 is vendored inside the plugin (`scripts/vendor/d3.v7.min.js`) and inlined into both the live page and the standalone snapshot, with the `d3js.org` CDN referenced only as a fallback when the vendored asset is missing. (Hardens INV-004/INV-038; complements INV-052.) (Superseded by INV-091: the offline-rendering guarantee now covers the Truth Set visualization server built in the Bootcamper's chosen language, not only the bundled Python one.) (Source: `vendor-d3-offline-visualization`, 2026-07-17.)
- **INV-072** — Entity Resolution Concepts is delivered as an optional "Module 0", presented after the onboarding preface and before Module 1, offered via a pinned-verbatim (INV-056) 👉 skip/keep gate. It is NOT counted among the mandatory numbered Modules 1–7 (exempt from INV-013's ordering) and skipping it is a permitted, requested skip (exempt from INV-014); it does NOT run the per-module completion apparatus (no journey map, before/after, step overview, `docs/bootcamp_recap.md` section, or `modules_completed` entry). (Source: `entity-resolution-module-zero`, 2026-07-17.) (Superseded by INV-078: Module 0 inclusion is driven by Bootcamp preparation selection; the skip/keep gate is retired.)
- **INV-073** — When the Bootcamper chooses to run Module 0 (does not skip it), Module 0 MUST present the ENTITY RESOLUTION CONCEPTS banner, a Senzing-MCP-sourced description of entity resolution, and the pinned-verbatim explore gate ("Are you ready to move on to the next module: Discover the Business Problem?") before handing off to Module 1. (Supersedes INV-019, INV-020, INV-021.) (Source: `entity-resolution-module-zero`, 2026-07-17.)
- **INV-074** — The bootcamp feedback workflow MUST be bracketed by pinned-verbatim banners: a "BOOTCAMP FEEDBACK" entry banner before its first 👉 question, and a "FEEDBACK SAVED — BACK TO THE BOOTCAMP" exit banner (a statement, not a question) after the entry is confirmed saved and before the pending bootcamp 👉 question is re-presented. (Complements the bookend banners INV-022/INV-047/INV-057; upholds INV-005/INV-006 on resume.) (Source: `feedback-flow-boundary-banner`, 2026-07-17.)
- **INV-075** — Bootcamp preparation is the first, mandatory module, run after the onboarding WELCOME preface. It presents the Core-vs-Customized path choice, per-module selection, the level-of-detail (verbosity) question, the programming-language question, and the version-control (git-init) question, and persists them in one consolidated write (INV-058). It is a lightweight setup module, exempt from the per-module completion apparatus (no journey map, before/after framing, step overview, `docs/bootcamp_recap.md` section, or `modules_completed` entry). (Relocates the verbosity and programming-language outcomes out of the preface; supersedes INV-024 and INV-026.) (See also INV-088: the software-integration and deployment-target questions were relocated into this module, then moved back to Module 1 Phase 2 by INV-097.) (The version-control/git-init **question** is removed by INV-095 — git is initialized automatically with no prompt; the git-init action and its persisted `git_init` value are retained.) (INV-099 relaxes this exemption to add a bootcamper-facing setup-choices recap at handoff; prep is still never added to `modules_completed` and still writes no recap-file section.) (Source: `customizable-module-selection`, 2026-07-18.)
- **INV-076** — In Bootcamp preparation the Bootcamper chooses a path: Core (every module, in order) or Customized (the required modules plus whichever optional modules the Bootcamper selects, run in prerequisite/dependency order). Required modules are always included and cannot be deselected; an optional module the Bootcamper does not select is a requested skip (INV-014). (Supersedes INV-013 and INV-025.) (Source: `customizable-module-selection`, 2026-07-18.)
- **INV-077** — The guaranteed Truth Set web-app visualization is delivered by the selectable "Truth Set visualization" module (a standalone module, `module-03b-truthset-visualization`; formerly Module 3, Phase 2 — see INV-087): it MUST be produced whenever that module is selected (always in Core; in Customized only if chosen); when it is not selected, no workstation-verification visualization is produced. (Supersedes INV-038; INV-070/INV-071 on how the visualization renders still apply when it runs.) (Source: `customizable-module-selection`, 2026-07-18; standalone-module location clarified by `split-truthset-visualization-into-standalone-module`, 2026-07-20.)
- **INV-078** — The optional Entity Resolution Concepts primer (Module 0) runs if and only if it was selected during Bootcamp preparation (Core always includes it; Customized includes it only if chosen); it has no separate skip/keep gate. When it runs it still presents the ENTITY RESOLUTION CONCEPTS banner, an MCP-sourced description, and the pinned explore gate (INV-073), and remains a lightweight preamble exempt from the per-module completion apparatus. (Supersedes the skip/keep-gate mechanism of INV-072.) (Amended by INV-092: Module 0 now appends its own name-based recap section and is added to `modules_completed` at its close, though it stays exempt from the rest of the module-start apparatus and the bootcamper-facing end-of-module summary.) (Source: `customizable-module-selection`, 2026-07-18.)
- **INV-079** — Bootcamper-facing module references use the module NAME, not a fixed number: the module-start banner is "MODULE: [NAME IN CAPS]" (no number); module-transition/readiness 👉 questions name the next module ("Are you ready to move on to the next module: {name}?"); and the end-of-module completion line is "✅ Module complete: {Module name}" (no number), lightly highlighted (bold, wrapped in a thin `─` rule — more visible than plain prose, lighter than the module-start banner). Internal references (module titles, prerequisites, cross-references, recap `## Module N:` headings, progress keys) may still use numbers. (Supersedes INV-028.) (The recap `## Module N:` heading clause is superseded by INV-085: recap headings are name-based.) (Source: `module-references-by-name-not-number`, 2026-07-18.)
- **INV-080** — Every bootcamp skill that can produce bootcamper-facing Senzing content MUST carry, prominently near the top of its `SKILL.md`, an explicit MCP-grounding / no-speculation clause — stating that all Senzing facts (SDK method and attribute names, config options, error codes, and entity-resolution specifics) come from the Senzing MCP tools and never from training data or speculation, and including the pre-response checklist — not merely a passing reference to "MCP-first". The canonical rule and tool routing remain the "MCP-first invariant" in `ground-rules.md`. (Source: `mcp-grounding-in-every-skill`, 2026-07-18.)
- **INV-081** — Every bootcamper-facing visual deliverable the bootcamp generates (the Truth-Set visualization web app and its standalone snapshot, the recap PDF, and any future generated charts/dashboards/HTML/PDF) MUST take its palette and typography from the shared Senzing brand tokens shipped at `plugins/senzing-bootcamp/scripts/brand_tokens.py` — the single shipped source (extracted from `resources/senzing-style-reference.pdf`, which is NOT required at runtime) — never an ad hoc per-generator palette. Consumers MUST fall back gracefully if the token module is absent and MUST keep rendering offline (no web-font/CDN fetch; upholds INV-071). (Source: `apply-senzing-style-guide-to-deliverables`, 2026-07-18.)
- **INV-082** — System Verification (Module 3) MUST verify with synthetic records that are deterministic **by construction** (designed to resolve into a known number of entities), and MUST NOT acquire, load, or visualize the Senzing Truth Set. The Truth Set is used exclusively by the selectable Truth Set visualization module (a separate, standalone module — see INV-087), which acquires and loads it itself. (Supersedes the Truth-Set coupling of INV-038/INV-068; the visualization guarantee itself remains INV-077.) (Source: `module3-synthetic-verification-data`, 2026-07-19; standalone-module location clarified by `split-truthset-visualization-into-standalone-module`, 2026-07-20.)
- **INV-083** — For both the System Verification load and the Truth Set visualization load, the data source code(s) present in the data about to be loaded MUST be registered as the default engine config **before** each load — the synthetic `VERIFY` code before the System Verification load (Module 3), and the Truth Set codes before the Truth Set visualization load (in the standalone Truth Set visualization module) — so no load fails with `SENZ2207` on the first attempt. (Supersedes INV-068, generalizing its register-before-load guarantee to the synthetic and Truth-Set loads.) (Source: `module3-synthetic-verification-data`, 2026-07-19; module locations clarified by `split-truthset-visualization-into-standalone-module`, 2026-07-20.) (⚠️ **See INV-089 before implementing, 2026-08-11.** It restates this register-before-load guarantee for *every* load — the two named here **and** the Module 5 Phase 3 test load and the Module 6 production load(s) — so satisfying this invariant alone leaves the later loads unguarded. This invariant is **not** superseded: it names the specific codes each bootcamp load registers, which INV-089 generalises to "the `DATA_SOURCE` code(s) present in the data". Both bind.)
- **INV-084** — Module 5's mapping/transformation output (Senzing-ready, load-ready JSONL) MUST be written to `data/senzing-ready/`, and every consumer (Module 6 loaders, graduation, the visualization server) MUST read it from there — never the former `data/transformed/`. (Renames the mapping-output directory; the INV-050 layout tree is updated to match.) (Source: `rename-transformed-to-senzing-ready`, 2026-07-19.)
- **INV-085** — Recap section headers in `docs/bootcamp_recap.md` and the rendered PDF MUST be name-based (`## {Module name} — {timestamp}`, no catalog module number), appended in the bootcamper's experienced (module-completion) order, and every module in `modules_completed` MUST have its own section by graduation; the recap PDF renderer (`generate_recap_pdf.py`) MUST parse name-based headers, still tolerating legacy `## Module N:` headers. (Amends the recap-heading clause of INV-079.) (Source: `recap-sections-name-based-and-complete`, 2026-07-19.) (Per INV-103 the renderer's required fourth subsection is "End-of-Module Summary", replacing "Journal"; legacy "Journal" headings remain tolerated by `--check`.)
- **INV-086** — When the Truth Set visualization module (Module 3, Phase 2) runs, the Module 3 close MUST record `truthset_visualization` as its own `modules_completed` entry (a module name token, placed after `system_verification` in experienced order) and append its own name-based `## Truth Set visualization` recap section — so it is a first-class module produced by the flow, not only recovered by graduation's reconcile backfill. `modules_completed` entries are module name tokens, never catalog numbers. As a first-class module it also receives the **full per-module apparatus**: at its Phase 2 start it presents a module-start banner ("MODULE: TRUTH SET VISUALIZATION"), a journey map with it shown `🔄` current, before/after framing, a step overview (INV-028–031/INV-079), and the model/effort nudge (INV-063); at Module 3 close it gets its own end-of-module summary and `✅ Module complete: Truth Set visualization` line (INV-032). It is NOT apparatus-exempt — contrast the explicitly-exempt Bootcamp preparation (INV-075) and Module 0 (INV-078). (The "Phase 2 start" / "Module 3 close records both" recording-location framing is **superseded by INV-087**, which makes Truth Set visualization a standalone module that presents its apparatus at its own module start and records itself at its own close; the first-class-module, full-apparatus, not-apparatus-exempt guarantee stated here otherwise stands.) (Hardens INV-077/INV-085; the full-apparatus clause clarified in place 2026-07-19, no meaning change — a "first-class module" always implied INV-028–032.) (Source: `record-truthset-visualization-completion`, 2026-07-19; full-apparatus clarification: `truthset-visualization-full-apparatus`, 2026-07-19.)
- **INV-087** — Truth Set visualization is a **standalone module** in its own skill directory (`module-03b-truthset-visualization`), separate from System Verification (Module 3): it presents its full per-module apparatus at its **own** module start and runs its **own** module-completion at its **own** close — recording `truthset_visualization` in `modules_completed` and appending its own name-based recap section there (not at the System Verification / Module 3 close), then asking its own transition question; System Verification records only `system_verification` at its own close. (Supersedes the "Phase 2 start" / "Module 3 close records both" recording-location framing of INV-086; INV-086's first-class-module, full-apparatus, not-apparatus-exempt guarantee otherwise stands.) (Source: `split-truthset-visualization-into-standalone-module`, 2026-07-20.)
- **INV-088** — The software-integration question ("will your entity-resolution results need to interface with other software") and the deployment-target question ("where do you plan to deploy the final solution") are asked in the **Bootcamp preparation** module (not in Module 1 / Discover the Business Problem), each as its own pinned-verbatim (INV-056) 👉 question, and their answers (`integration_targets`, `deployment_target`/`cloud_provider`) are persisted in Bootcamp preparation's single consolidated write (INV-058) and read from `config/bootcamp_preferences.yaml` by Module 1's problem statement and by graduation. (Extends the Bootcamp-preparation setup set of INV-075.) (Superseded by INV-097: these two questions are moved back to Module 1 Phase 2 (Step 10a), asked after the scenario and before the artifacts; Bootcamp preparation no longer asks them.) (Source: `relocate-setup-questions-to-bootcamp-preparation`, 2026-07-20.)
- **INV-089** — For every load into the Senzing repository — the System Verification and Truth Set visualization loads already covered by INV-083, **and** the Module 5 Phase 3 test load and the Module 6 production load(s) — the `DATA_SOURCE` code(s) present in the data MUST be registered as the default engine config, idempotently, **before** that load runs, so no load fails with `SENZ2207` on the first attempt. (Generalizes INV-083's register-before-load guarantee to the production load.) (Source: `module6-register-data-sources-before-load`, 2026-07-20.)
- **INV-090** — The Truth Set visualization server MUST be built and run in the Bootcamper's chosen programming language (`programming_language` in `config/bootcamp_preferences.yaml`), modeled on the shipped reference `scripts/senzing_viz_server.py` and the `visualization-api-reference.md` contract; the bundled Python `scripts/senzing_viz_server.py` is run directly ONLY when the chosen language is Python, and for any other language is a reference model that is never run during the bootcamp. (Reverses the earlier "bundled Python server as the running default" decision; strengthens INV-002; the produced-when-selected guarantee remains INV-077, offline rendering INV-091, brand tokens INV-081.) (Source: `visualization-server-in-chosen-language`, 2026-07-21.)
- **INV-091** — The Truth Set visualization server — the language-native one generated in the module, or the bundled Python `scripts/senzing_viz_server.py` when Python is chosen — MUST render with no network access: D3 is vendored at `scripts/vendor/d3.v7.min.js` and inlined into both the live page and the standalone snapshot, with the `d3js.org` CDN referenced only as a fallback when the vendored asset is missing. (Generalizes INV-071 — which named only the bundled Python server — to the chosen-language server; supersedes INV-071.) (Source: `visualization-server-in-chosen-language`, 2026-07-21.)
- **INV-092** — When the Entity Resolution Concepts module (Module 0) runs, it MUST — notwithstanding its module-start apparatus exemption (INV-078) — add `entity_resolution_concepts` to `modules_completed` and append its own name-based recap section (`## Entity Resolution Concepts — {timestamp}`, with the four subsections Information Shared / Questions & Responses / Actions Taken / Journal) to `docs/bootcamp_recap.md` at its close, so it is captured in the recap and reconciled at graduation (INV-085). It remains exempt from the rest of the module-start apparatus (no module-start banner beyond its ENTITY RESOLUTION CONCEPTS banner, no journey map of its own, no before/after framing, no step overview, and no bootcamper-facing end-of-module summary). Bootcamp preparation stays fully exempt and is never added to `modules_completed`. (INV-099 relaxes "fully exempt" to add a bootcamper-facing setup-choices recap at Bootcamp preparation's handoff; prep is still never added to `modules_completed` and still appends no recap section.) (Amends the "no recap section / not in `modules_completed`" clause of INV-072/INV-078.) (Source: `capture-entity-resolution-concepts-in-recap`, 2026-07-21.) (The fourth subsection "Journal" is renamed to "End-of-Module Summary" by INV-103; legacy "Journal" headings remain tolerated.)
- **INV-093** — The Senzing License Key prompt MUST be presented at most once across the entire bootcamp — at the start of Data collection (Module 4), after the Bootcamper's data volume is known and before any load, and only when that volume exceeds the active license's record limit. SDK setup (Module 2) MUST establish only the built-in evaluation license with no license prompt, and every other module MUST read the persisted result (`license_record_limit` in `config/bootcamp_progress.json`, or `license` in `config/bootcamp_preferences.yaml`) rather than re-asking (INV-006). (Relocates the custom-License-Key gate of INV-036 to Module 4; INV-036's "a license is established" remains satisfied in Module 2 by the built-in evaluation license.) (Source: `single-license-gate-at-data-processing`, 2026-07-22.)
- **INV-094** — Graduation MUST produce a silent, non-blocking revisit/resume bundle before the terminal banner, comprising: (a) a database backup under the reserved top-level `backups/` directory (a `pg_dump` for PostgreSQL — via `docker exec` when the database runs in a container — or a file copy of the SQLite repository), (b) a `RESUME_STATE` manifest snapshotting the resume-critical state (`config/bootcamp_progress.json`, `config/bootcamp_preferences.yaml`, `config/data_sources.yaml`, `config/engine_config.json`, `config/license.json`, and `docs/mapping/`), and (c) a `docs/REVISIT_BOOTCAMP.md` return guide covering how to restore the database, re-initialize the engine, and re-run the pipeline. Every part warns-and-continues on failure (never blocking graduation) and asks before overwriting an existing bundle or performing any destructive action; the bundle lives outside `production/`, so the Step 2 "never copy the eval database into `production/`" rule is preserved. (Source: `graduation-revisit-resume-bundle`, 2026-07-22.)
- **INV-095** — In Bootcamp preparation, when the working directory is not already a git repository, the bootcamp MUST initialize one automatically (`git init`) as a quiet administrative action with **no 👉 question**, recording `git_init: true` (or `git_init: existing` when already a repo, or `git_init: unavailable` when `git` is absent) in the single consolidated preference write (INV-058); it MUST NOT prompt about version control. (Removes the git-init *question* from INV-075's enumerated Bootcamp-preparation questions; the git-init *action* and its persisted `git_init` value are retained.) (Source: `auto-initialize-git-without-prompt`, 2026-07-22.)
- **INV-096** — Every numbered content module's start apparatus MUST include an estimated time-to-complete, placed after the step overview and before the model/effort prompt: honest and range-based, explicitly caveated as varying with workstation power, business-scenario complexity, data volume, and download/install time, and stated as "hard to estimate" rather than fabricated when no meaningful estimate is possible. It is explanatory output — suppressed under the `minimal` verbosity preset and one line under `concise` (INV-011/INV-012) — and does NOT apply to the apparatus-exempt setup modules (Bootcamp preparation INV-075, Module 0 INV-078). (Source: `module-preface-time-estimate`, 2026-07-22.)
- **INV-097** — The software-integration question ("will your entity-resolution results need to interface with other software") and the deployment-target question ("where do you plan to deploy the final solution") are asked in Module 1 (Discover the Business Problem), Phase 2, after the scenario is identified (Step 10) and before the problem-statement artifacts are written (Step 11), each as its own pinned-verbatim (INV-056) 👉 question; their answers (`integration_targets`, `deployment_target`/`cloud_provider`) are persisted to `config/bootcamp_preferences.yaml` there and read by the Module 1 problem statement and by graduation. Bootcamp preparation does NOT ask them. (Supersedes INV-088, reversing the relocation into Bootcamp preparation; the programming-language question remains in Bootcamp preparation per INV-075.) (Source: `relocate-integration-deployment-questions-to-module1`, 2026-07-22.)
- **INV-098** — The module/graduation-start model/effort nudge MUST adapt to the Claude interface in use: on the Claude Code CLI it presents the exact `/model` and `/effort` commands (INV-063); in Claude Desktop, the Claude web app, or a Claude IDE extension — or when the interface is unknown — it phrases the recommendation by intent (naming the recommended model and reasoning-effort level and directing the bootcamper to that interface's model/effort controls), never presenting CLI-only commands as the sole instruction and never hardcoding a UI control label that may drift. The pinned switch question and confirmation gate (INV-063/INV-069) keep their interface-neutral core wording; only the command/instruction detail and the gate's answer hint adapt. (Qualifies INV-063's "exact commands" clause to the Claude Code CLI.) (Source: `surface-aware-model-effort-instructions`, 2026-07-22.) (Wording clarified in place 2026-07-28 per **INV-158**, no meaning change: "surface" → "interface", and the vague "their Claude app's controls" replaced by naming the interface. The four interfaces it distinguishes are unchanged.)
- **INV-099** — Bootcamp preparation MUST close, before handing off to the first content module, with a concise bootcamper-facing recap of the setup choices it collected (path, selected modules, verbosity, programming language, and git status), read from the consolidated preference write and respecting the active verbosity preset. It otherwise remains apparatus-exempt: it is NOT added to `modules_completed` and appends NO `docs/bootcamp_recap.md` section (INV-092 preserved), so it never appears in the graduation recap PDF. (Relaxes only the "no bootcamper-facing end-of-module summary" part of Bootcamp preparation's exemption in INV-075/INV-092.) (Source: `bootcamp-preparation-end-of-module-recap`, 2026-07-22.)
- **INV-100** — The recap PDF (`docs/bootcamp_recap.pdf`) MUST end with a Certificate of Completion page carrying the bootcamper's name, the bootcamp date, and the modules completed, rendered in **landscape** orientation while every other page stays portrait — in both the fpdf2 renderer and the stdlib fallback (INV-066). The certificate takes its palette and typography from `brand_tokens` (INV-081) and renders offline; the four required recap subsections and `--check` remain satisfied (INV-048). (Source: `landscape-certificate-of-completion`, 2026-07-22.)
- **INV-101** — Any Docker container the bootcamp starts MUST be recorded in `config/bootcamp_progress.json` (a `docker_containers` list, each entry carrying at least the container name), and the python3 `SessionEnd` hook MUST stop those recorded containers when the bootcamp is active and the `docker` CLI is available (`docker stop`, not remove, so they can be restarted), while the python3 `SessionStart` hook surfaces the recorded containers on resume for the guide to restart or regenerate. All Docker interaction is optional and gated on `docker` availability: when `docker` is absent or errors, every step warns-and-continues and never blocks the hook (INV-052/INV-001/INV-002). (Source: `docker-container-lifecycle-teardown-and-resume`, 2026-07-22.) (⚠️ **Its Docker-only scope is superseded by INV-195, 2026-08-11.** That invariant generalises this rule to any recorded container runtime — `docker`, `podman`, or Apple's `container` — and every lifecycle action MUST dispatch on the recorded runtime rather than assuming Docker, so an implementation written from this invariant alone would be Docker-only and wrong. **Every other guarantee here stands**, including the `config/bootcamp_progress.json` recording requirement and the warn-and-continue contract. Read INV-195 first.)
- **INV-102** — Graduation MUST open with the module-start apparatus, adapted to a terminal module: after the GRADUATION banner and before the model/effort prompt it presents a journey map (all selected modules shown ✅ experienced, Graduation 🔄 as the current/final stage, nothing upcoming), before/after framing, a numbered step overview, and an estimated time-to-complete (INV-096-style: honest, range-based, caveated; suppressed under the `minimal` verbosity preset and one line under `concise`). Graduation stays terminal — no next-module transition, no `✅ Module complete` line — and ends on the single END OF SENZING BOOTCAMP banner (INV-057). (Applies INV-029–032 and extends INV-096 to graduation; graduation is therefore NOT apparatus-exempt — contrast Bootcamp preparation INV-075 and Module 0 INV-078.) (Source: `graduation-full-module-preface`, 2026-07-23.)
- **INV-103** — Each per-module recap section in `docs/bootcamp_recap.md` (and the rendered PDF) MUST carry exactly four labeled subsections: Information Shared, Questions & Responses, Actions Taken, and **End-of-Module Summary** (What you accomplished / Files produced / Why it matters, plus an optional "Bootcamper's takeaway" omitted when empty). The End-of-Module Summary replaces the former **Journal** subsection — persisting the bootcamper-facing end-of-module epilog (INV-032) into the keepsake and removing the Journal/summary redundancy. `generate_recap_pdf.py` MUST render it and its `--check` MUST validate it, while still tolerating legacy recaps whose fourth subsection is titled "Journal" (treated as an alias). (Supersedes the "Journal" subsection-naming clause of INV-048, INV-085, and INV-092; the four-required-subsections guarantee otherwise stands.) (Source: `consolidate-recap-per-module-summary`, 2026-07-23.)
- **INV-104** — The bootcamp's results / entity-resolution visualizations MUST be delivered as ONE consolidated, tabbed interactive app (built per INV-090 from `visualization-api-reference.md`), never as separate static HTML pages. The entity graph, relationship network, results dashboard, cross-source overlap, match-key frequency, and feature-score views are tabs of that single app; a tab is shown only when its data exists; and no redundant tab duplicates another (entity-size distribution = Merge Statistics; the cross-source entity-relationship view = Entity Graph). Module 7 offers this single app via one pinned gate (INV-056), which counts as offering the entity graph and results dashboard for INV-046. (Source: `consolidate-module7-visualizations-as-truthset-app-tabs`, 2026-07-23.) (The **tab enumeration** in this invariant — which named "relationship network" and "results dashboard" as tabs — is superseded by **INV-155**; every other guarantee here stands.)
- **INV-105** — The bootcamp MUST display the SBCP plugin version (read from `.claude-plugin/plugin.json`) to the bootcamper at bootcamp start (verbosity-aware, suppressed under the `minimal` preset), and MUST record in `docs/bootcamp_recap.md` and the rendered `docs/bootcamp_recap.pdf` both the plugin version and a run-environment provenance block — operating system + architecture, Python version, chosen-language runtime, Senzing SDK/engine version, and database backend. The environment block is recap-only (never shown in bootcamp output, INV-012), reuses the persisted OS/arch detection (INV-061) and the Senzing MCP tools for the SDK version (INV-080), renders in both the fpdf2 and stdlib recap renderers (INV-066), and contains no hostname/username/IP or other PII (INV-065). (Clarified 2026-07-26, no meaning change: "verbosity-aware" applies **where a preset is readable**. The version is displayed in the onboarding preface, but INV-075 moved the verbosity question into Bootcamp preparation, which runs *after* it — so on a fresh bootcamp no preset exists yet and the version line is simply shown. The suppression path is reachable on a resumed run or when the Bootcamper pre-seeded the preference (INV-133). The preface MUST NOT ask for verbosity to satisfy this. Found by the phase-3 conversational dry run.) (Source: `show-plugin-version-and-record-environment`, 2026-07-23.)
- **INV-106** — Any value embedded into an inline `<script>` block in generated HTML (e.g. the self-contained visualization snapshot) MUST have `<`, `>` and `&` escaped as their `\uXXXX` JSON escapes after serialization — a plain JSON writer alone is NOT sufficient, because JSON does not escape `<`, so a data-sourced string containing `</script>` breaks out of the script context (stored-XSS guard, in an artifact that is saved and shared). Data-sourced strings written into rendered HTML MUST likewise be escaped for that context. Live `/api/*` responses served as `application/json` are exempt (not an HTML-embed surface). Because the visualization server is built in the Bootcamper's chosen language (INV-090), this requirement MUST be stated as behaviour in `visualization-api-reference.md` and satisfied in whatever language the server is written in; `senzing_viz_server.py`'s `_script_json` / `_esc_html` are the **Python reference implementation** of it, not the requirement itself. (Wording clarified in place 2026-07-26 — no meaning change: the guard always applied to generated HTML in any language, but naming only the Python helper left it unsatisfiable for a non-Python server and the requirement absent from the any-language contract. Source: `escape-viz-snapshot-script-payload`, 2026-07-24.)
- **INV-107** — The inlined brand-palette fallback constants in `senzing_viz_server.py` (`_FALLBACK_BRAND`, `_FALLBACK_SOURCE_COLORS`, `_FALLBACK_COLORS`) and `generate_recap_pdf.py` (`_FALLBACK_RGB`) MUST equal the values derived from the shared `brand_tokens.py`, so the offline-fallback copies cannot drift from the source of truth; `tests/test_brand_sync.py` enforces this and MUST pass. (Source: `pr4-review-minor-fixes`, 2026-07-24.) (⚠️ **Generalized by INV-184 — read it before implementing, 2026-08-12, no meaning change.** This invariant *enumerates two generators*; **INV-184** binds **every** shipped generator that inlines a fallback copy of the brand palette, additionally requires that copy to live in a single named constant rather than repeated literals per code path, and requires `tests/test_brand_sync.py` to assert it. A third generator, `generate_discoveries_pdf.py`, drifted out of scope unnoticed on exactly this wording — while its own comment claimed a test asserted it — which is *why* INV-184 exists. So an implementation written from the two files named here reproduces the defect INV-184 records rather than conforming to it. **Nothing here is superseded:** these constants and their equality still bind, and `tests/test_brand_sync.py` covers all three generators today. The pointer was missing because INV-184 recorded the widening on itself alone, which is the same class-drift shape one level up.)
- **INV-108** — Dev-only tests MUST live in the repo top-level `tests/` directory (never under `plugins/`), so the propagate step — which mirrors `plugins/` into the public repo — never ships them to bootcampers. They MUST rely only on the standard library and run via `python3 -m unittest discover -s tests`. (Source: `write-gate-tests`, 2026-07-24.)
- **INV-109** — The PreToolUse write-gate (`write-gate.py`) MUST detect, at minimum, PEM private keys, AWS access-key IDs, and Senzing `AQAAAD` license blobs, blocking the write with `SECRET_MSG`; the secret check runs independently of the location logic and fails closed. (Source: `harden-write-gate`, 2026-07-24.)
